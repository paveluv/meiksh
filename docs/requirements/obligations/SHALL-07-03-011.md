---
id: SHALL-07-03-011
category: shall
section: "7.3 Locale Definition"
anchor: tag_07_03
source_file: basedefs/V1_chap07.html
source_lines: [158, 158]
keywords: ["shall"]
tier: 2
status: enriched
refs: []
related_sections: []
tests: []
---

## Extracted Text

> A line can be continued by placing an escape character as the last character on the line; this continuation character shall be discarded from the input. Although the implementation need not accept any one portion of a continued line with a length exceeding {LINE_MAX} bytes, it shall place no limits on the accumulated length of the continued line. Comment lines shall not be continued on a subsequent line using an escaped <newline>.

## Context

**Behavior:** In locale definition files:
1. Line continuation is done by placing the escape character as the last character on a line; the escape character and newline are discarded.
2. Individual physical line segments may be limited to `{LINE_MAX}` bytes, but the total accumulated logical line has no length limit.
3. Comment lines cannot be continued with escaped newlines.

**Shell relevance:** The shell does not parse locale definition files. This is `localedef` input syntax. No shell implementation action needed. (Note: the shell has its own line continuation rules for shell scripts, which are similar but separate.)

**Spec reference:** Section 7.3, line 158 in `basedefs/V1_chap07.html`.

## Implementation Notes

*(to be filled during implementation)*
