---
id: SHALL-20-110-08-025
category: shall
section: "ENVIRONMENT VARIABLES"
anchor: tag_20_110_08
source_file: utilities/sh.html
source_lines: [231, 235]
keywords: ["shall"]
tier: 1
status: enriched
refs: ["javascript:open_code('UP')"]
related_sections: []
tests: []
spec_tested: blocked
testing_blockers: "requires interactive shell with timed mail checking; cannot test in batch mode"
---

## Extracted Text

> The following environment variables shall affect the execution of sh:: [UP] Establish a decimal integer value that specifies how often (in seconds) the shell shall check for the arrival of mail in the files specified by the MAILPATH or MAIL variables. The default value shall be 600 seconds. If set to zero, the shell shall check before issuing each primary prompt.

## Context

**Behavior:** MAILCHECK controls the mail check interval:

1. **Value:** A decimal integer specifying the interval in seconds between mail checks.
2. **Default:** If MAILCHECK is unset, the default shall be 600 seconds (10 minutes).
3. **Zero value:** If set to zero, the shell shall check for new mail before issuing each primary prompt (PS1).
4. **Normal operation:** The shell checks for mail only after the MAILCHECK interval has elapsed since the last check, and only when about to display the primary prompt.

**Applies to:** Interactive shells (User Portability [UP] option).

**Implementation detail:** The shell should parse MAILCHECK as a decimal integer at the time of each check. Store a timestamp of the last mail check; before each PS1 prompt, compute elapsed time and check mail if the interval has passed (or if MAILCHECK is 0).

**Spec reference:** Section "sh" ENVIRONMENT VARIABLES, lines 231-235 in `utilities/sh.html`.

## Implementation Notes

*(to be filled during implementation)*
