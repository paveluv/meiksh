---
id: SHALL-08-01-006
category: shall
section: "8.1 Environment Variable Definition"
anchor: tag_08_01
source_file: basedefs/V1_chap08.html
source_lines: [382, 386]
keywords: ["shall", "may"]
tier: 2
status: enriched
spec_tested: blocked
testing_blockers: "Requires setuid/privilege-escalation test environment to verify secure_getenv behavior"
refs: ["../functions/secure_getenv.html", "../functions/getenv.html", "../functions/getenv.html#"]
related_sections: []
tests: []
---

## Extracted Text

> Implementations may ignore some environment variables at the point of use for security reasons, for example in programs whose real and effective user IDs or real and effective group IDs were not equal at program startup. The behavior shall be as if the implementation obtains the values for these environment variables using secure_getenv() instead of getenv() (see getenv()); they shall not be removed from the environment of affected processes and shall be inherited as required by this standard.

## Context

Implementations may ignore certain environment variables for security reasons (e.g., when real and effective UIDs differ). The behavior shall be as if `secure_getenv()` is used instead of `getenv()`. Such variables shall not be removed from the environment and shall be inherited as required.

**Shell relevance:** When the shell runs with elevated privileges (setuid), it may ignore security-sensitive variables like PATH, IFS, etc. for safety. However, these variables must still be present in the environment and inherited by child processes. See XBD 8.1.

## Implementation Notes

*(to be filled during implementation)*
