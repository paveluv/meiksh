---
id: SHALL-20-132-14-005
category: shall
section: "EXIT STATUS"
anchor: tag_20_132_14
source_file: utilities/umask.html
source_lines: [152, 152]
keywords: ["shall"]
tier: 2
status: enriched
spec_tested: true
refs: []
related_sections: []
tests:
  - ../../../tests/spec/SHALL-20-132-14-005.sh
reviewed: GPT-5.4
---

## Extracted Text

> An error occurred.

## Context

**Behavior:** This is the description for exit status >0 of `umask`. A non-zero exit status indicates that an error occurred. Typical errors include:
- Invalid mask operand (e.g., `umask abc`, `umask 999`)
- Unrecognized option (e.g., `umask -Z`)
- Malformed symbolic mode string

**Error handling:** This row itself only requires that an error be reflected by a non-zero exit status. STDERR behavior is covered separately, and mask-preservation behavior would need to be justified from other POSIX text if asserted.

**Spec reference:** Section EXIT STATUS (tag_20_132_14) in `utilities/umask.html`, line 152.

GPT-5.4 review: The prior extracted text incorrectly merged the EXIT STATUS heading with the error description. The corrected quote now matches the actual POSIX text in `docs/posix/utilities/umask.html#tag_20_132_14`. The earlier extra error-handling sentence was broader than this one-line SHALL and has been narrowed accordingly.

## Implementation Notes

GPT-5.4 review of `tests/spec/SHALL-20-132-14-005.sh`: the script appropriately exercises more than one invalid-input path and checks only the behavior this row requires: a non-zero exit status.
