---
id: SHALL-11-01-03-001
category: shall
section: "11.1.3 The Controlling Terminal"
anchor: tag_11_01_03
source_file: basedefs/V1_chap11.html
source_lines: [77, 83]
keywords: ["implementation-defined", "shall", "may"]
tier: 3
status: enriched
refs: ["../functions/open.html", "../functions/open.html"]
related_sections: []
tests: []
---

## Extracted Text

> A terminal may belong to a process as its controlling terminal. Each process of a session that has a controlling terminal has the same controlling terminal. A terminal may be the controlling terminal for at most one session. The controlling terminal for a session is allocated by the session leader in an implementation-defined manner. If a session leader has no controlling terminal, and opens a terminal device file that is not already associated with a session without using the O_NOCTTY option (see open()), it is implementation-defined whether the terminal becomes the controlling terminal of the session leader. If a process which is not a session leader opens a terminal file, or the O_NOCTTY option is used on open(), then that terminal shall not become the controlling terminal of the calling process. When a controlling terminal becomes associated with a session, its foreground process group shall be set to the process group of the session leader.

## Context

**Behavior:** Controlling terminal rules:
- All processes in a session share the same controlling terminal
- A terminal can be the controlling terminal for at most one session
- Session leader allocates the controlling terminal (implementation-defined how)
- Non-session-leader opening a terminal or using O_NOCTTY → terminal shall NOT become controlling terminal
- When a controlling terminal is associated with a session, the foreground process group shall be set to the session leader's process group

**Conditions:** Applies to session/terminal association. The shell is typically the session leader and acquires its controlling terminal at startup.

**Shell relevance:** The shell as session leader acquires its controlling terminal. When the shell calls setsid() (e.g., for a login shell), it needs to establish a controlling terminal. O_NOCTTY prevents accidental terminal acquisition.

**Spec reference:** XBD 11.1.3 "The Controlling Terminal" (tag_11_01_03), `basedefs/V1_chap11.html`, lines 77–83.

## Implementation Notes

*(to be filled during implementation)*
