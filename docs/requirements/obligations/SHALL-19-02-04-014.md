---
id: SHALL-19-02-04-014
category: shall
section: "2.2.4 Dollar-Single-Quotes"
anchor: tag_19_02_04
source_file: utilities/V3_chap02.html
source_lines: [209, 211]
keywords: ["unspecified", "shall"]
tier: 1
status: enriched
refs: []
related_sections: []
tests: []
---

## Extracted Text

> A sequence of characters starting with a <dollar-sign> immediately followed by a single-quote ($') shall preserve the literal value of all characters up to an unescaped terminating single-quote ('), with the exception of certain <backslash>-escape sequences, as follows:: \xXX yields the byte whose value is the hexadecimal value XX (one or more hexadecimal digits). If more than two hexadecimal digits follow \x, the results are unspecified.

## Context

**Behavior:** Inside `$'...'`, the escape sequence `\xXX` yields the byte whose value is the hexadecimal number XX. At least one hex digit is required; up to two hex digits are consumed (see SHALL-19-02-04-017 for termination rules). If more than two hex digits follow `\x`, the results are unspecified (see UNSPEC-19-02-04-001).

**Applies to:** All shell modes during `$'...'` escape processing.

**Implementation detail:** After `\x`, consume hex digits (0-9, a-f, A-F). Stop at the first non-hex character or after consuming 2 digits, whichever comes first. Convert the consumed digits to a byte value. If the byte value is 0, behavior regarding null inclusion is unspecified (see UNSPEC-19-02-04-003).

**Recommended approach for >2 hex digits:** Since the spec says results are unspecified for more than 2 hex digits, the implementation should consume exactly up to 2 hex digits (matching the termination rule in SHALL-19-02-04-017) and leave any remaining hex digits as literal characters.

**Spec reference:** Section 2.2.4, lines 209-211 in `utilities/V3_chap02.html`.

## Implementation Notes

*(to be filled during implementation)*
