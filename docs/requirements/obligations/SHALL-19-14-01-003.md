---
id: SHALL-19-14-01-003
category: shall
section: "2.14.1 Patterns Matching a Single Character"
anchor: tag_19_14_01
source_file: utilities/V3_chap02.html
source_lines: [2965, 2965]
keywords: ["unspecified", "shall"]
tier: 1
status: enriched
spec_tested: true
refs: []
related_sections: []
tests: []
---

## Extracted Text

> In a pattern, or part of one, where a shell-quoting <backslash> cannot be used to preserve the literal value of a character that would otherwise be treated as special:: A <backslash> character that is not inside a bracket expression shall preserve the literal value of the following character, unless the following character is in a part of the pattern where shell quoting can be used and is a shell quoting character, in which case the behavior is unspecified.

## Context

**Backslash in patterns where shell quoting is NOT available (outside bracket expressions).**

In pattern contexts where shell-quoting backslash cannot be used (e.g., patterns resulting from earlier expansion, or patterns passed to `fnmatch()`/`glob()`), a `\` that is NOT inside a bracket expression **shall** preserve the literal value of the following character. This makes `\*`, `\?`, `\[` match literal characters.

However, if the following character is in a part of the pattern where shell quoting CAN be used and that character is a shell quoting character, the behavior is **unspecified**. This edge case arises in mixed-context patterns and the implementation need not handle it predictably.

This distinction matters for pathname expansion when a pattern word contains both literal text and text produced by variable expansion — the expanded portion cannot use shell quoting, so backslash-escaping in that portion follows these non-shell-quoting rules.

## Implementation Notes

*(to be filled during implementation)*
