---
id: SHALL-07-03-012
category: shall
section: "7.3 Locale Definition"
anchor: tag_07_03
source_file: basedefs/V1_chap07.html
source_lines: [162, 165]
keywords: ["shall"]
tier: 2
status: enriched
refs: []
related_sections: []
tests: []
---

## Extracted Text

> Individual characters, characters in strings, and collating elements shall be represented using symbolic names, as defined below. In addition, characters can be represented using the characters themselves or as octal, hexadecimal, or decimal constants. When non-symbolic notation is used, the resultant locale definitions are in many cases not portable between systems. The left angle bracket ('<') is a reserved symbol, denoting the start of a symbolic name; when used to represent itself it shall be preceded by the escape character. The following rules apply to character representation:

## Context

**Behavior:** In locale definition files, characters are represented as:
1. Symbolic names (e.g., `<A>`, `<space>`) — preferred for portability
2. Literal characters from the portable character set
3. Octal, hexadecimal, or decimal constants

The `<` character is reserved for symbolic names and must be escaped to represent itself literally. Non-symbolic representations are not portable across systems.

**Shell relevance:** The shell does not parse locale definition files. This is `localedef` input character representation syntax. The shell uses symbolic character class names (like `[:alpha:]`) in bracket expressions, but those come from the compiled locale, not from parsing locale definition files directly.

**Spec reference:** Section 7.3, lines 162-165 in `basedefs/V1_chap07.html`.

## Implementation Notes

*(to be filled during implementation)*
