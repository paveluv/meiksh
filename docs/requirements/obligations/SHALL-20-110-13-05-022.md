---
id: SHALL-20-110-13-05-022
category: shall
section: "vi Line Editing Command Mode"
anchor: tag_20_110_13_05
source_file: utilities/sh.html
source_lines: [448, 452]
keywords: ["shall"]
tier: 1
status: enriched
refs: []
related_sections: []
tests: []
spec_tested: blocked
testing_blockers: "vi command mode dot repeat requires interactive TTY simulation"
reviewed: GPT-5.4
---

## Extracted Text

> Repeat the most recent non-motion command, even if it was executed on an earlier command line. If the previous command was preceded by a count, and no count is given on the '.' command, the count from the previous command shall be included as part of the repeated command. If the '.' command is preceded by a count, this shall override any count argument to the previous command. The count specified in the '.' command shall become the count for subsequent '.' commands issued without a count.

## Context

**Behavior:** In vi command mode, `.` (dot) shall repeat the most recent non-motion command, even if it was executed on an earlier command line.

**Count interaction:**
- If the previous command had a count and `.` has no count: use the previous command's count.
- If `.` is preceded by a count: override the previous command's count with this new count.
- The count used with `.` becomes the count for subsequent `.` commands issued without a count.

**Implementation:** Store the last non-motion command (its key, any argument character, and original count) in the editing state. On `.`, replay that command with the appropriate count. Motion commands are: `<space>`, `0`, `b`, `F`, `l`, `W`, `^`, `$`, `;`, `E`, `f`, `T`, `w`, `|`, `,`, `B`, `e`, `h`, `t`.

**Spec reference:** vi Line Editing Command Mode in `utilities/sh.html`, line 448–452.

## Implementation Notes

GPT-5.4 review: The prior extracted text incorrectly merged the command-list heading with the command semantics. The corrected quote now matches `docs/posix/issue8/sh-utility.html#tag_20_110_13_05`. Canonical coverage requires interactive command-mode state across command lines because the rule explicitly spans earlier command lines and remembered counts.
