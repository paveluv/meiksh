---
id: SHALL-20-110-08-004
category: shall
section: "ENVIRONMENT VARIABLES"
anchor: tag_20_110_08
source_file: utilities/sh.html
source_lines: [176, 176]
keywords: ["shall"]
tier: 1
status: enriched
refs: []
related_sections: []
tests: []
spec_tested: blocked
testing_blockers: "heading-only intro for FCEDIT; behavior tested via SHALL-20-110-08-005"
reviewed: GPT-5.4
---

## Extracted Text

> FCEDIT

## Context

**Behavior:** The FCEDIT environment variable is one of the variables that shall affect the execution of `sh`. This is the variable name heading; the detailed semantics are specified in the subsequent description (see SHALL-20-110-08-005).

**Applies to:** Interactive shells using the `fc` built-in command (User Portability [UP] option).

**Details:** FCEDIT provides the default editor for the `fc` built-in's `-e` option. When FCEDIT is null or unset, `ed` is used as the default editor. See SHALL-20-110-08-005 for the full behavior specification.

**Spec reference:** Section "sh" ENVIRONMENT VARIABLES, line 176 in `utilities/sh.html`.

## Implementation Notes

GPT-5.4 review: The prior extracted text incorrectly merged the ENVIRONMENT VARIABLES heading with the `FCEDIT` variable label. The corrected quote now matches `docs/posix/issue8/sh-utility.html#tag_20_110_08`. This is a label row whose substantive behavior is carried by the blocked primary row `SHALL-20-110-08-005`.
