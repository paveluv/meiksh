---
id: SHALL-11-01-06-003
category: shall
section: "11.1.6 Canonical Mode Input Processing"
anchor: tag_11_01_06
source_file: basedefs/V1_chap11.html
source_lines: [165, 165]
keywords: ["shall"]
tier: 3
status: enriched
refs: ["#tag_11_01_09"]
related_sections: []
tests: []
---

## Extracted Text

> Erase and kill processing occur when either of two special characters, the ERASE and KILL characters (see 11.1.9 Special Characters), is received. This processing shall affect data in the input queue that has not yet been delimited by an NL, EOF, or EOL character. This un-delimited data makes up the current line. The ERASE character shall delete the last character in the current line, if there is one. The KILL character shall delete all data in the current line, if there is any. The ERASE and KILL characters shall have no effect if there is no data in the current line. The ERASE and KILL characters themselves shall not be placed in the input queue.

## Context

**Behavior:** In canonical mode, when the ERASE or KILL special characters are received:
1. **ERASE** shall delete the last character in the current (un-delimited) line. It shall not erase beyond the start of the line (delimited by NL, EOF, or EOL). No effect if the current line is empty.
2. **KILL** shall delete all data in the current (un-delimited) line. No effect if the current line is empty.
3. Both ERASE and KILL characters shall **not** be placed in the input queue themselves — they are consumed by the processing.

**Conditions:** Applies only when ICANON is set in `c_lflag`. The "current line" is the data in the input queue not yet delimited by NL, EOF, or EOL.

**Shell relevance:** This is the terminal driver's canonical-mode line editing, not the shell's own line editor. When the shell is in canonical mode (no custom line editing), the terminal driver provides basic ERASE (typically backspace/delete) and KILL (typically ^U) processing. The shell's own line editor (emacs/vi mode) operates in non-canonical mode and implements its own erase/kill logic, bypassing this terminal driver behavior.

**Spec reference:** POSIX.1-2024, §11.1.6 Canonical Mode Input Processing (tag_11_01_06), `basedefs/V1_chap11.html` line 165. See also §11.1.9 Special Characters.

## Implementation Notes

*(to be filled during implementation)*
