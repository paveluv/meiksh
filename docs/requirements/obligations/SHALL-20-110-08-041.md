---
id: SHALL-20-110-08-041
category: shall
section: "ENVIRONMENT VARIABLES"
anchor: tag_20_110_08
source_file: utilities/sh.html
source_lines: [256, 256]
keywords: ["shall", "may"]
tier: 1
status: enriched
refs: []
related_sections: []
tests:
  - ../../../tests/spec/SHALL-20-110-08-033.sh
spec_tested: blocked
testing_blockers: "Duplicate extraction of SHALL-20-110-08-033; canonical coverage belongs to the shared PWD script"
reviewed: GPT-5.4
---

## Extracted Text

> This variable shall represent an absolute pathname of the current working directory. Assignments to this variable may be ignored.

## Context

**Behavior:** This is a duplicate extraction of the PWD variable description (same source line as SHALL-20-110-08-033). The full behavior specification is identical:

1. **Shall represent absolute pathname:** PWD shall always contain an absolute pathname of the current working directory.
2. **Assignments may be ignored:** User assignments to PWD (`PWD=/foo`) may be silently ignored by the shell.

**See also:** SHALL-20-110-08-033 for the primary obligation covering this same text.

**Spec reference:** Section "sh" ENVIRONMENT VARIABLES, line 256 in `utilities/sh.html`.

GPT-5.4 review: This quote correctly matches the same POSIX source text as `SHALL-20-110-08-033`, making this a duplicate extraction rather than a separate behavior row. The canonical test coverage belongs with the primary PWD obligation.

## Implementation Notes

GPT-5.4 review: no separate canonical `tests/spec` script is needed for this duplicate row.
