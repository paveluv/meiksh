---
id: MAY-08-02-001
category: may
section: "8.2 Internationalization Variables"
anchor: tag_08_02
source_file: basedefs/V1_chap08.html
source_lines: [391, 392]
keywords: ["may"]
tier: 2
status: enriched
refs: ["../functions/setlocale.html"]
related_sections: []
tests: []
---

## Extracted Text

> Users may use the following environment variables to announce specific localization requirements to applications. Applications can retrieve this information using the setlocale() function to initialize the correct behavior of the internationalized interfaces. The descriptions of the internationalization environment variables describe the resulting behavior only when the application locale is initialized in this way. The use of the internationalization variables by utilities described in the Shell and Utilities volume of POSIX.1-2024 is described in the ENVIRONMENT VARIABLES section for those utilities in addition to the global effects described in this section.

## Context

Users may use internationalization environment variables (LANG, LC_ALL, LC_COLLATE, LC_CTYPE, LC_MESSAGES, LC_MONETARY, LC_NUMERIC, LC_TIME) to announce localization requirements. Applications retrieve this via `setlocale()`. The described behavior applies only when the application locale is initialized via `setlocale()`. Per-utility effects are described in each utility's ENVIRONMENT VARIABLES section.

**Shell relevance:** The shell should call `setlocale(LC_ALL, "")` at startup to honor the user's locale settings. The shell must document which LC_* variables affect its behavior. See XBD 8.2.

## Implementation Notes

*(to be filled during implementation)*
