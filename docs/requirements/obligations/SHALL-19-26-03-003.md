---
id: SHALL-19-26-03-003
category: shall
section: "DESCRIPTION"
anchor: tag_19_26_03
source_file: utilities/V3_chap02.html
source_lines: [4766, 4769]
keywords: ["shall"]
tier: 1
status: enriched
refs: ["#set"]
related_sections: []
tests: []
---

## Extracted Text

> When options are specified, they shall set or unset attributes of the shell, as described below. When arguments are specified, they cause positional parameters to be set or unset, as described below. Setting or unsetting attributes and positional parameters are not necessarily related actions, but they can be combined in a single invocation of set.

## Context

**Behavior:** The `set` built-in has two independent functionalities:
1. **Options** (`-a`, `-e`, `-f`, etc.) set or unset shell attributes.
2. **Arguments** (non-option operands) set or unset positional parameters.

Both can be combined in a single invocation. Setting attributes and setting positional parameters are logically independent operations.

**Applies to:** All shell modes.

**Spec reference:** `set` DESCRIPTION in `utilities/V3_chap02.html`, lines 4766-4769.

## Implementation Notes

*(to be filled during implementation)*
