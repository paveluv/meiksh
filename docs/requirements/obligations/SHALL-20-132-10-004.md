---
id: SHALL-20-132-10-004
category: shall
section: "STDOUT"
anchor: tag_20_132_10
source_file: utilities/umask.html
source_lines: [130, 130]
keywords: ["shall"]
tier: 2
status: enriched
spec_tested: true
refs: []
related_sections: []
tests:
  - ../../../tests/spec/SHALL-20-132-10-004.sh
reviewed: GPT-5.4
---

## Extracted Text

> If a mask operand is specified, there shall be no output written to standard output.

## Context

**Behavior:** When `umask` is invoked with a mask operand (i.e., in "set" mode, not "query" mode), the utility shall not write anything to stdout. It silently sets the mask.

**Conditions:** This applies regardless of whether `-S` is also specified. If a mask operand is present, no stdout output is produced.

**Error handling:** This row constrains successful set-mode stdout behavior only; stderr handling is covered separately by the STDERR section.

**Spec reference:** Section STDOUT (tag_20_132_10) in `utilities/umask.html`, line 130.

GPT-5.4 review: The extracted quote matches `docs/posix/utilities/umask.html#tag_20_132_10`. The earlier error-handling sentence was broader than this specific SHALL. The core normative point here is simply that when a `mask` operand is supplied, no output is written to standard output.

## Implementation Notes

GPT-5.4 review of `tests/spec/SHALL-20-132-10-004.sh`: the strengthened script now checks both plain set mode and `-S` plus a `mask` operand, which better matches the note in the enrichment that the no-stdout rule still applies when `-S` is present.
