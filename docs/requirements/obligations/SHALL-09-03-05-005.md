---
id: SHALL-09-03-05-005
category: shall
section: "9.3.5 RE Bracket Expression"
anchor: tag_09_03_05
source_file: basedefs/V1_chap09.html
source_lines: [248, 258]
keywords: ["shall"]
tier: 2
status: enriched
refs: ["../basedefs/V1_chap07.html#tag_07_03_02_04"]
related_sections: []
tests: []
---

## Extracted Text

> The following rules and definitions apply to bracket expressions:: A collating symbol is a collating element enclosed within bracket-period ("[." and ".]") delimiters. Collating elements are defined as described in 7.3.2.4 Collation Order. Conforming applications shall represent multi-character collating elements as collating symbols when it is necessary to distinguish them from a list of the individual characters that make up the multi-character collating element. For example, if the string "ch" is a collating element defined using the line: collating-element <ch-digraph> from "<c><h>" in the locale definition, the expression "[[.ch.]]" shall be treated as an RE containing the collating symbol 'ch', while "[ch]" shall be treated as an RE matching 'c' or 'h'. Collating symbols are recognized only inside bracket expressions. If the string is not a collating element in the current locale, the expression is invalid.

## Context

**Behavior:** A collating symbol is a collating element enclosed in `[.` and `.]` delimiters. Multi-character collating elements must be represented as collating symbols to distinguish them from a list of individual characters. For example, if `ch` is a collating element, `[[.ch.]]` matches the two-character sequence `ch` as a single collating element, while `[ch]` matches `c` or `h` individually. Collating symbols are recognized only inside bracket expressions. If the string is not a collating element in the current locale, the expression is invalid.

**Shell relevance:** The shell must support collating symbols in bracket expressions within glob and `case` patterns. The parser must recognize `[.` and `.]` delimiters inside bracket expressions. If the collating symbol is not defined in the current locale, the shell must treat the expression as invalid (error). In the POSIX locale, only single-character collating elements exist.

**Spec reference:** Section 9.3.5, lines 248-258 in `basedefs/V1_chap09.html`; 7.3.2.4 Collation Order.

## Implementation Notes

*(to be filled during implementation)*
