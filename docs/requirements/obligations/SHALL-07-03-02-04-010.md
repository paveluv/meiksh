---
id: SHALL-07-03-02-04-010
category: shall
section: "7.3.2.4 Collation Order"
anchor: tag_07_03_02_04
source_file: basedefs/V1_chap07.html
source_lines: [2563, 2564]
keywords: ["shall"]
tier: 2
status: enriched
refs: []
related_sections: []
tests: []
---

## Extracted Text

> All characters specified via an ellipsis shall by default be assigned unique weights, equal to the relative order of characters. Characters specified via an explicit or implicit UNDEFINED special symbol shall by default be assigned the same primary weight (that is, they belong to the same equivalence class) if the collation order has more than one weight level. If the collation order has only one weight level, these characters shall be assigned unique primary weights, equal to the relative order of their character in the character collation sequence.

## Context

Default weight assignment rules in LC_COLLATE: (1) Characters filled in by an ellipsis get unique weights matching their position in the collation order. (2) Characters covered by UNDEFINED (explicit or implicit) get the same primary weight (forming one equivalence class) when there are multiple weight levels, but get unique primary weights when there is only one weight level. The shell is affected because these default weights determine equivalence class membership (for `[=x=]` bracket expressions) and range ordering (for `[a-z]` bracket expressions). In the POSIX locale (single weight level), UNDEFINED characters get unique weights ordered by code point.

See: `basedefs/V1_chap07.html` §7.3.2.4 Collation Order.

## Implementation Notes

*(to be filled during implementation)*
