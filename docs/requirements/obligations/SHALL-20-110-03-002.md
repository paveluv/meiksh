---
id: SHALL-20-110-03-002
category: shall
section: "DESCRIPTION"
anchor: tag_20_110_03
source_file: utilities/sh.html
source_lines: [57, 57]
keywords: ["shall"]
tier: 1
status: enriched
refs: []
related_sections: []
tests:
  - ../../../tests/spec/SHALL-20-110-03-002.sh
spec_tested: true
reviewed: GPT-5.4
---

## Extracted Text

> Pathname expansion shall not fail due to the size of a file.

## Context

**Behavior:** Pathname expansion (globbing) must **not** fail due to the size of a file. The shell's glob implementation must not have limits that cause failures when matching against files of any size. This applies to the glob pattern matching itself, not reading file contents.

**Implementation:** Ensure the pathname expansion code does not have fixed buffer limits or size-based error conditions related to file metadata.

**Applies to:** All shell modes.

**Spec reference:** `sh` DESCRIPTION in `utilities/sh.html`, line 57.

GPT-5.4 review: The extracted quote matches `docs/posix/issue8/sh-utility.html#tag_20_110_03`. The enrichment is directionally correct, though the actual SHALL is narrowly about pathname expansion not failing because of file size. It does not prescribe a particular internal implementation strategy beyond that observable constraint.

## Implementation Notes

GPT-5.4 review of `tests/spec/SHALL-20-110-03-002.sh`: the script uses one large file and one small file under the same glob so that pathname expansion is exercised without any dependency on file contents.
