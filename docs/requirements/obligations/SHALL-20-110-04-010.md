---
id: SHALL-20-110-04-010
category: shall
section: "OPTIONS"
anchor: tag_20_110_04
source_file: utilities/sh.html
source_lines: [75, 77]
keywords: ["shall"]
tier: 1
status: enriched
refs: ["../utilities/V3_chap02.html#tag_19_05_02"]
related_sections: []
tests:
  - ../../../tests/spec/SHALL-20-110-04-005.sh
spec_tested: blocked
testing_blockers: "Duplicate of SHALL-20-110-04-005; tested there"
reviewed: GPT-5.4
---

## Extracted Text

> Read commands from the command_string operand. Set the value of special parameter 0 (see 2.5.2 Special Parameters) from the value of the command_name operand and the positional parameters ($1, $2, and so on) in sequence from the remaining argument operands. No commands shall be read from the standard input.

## Context

Duplicate of SHALL-20-110-04-005 (same source lines 75-77). Covers the `-c` option: read commands from `command_string`, set `$0` from `command_name`, set positional parameters from remaining arguments, do not read from stdin.

See SHALL-20-110-04-005 for the full context. See utilities/sh.html#tag_20_110_04.

GPT-5.4 review: This quote correctly matches the same POSIX text as `SHALL-20-110-04-005`, making this a duplicate extraction rather than a separate behavior row. The canonical executable coverage belongs to `tests/spec/SHALL-20-110-04-005.sh`.

## Implementation Notes

GPT-5.4 review: no separate canonical `tests/spec` script is needed for this duplicate row.
