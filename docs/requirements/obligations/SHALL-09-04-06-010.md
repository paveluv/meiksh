---
id: SHALL-09-04-06-010
category: shall
section: "9.4.6 EREs Matching Multiple Characters"
anchor: tag_09_04_06
source_file: basedefs/V1_chap09.html
source_lines: [554, 558]
keywords: ["shall"]
tier: 2
status: enriched
refs: []
related_sections: []
tests: []
---

## Extracted Text

> When an ERE matching a single character or an ERE enclosed in parentheses is followed by the special character <asterisk> ('*'), together with that <asterisk> it shall match what zero or more consecutive occurrences of the ERE would match. For example, the ERE "b*c" matches the first character in the string "cabbbcde", and the ERE "b*cd" matches the third to seventh characters in the string "cabbbcdebbbbbbcdbc". And, "[ab]*" and "[ab][ab]" are equivalent when matching the string "ab".

## Context

This is a duplicate extraction of the `*` operator rule from SHALL-09-04-06-004. The `*` quantifier applied to a preceding atom matches zero or more consecutive occurrences. It is equivalent to `{0,}`. Matching is greedy by default. Zero occurrences means the repeated part can match the empty string, which is what allows `b*c` to match "c" (zero `b`s).

## Implementation Notes

*(to be filled during implementation)*
