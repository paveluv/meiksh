---
id: SHALL-20-110-06-002
category: shall
section: "STDIN"
anchor: tag_20_110_06
source_file: utilities/sh.html
source_lines: [134, 135]
keywords: ["shall"]
tier: 1
status: enriched
refs: []
related_sections: []
tests:
  - ../../../tests/spec/SHALL-20-110-06-002.sh
spec_tested: true
reviewed: GPT-5.4
---

## Extracted Text

> The -s option is specified.

## Context

**Behavior:** When the `-s` option is specified on the `sh` command line, the shell shall read commands from standard input.

**Conditions:** This applies regardless of whether operands are present. With `-s`, operands become positional parameters (`$1`, `$2`, ...) rather than script filenames. The shell reads its command stream from stdin until EOF.

**Applies to:** Both interactive and non-interactive invocations. If stdin is a terminal and `-s` is given (and `-i` is not explicitly disabled), the shell is interactive.

**Spec reference:** STDIN section of `utilities/sh.html`, line 134–135.

## Implementation Notes

GPT-5.4 review: The prior extracted text incorrectly merged the STDIN parent sentence with the first bullet. The corrected quote now matches `docs/posix/issue8/sh-utility.html#tag_20_110_06`. The script is well targeted because it checks both that `-s` causes stdin command reading and that remaining operands become positional parameters.
