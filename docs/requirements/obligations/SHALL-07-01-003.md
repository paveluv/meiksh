---
id: SHALL-07-01-003
category: shall
section: "7.1 General"
anchor: tag_07_01
source_file: basedefs/V1_chap07.html
source_lines: [58, 61]
keywords: ["shall"]
tier: 2
status: enriched
refs: ["../functions/uselocale.html", "../functions/setlocale.html"]
related_sections: []
tests: []
---

## Extracted Text

> The standard utilities in the Shell and Utilities volume of POSIX.1-2024 shall base their behavior on the current locale, as defined in the ENVIRONMENT VARIABLES section for each utility. The behavior of some of the C-language functions defined in the System Interfaces volume of POSIX.1-2024 shall also be modified based on a locale selection. The locale to be used by these functions can be selected in the following ways:: For functions that do not take a locale object as an argument, the current locale for the thread can be set by calling uselocale() or the global locale for the process can be set by calling setlocale(). Such functions shall use the current locale of the calling thread if one has been set for that thread; otherwise, they shall use the global locale.

## Context

**Behavior:** Functions without a locale argument use the thread-local locale (set by `uselocale()`) if available, otherwise the global locale (set by `setlocale()`). Thread-local locale takes priority over global locale.

**Shell relevance:** The shell should call `setlocale(LC_ALL, "")` at startup to initialize the global locale from environment variables. If the shell uses threads internally, it may need `uselocale()` for thread-safe locale handling. The refs to `functions/` pages are C API documentation — not shell-relevant files to extract.

**Spec reference:** Section 7.1, lines 58-61 in `basedefs/V1_chap07.html`.

## Implementation Notes

*(to be filled during implementation)*
