---
id: SHALL-19-09-04-05-001
category: shall
section: "2.9.4.3 Case Conditional Construct"
anchor: tag_19_09_04_05
source_file: utilities/V3_chap02.html
source_lines: [2262, 2270]
keywords: ["shall"]
tier: 1
status: enriched
refs: ["#tag_19_14"]
related_sections: []
tests: []
---

## Extracted Text

> The conditional construct case shall execute the compound-list corresponding to the first pattern (see 2.14 Pattern Matching Notation), if any are present, that is matched by the string resulting from the tilde expansion, parameter expansion, command substitution, arithmetic expansion, and quote removal of the given word. The reserved word in shall denote the beginning of the patterns to be matched. Multiple patterns with the same compound-list shall be delimited by the '|' symbol. The control operator ')' terminates a list of patterns corresponding to a given action. The terminated pattern list and the following compound-list is called a case statement clause. Each case statement clause, with the possible exception of the last, shall be terminated with either ";;" or ";&". The case construct terminates with the reserved word esac (case reversed).

## Context

**Behavior:** The `case` construct matches a word against patterns and executes the compound-list for the first matching pattern. Key requirements:

1. The `word` is subjected to tilde expansion, parameter expansion, command substitution, arithmetic expansion, and quote removal.
2. The `in` reserved word denotes the beginning of the pattern list.
3. Multiple patterns for the same compound-list are delimited by `|`.
4. The `)` control operator terminates each pattern list.
5. Each case clause (pattern list + compound-list), except possibly the last, must be terminated by `;;` or `;&`.
6. The `case` construct terminates with `esac`.

**Fall-through:** `;;` terminates matching (no further clauses examined). `;&` causes fall-through: the compound-list of each subsequent clause is executed in order until a `;;` clause is reached or there are no more clauses.

**Pattern matching:** Uses the rules from section 2.14 Pattern Matching Notation (filename-style glob patterns, not regex).

**Applies to:** All shell modes.

**Spec reference:** Section 2.9.4.3 Case Conditional Construct in `utilities/V3_chap02.html`, lines 2262-2270. Cross-ref: 2.14 Pattern Matching Notation.

## Implementation Notes

*(to be filled during implementation)*
