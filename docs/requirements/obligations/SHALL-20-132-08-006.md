---
id: SHALL-20-132-08-006
category: shall
section: "ENVIRONMENT VARIABLES"
anchor: tag_20_132_08
source_file: utilities/umask.html
source_lines: [103, 103]
keywords: ["shall"]
tier: 2
status: enriched
spec_tested: blocked
testing_blockers: "LC_CTYPE locale behavior requires non-C locales with multi-byte encodings; cannot portably test"
refs: []
related_sections: []
tests: []
reviewed: GPT-5.4
---

## Extracted Text

> LC_CTYPE

## Context

**Behavior:** `LC_CTYPE` is one of the environment variables listed as affecting the execution of `umask`. Its substantive effect is described in the following subordinate row.

**Conditions:** LC_CTYPE is relevant when the mask operand or option arguments contain characters that could be interpreted differently depending on the locale (e.g., single-byte vs. multi-byte encodings).

**Spec reference:** Section ENVIRONMENT VARIABLES (tag_20_132_08) in `utilities/umask.html`, line 103.

GPT-5.4 review: The prior extracted text incorrectly merged the ENVIRONMENT VARIABLES heading with the variable label. The actual POSIX text at `docs/posix/utilities/umask.html#tag_20_132_08` is simply `LC_CTYPE`, with the detailed effect given by the following row. This row is therefore a framing/value-label entry.

## Implementation Notes

GPT-5.4 review: no dedicated `tests/spec` script exists for this label row.
