---
id: SHALL-20-110-08-029
category: shall
section: "ENVIRONMENT VARIABLES"
anchor: tag_20_110_08
source_file: utilities/sh.html
source_lines: [248, 249]
keywords: ["shall"]
tier: 1
status: enriched
refs: ["javascript:open_code('XSI')"]
related_sections: []
tests: []
spec_tested: blocked
testing_blockers: "XSI feature requiring message catalog infrastructure (catopen/catgets); not available in batch tests"
reviewed: GPT-5.4
---

## Extracted Text

> [XSI] Determine the location of messages objects and message catalogs.

## Context

**Behavior:** NLSPATH determines the location of message objects and message catalogs used by the shell for internationalized diagnostic messages. This is part of the XSI message catalog system (catopen/catgets).

**Applies to:** All shell modes (XSI option).

**Details:** NLSPATH is used by the native language support (NLS) system to locate message catalogs. The shell uses it when looking up locale-specific error and diagnostic messages. The format of NLSPATH is a colon-separated list of pathname templates with substitution fields (%L, %l, %t, %c, %N) as defined in the catopen() specification.

GPT-5.4 review: This row is an XSI-specific environment-variable effect statement. The quoted behavior here is narrow; the underlying message-catalog mechanisms live in the XSI interfaces, not in this `sh` sentence alone.

**Feature gate:** This is an [XSI] (X/Open System Interfaces) option feature.

**Spec reference:** Section "sh" ENVIRONMENT VARIABLES, lines 248-249 in `utilities/sh.html`.

## Implementation Notes

GPT-5.4 review: The prior extracted text incorrectly included the section heading. The corrected quote now matches `docs/posix/issue8/sh-utility.html#tag_20_110_08`. This remains doc-only because portable verification would require XSI message-catalog infrastructure.
