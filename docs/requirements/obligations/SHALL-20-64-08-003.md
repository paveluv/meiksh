---
id: SHALL-20-64-08-003
category: shall
section: "ENVIRONMENT VARIABLES"
anchor: tag_20_64_08
source_file: utilities/kill.html
source_lines: [165, 166]
keywords: ["shall"]
tier: 3
spec_tested: blocked
testing_blockers: "LANG default-value precedence is libc locale infrastructure; no shell-specific behavior to test"
status: enriched
refs: ["../basedefs/V1_chap08.html#tag_08_02"]
related_sections: []
tests: []
reviewed: GPT-5.4
---

## Extracted Text

> Provide a default value for the internationalization variables that are unset or null. (See XBD 8.2 Internationalization Variables for the precedence of internationalization variables used to determine the values of locale categories.)

## Context

**Behavior:** `LANG` provides default values for any LC_* variable that is unset or null. The precedence order (per XBD 8.2) is: LC_ALL overrides everything; then specific LC_* variables; then LANG as fallback. The `kill` built-in must follow this standard precedence when determining locale for character interpretation and diagnostic messages.

**Spec reference:** Section ENVIRONMENT VARIABLES (tag_20_64_08) in `utilities/kill.html`, lines 165–166. See also XBD 8.2 Internationalization Variables.

GPT-5.4 review: The earlier extracted text merged the section heading with the `LANG` description. The corrected quote now matches the actual descriptive POSIX text under `docs/posix/utilities/kill.html#tag_20_64_08`. The enrichment is accurate, and the blocked-test rationale is reasonable because this default-value precedence is part of the locale framework rather than shell-specific `kill` behavior.

## Implementation Notes

GPT-5.4 review: no dedicated `tests/spec` script exists for this locale-framework row.
