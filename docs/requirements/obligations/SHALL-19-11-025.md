---
id: SHALL-19-11-025
category: shall
section: "2.11 Job Control"
anchor: tag_19_11
source_file: utilities/V3_chap02.html
source_lines: [2809, 2809]
keywords: ["unspecified", "shall"]
tier: 1
status: enriched
spec_tested: blocked
testing_blockers: "Requires SIGTSTP delivery to complex pipeline and verifying suspended job contents; needs interactive pty"
refs: []
related_sections: []
tests: []
---

## Extracted Text

> Otherwise, the shell shall create a suspended job consisting of a set of commands, from within the list comprising the foreground job, that is unspecified except that the set shall include at least the pipeline to which the stopped process belongs. Commands in the foreground job that have not already completed and are not included in the suspended job shall be discarded.

## Context

**Foreground job suspension — complex pipeline case (detail):** When the AND-OR list is not a single simple-command pipeline and a process is stopped, the shell creates a suspended job from an unspecified subset of commands. The minimum requirement is that the pipeline containing the stopped process is included. Any incomplete commands not in the suspended job are discarded.

This gives the implementation freedom: it may suspend just the stopped pipeline, or include additional context. Commands that have already completed are not re-executed if the job is later resumed.

## Implementation Notes

*(to be filled during implementation)*
