---
id: SHALL-20-64-04-007
category: shall
section: "OPTIONS"
anchor: tag_20_64_04
source_file: utilities/kill.html
source_lines: [84, 86]
keywords: ["shall"]
tier: 3
spec_tested: true
status: enriched
refs: []
related_sections: []
tests: []
reviewed: GPT-5.4
---

## Extracted Text

> [XSI] Equivalent to -s signal_name.

## Context

**Behavior:** The `kill` built-in must support the `-signal_name` option form (XSI extension). This allows specifying the signal as a bare option, e.g., `kill -HUP pid`. POSIX states that this form is equivalent to `-s signal_name` (see SHALL-20-64-04-008).

**Conditions:** This is an XSI option. The same case-independent matching and SIG-prefix-stripping rules apply as for `-s signal_name`. This form may exceed the single-character option convention from XBD 12.2 Utility Syntax Guidelines.

**Spec reference:** Section OPTIONS (tag_20_64_04) in `utilities/kill.html`, lines 84-86.

GPT-5.4 review: The prior extracted quote merged the general list-introduction text with the `-signal_name` option tag and did not match the cited source line. The corrected quote now uses the exact normative text from `docs/posix/utilities/kill.html#tag_20_64_04`: the XSI `-signal_name` form is equivalent to `-s signal_name`. That equivalence is the key requirement for this obligation.

## Implementation Notes

GPT-5.4 review of `tests/spec/SHALL-20-64-04-007.sh`: the revised script checks that the `-signal_name` form is accepted directly and successfully delivers the named signal, which is the observable consequence required by `docs/posix/utilities/kill.html#tag_20_64_04`.
