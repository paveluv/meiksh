---
id: SHALL-20-64-05-002
category: shall
section: "OPERANDS"
anchor: tag_20_64_05
source_file: utilities/kill.html
source_lines: [123, 123]
keywords: ["shall"]
tier: 3
spec_tested: blocked
testing_blockers: "Framing statement naming the pid operand; detailed semantics tested via SHALL-20-64-05-003"
status: enriched
refs: []
related_sections: []
tests:
  - ../../../tests/spec/SHALL-20-64-05-003.sh
reviewed: GPT-5.4
---

## Extracted Text

> pid

## Context

**Behavior:** The `kill` built-in must accept one or more `pid` operands when not in `-l` (list) mode. Each `pid` operand identifies a target process or process group to receive the signal. See SHALL-20-64-05-003 and SHALL-20-64-05-006 for the detailed semantics of how `pid` values are interpreted.

**Spec reference:** Section OPERANDS (tag_20_64_05) in `utilities/kill.html`, line 123.

GPT-5.4 review: The prior extracted text incorrectly merged the operand-list heading with the operand label. The actual POSIX text at `docs/posix/utilities/kill.html#tag_20_64_05` is simply the operand label `pid`, with the substantive semantics given in the following lines already reviewed under `SHALL-20-64-05-003`. This row is therefore best treated as a framing/value-label entry.

## Implementation Notes

GPT-5.4 review: no separate `tests/spec/SHALL-20-64-05-002.sh` exists, and none is needed. The corresponding `pid` semantics are already covered by the reviewed canonical row `SHALL-20-64-05-003` and `tests/spec/SHALL-20-64-05-003.sh`.
