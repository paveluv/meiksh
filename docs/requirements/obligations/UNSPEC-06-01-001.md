---
id: UNSPEC-06-01-001
category: unspecified
section: "6.1 Portable Character Set"
anchor: tag_06_01
source_file: basedefs/V1_chap06.html
source_lines: [1506, 1506]
keywords: ["unspecified"]
tier: 2
status: enriched
refs: []
related_sections: []
tests: []
---

## Extracted Text

> POSIX.1-2024 places only the following requirements on the encoded values of the characters in the portable character set:: If the encoded values associated with each member of the portable character set are not invariant across all locales supported by the implementation, if an application uses any pair of locales where the character encodings differ, or accesses data from an application using a locale which has different encodings from the locales used by the application, the results are unspecified.

## Context

If the shell changes locales at runtime (via `LC_*` or `LANG` variable changes) and the character encodings differ between locales, the behavior when processing data from the previous locale is unspecified. For the shell, this means:

- If a script sets `LANG` or `LC_CTYPE` to a locale with different character encodings mid-execution, any data (variables, input) encoded under the previous locale may not be correctly interpreted.
- The shell should ideally re-initialize its locale-dependent processing (character classification, multibyte handling) when locale variables change.
- In practice, most modern systems use UTF-8 uniformly, minimizing this issue.

See: `basedefs/V1_chap06.html` §6.1 Portable Character Set.

## Implementation Notes

*(to be filled during implementation)*
