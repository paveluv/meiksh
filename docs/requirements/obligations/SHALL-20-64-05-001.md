---
id: SHALL-20-64-05-001
category: shall
section: "OPERANDS"
anchor: tag_20_64_05
source_file: utilities/kill.html
source_lines: [120, 120]
keywords: ["shall"]
tier: 3
spec_tested: blocked
testing_blockers: "Framing statement introducing operands list; not independently testable"
status: enriched
refs: []
related_sections: []
tests:
  - ../../../tests/spec/SHALL-20-64-05-003.sh
  - ../../../tests/spec/SHALL-20-64-05-005.sh
reviewed: GPT-5.4
---

## Extracted Text

> The following operands shall be supported:

## Context

**Behavior:** The `kill` built-in must accept the operands defined in the OPERANDS section: `pid` (one or more process/process-group identifiers or job IDs) and `exit_status` (used only with `-l`). This is the introductory statement; the specific operand requirements are in SHALL-20-64-05-002 through SHALL-20-64-05-006.

**Spec reference:** Section OPERANDS (tag_20_64_05) in `utilities/kill.html`, line 120.

GPT-5.4 review: The extracted quote is faithful to `docs/posix/utilities/kill.html#tag_20_64_05`. This row is correctly blocked as a framing statement. Its purpose is to introduce the operand table, while the specific testable semantics live in the subordinate `pid` and `exit_status` rows.

## Implementation Notes

GPT-5.4 review: no separate `tests/spec` script is needed for this heading row. The corresponding executable coverage belongs to the reviewed subordinate operand rows.
