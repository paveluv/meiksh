---
id: SHALL-11-02-02-002
category: shall
section: "11.2.2 Input Modes"
anchor: tag_11_02_02
source_file: basedefs/V1_chap11.html
source_lines: [501, 501]
keywords: ["implementation-defined", "shall"]
tier: 3
status: enriched
refs: []
related_sections: []
tests: []
---

## Extracted Text

> In the context of asynchronous serial data transmission, a break condition shall be defined as a sequence of zero-valued bits that continues for more than the time to send one byte. The entire sequence of zero-valued bits is interpreted as a single break condition, even if it continues for a time equivalent to more than one byte. In contexts other than asynchronous serial data transmission, the definition of a break condition is implementation-defined.

## Context

**Behavior:**
1. For asynchronous serial data: a break condition **shall** be defined as a continuous sequence of zero-valued bits lasting longer than one byte time. The entire sequence is a single break condition.
2. For non-serial contexts (e.g., pseudo-terminals, virtual consoles): the definition of a break condition is **implementation-defined**.

**Conditions:** Applies to terminal input processing. Break conditions interact with IGNBRK, BRKINT, and PARMRK flags (see SHALL-11-02-02-003).

**Shell relevance:** The shell is unlikely to directly encounter break conditions on modern pseudo-terminal connections. However, the `stty` builtin must support the IGNBRK and BRKINT flags that control break handling. For serial terminal connections, the shell should be aware that a break condition can generate SIGINT (if BRKINT is set). meiksh should document its implementation-defined break condition behavior for non-serial contexts.

**Spec reference:** POSIX.1-2024, §11.2.2 Input Modes (tag_11_02_02), `basedefs/V1_chap11.html` line 501.

## Implementation Notes

*(to be filled during implementation)*
