---
id: SHALL-20-110-06-006
category: shall
section: "STDIN"
anchor: tag_20_110_06
source_file: utilities/sh.html
source_lines: [152, 152]
keywords: ["shall"]
tier: 1
status: enriched
refs: []
related_sections: []
tests: []
spec_tested: blocked
testing_blockers: "requires FIFO or terminal fd with O_NONBLOCK set; cannot reliably set up in batch test"
reviewed: GPT-5.4
---

## Extracted Text

> If the standard input to sh is a FIFO or terminal device and is set to non-blocking reads, then sh shall enable blocking reads on standard input. This shall remain in effect when the command completes.

## Context

**Behavior:** If the shell's standard input is a FIFO (named pipe) or terminal device AND it is currently set to non-blocking reads (O_NONBLOCK), the shell shall enable blocking reads on stdin. This blocking mode shall remain in effect even after a command completes (i.e., the shell does not restore non-blocking mode).

**Implementation:** This row constrains the observable stdin mode transition: when the preconditions hold, `sh` must make stdin blocking and keep it that way after command completion.

**Applies to:** Shell startup when reading from stdin. This prevents the shell from spinning on `EAGAIN`/`EWOULDBLOCK` errors.

**Spec reference:** STDIN section of `utilities/sh.html`, lines 152-153.

GPT-5.4 review: The extracted quote matches `docs/posix/issue8/sh-utility.html#tag_20_110_06`. Keeping this row blocked is appropriate because authoritative coverage requires controlling the file type and `O_NONBLOCK` state of the shell's actual stdin in a way that the current batch harness does not provide portably.

## Implementation Notes

GPT-5.4 review: no portable `tests/spec` script is kept for this stdin-file-descriptor row.
