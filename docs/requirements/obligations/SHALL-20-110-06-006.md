---
id: SHALL-20-110-06-006
category: shall
section: "STDIN"
anchor: tag_20_110_06
source_file: utilities/sh.html
source_lines: [152, 152]
keywords: ["shall"]
tier: 1
status: enriched
refs: []
related_sections: []
tests: []
---

## Extracted Text

> If the standard input to sh is a FIFO or terminal device and is set to non-blocking reads, then sh shall enable blocking reads on standard input. This shall remain in effect when the command completes.

## Context

**Behavior:** If the shell's standard input is a FIFO (named pipe) or terminal device AND it is currently set to non-blocking reads (O_NONBLOCK), the shell shall enable blocking reads on stdin. This blocking mode shall remain in effect even after a command completes (i.e., the shell does not restore non-blocking mode).

**Implementation:** At startup (or when beginning to read from stdin), check if fd 0 is a FIFO or terminal using `fstat()` and `isatty()`. If so, clear the `O_NONBLOCK` flag using `fcntl(fd, F_SETFL, flags & ~O_NONBLOCK)`. Do not restore non-blocking mode after command execution.

**Applies to:** Shell startup when reading from stdin. This prevents the shell from spinning on `EAGAIN`/`EWOULDBLOCK` errors.

**Spec reference:** STDIN section of `utilities/sh.html`, line 152–153.

## Implementation Notes

*(to be filled during implementation)*
