---
id: SHALL-19-05-03-037
category: shall
section: "2.5.3 Shell Variables"
anchor: tag_19_05_03
source_file: utilities/V3_chap02.html
source_lines: [587, 592]
keywords: ["shall"]
tier: 1
status: enriched
refs: ["#exec", "../utilities/sh.html"]
related_sections: []
tests: []
---

## Extracted Text

> Determine the interpretation of sequences of bytes of text data as characters (for example, single-byte as opposed to multi-byte characters), which characters are defined as letters (character class alpha) and <blank> characters (character class blank), and the behavior of character classes within pattern matching. Changing the value of LC_CTYPE after the shell has started shall not affect the lexical processing of shell commands in the current shell execution environment or its subshells. Invoking a shell script or performing exec sh subjects the new shell to the changes in LC_CTYPE .

## Context

**Duplicate of SHALL-19-05-03-016.** This obligation covers the same `LC_CTYPE` variable text from the same source lines. See SHALL-19-05-03-016 for the full enriched context.

**Summary:** `LC_CTYPE` determines byte-to-character interpretation, letter/blank classification, and character class behavior in pattern matching. Changing LC_CTYPE after shell startup must NOT affect lexical processing in the current shell or subshells. A new shell via script invocation or `exec sh` does see the new value.

See 2.5.3 "Shell Variables" (`LC_CTYPE`) in `utilities/V3_chap02.html`.

## Implementation Notes

*(to be filled during implementation)*
