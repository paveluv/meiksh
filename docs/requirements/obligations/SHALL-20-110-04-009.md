---
id: SHALL-20-110-04-009
category: shall
section: "OPTIONS"
anchor: tag_20_110_04
source_file: utilities/sh.html
source_lines: [84, 84]
keywords: ["shall"]
tier: 1
status: enriched
refs: []
related_sections: []
tests:
  - ../../../tests/spec/SHALL-20-110-04-009.sh
spec_tested: true
reviewed: GPT-5.4
---

## Extracted Text

> Read commands from the standard input.

## Context

The `-s` option directs the shell to read commands from **standard input**. This is the explicit form of the default behavior when no `command_file` and no `-c` are given.

When `-s` is specified:
- Commands are read from stdin (which may be a terminal, pipe, file redirect, etc.)
- If stdin is a terminal and stderr is also a terminal, the shell is interactive
- Any non-option operands become positional parameters (`$1`, `$2`, etc.)

See utilities/sh.html#tag_20_110_04.

GPT-5.4 review: The prior extracted text incorrectly merged the additional-options heading with the `-s` description. The corrected quote now matches the actual POSIX text in `docs/posix/issue8/sh-utility.html#tag_20_110_04`. The enrichment is accurate: this row is specifically about `-s` directing the shell to read commands from standard input.

## Implementation Notes

GPT-5.4 review of `tests/spec/SHALL-20-110-04-009.sh`: the script directly exercises the required behavior by piping commands to `sh -s` and checking that they are executed from stdin.
