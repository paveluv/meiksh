---
id: SHALL-20-110-08-017
category: shall
section: "ENVIRONMENT VARIABLES"
anchor: tag_20_110_08
source_file: utilities/sh.html
source_lines: [212, 212]
keywords: ["shall"]
tier: 1
status: enriched
refs: []
related_sections: []
tests: []
spec_tested: blocked
testing_blockers: "requires multiple locales with different collation; not reliably available in batch tests"
reviewed: GPT-5.4
---

## Extracted Text

> Determine the behavior of range expressions, equivalence classes, and multi-character collating elements within pattern matching.

## Context

**Behavior:** LC_COLLATE determines the behavior of three features within shell pattern matching:

1. **Range expressions** (e.g., `[a-z]`): The collation order determines which characters fall within the range. In the POSIX locale, `[a-z]` matches lowercase ASCII letters; in other locales, the set of matching characters may differ.
2. **Equivalence classes** (e.g., `[[=a=]]`): Characters that collate equivalently are treated as matching. For example, in a French locale, `[[=e=]]` might match é, è, ê, ë.
3. **Multi-character collating elements** (e.g., `[[.ch.]]`): Some locales define multi-character sequences that are treated as single collating elements.

**Applies to:** All shell modes during pattern matching — this includes `case` statements, filename globbing (pathname expansion), and the `[[ ]]` / `test` pattern operations.

GPT-5.4 review: The extraction here is limited to the `sh` utility's statement about which shell behavior this variable affects. Locale-variable precedence comes from XBD 8.2, not from this sentence alone.

**Spec reference:** Section "sh" ENVIRONMENT VARIABLES, line 212 in `utilities/sh.html`.

## Implementation Notes

GPT-5.4 review: The prior extracted text incorrectly included the section heading. The corrected quote now matches `docs/posix/issue8/sh-utility.html#tag_20_110_08`. This remains doc-only because portable verification would require locale sets with distinct collation behavior.
