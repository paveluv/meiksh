---
id: SHALL-20-147-05-004
category: shall
section: "OPERANDS"
anchor: tag_20_147_05
source_file: utilities/wait.html
source_lines: [59, 75]
keywords: ["shall"]
tier: 2
status: enriched
refs: ["../basedefs/V1_chap03.html#tag_03_182", "../utilities/V3_chap02.html#tag_19_13"]
related_sections: []
tests: []
---

## Extracted Text

> One of the following: The unsigned decimal integer process ID of a child process whose termination the utility is to wait for. A job ID (see XBD 3.182 Job ID) that identifies a process group in the case of a job-control background job, or a process ID in the case of a non-job-control background job (if supported), to be waited for. The job ID notation is applicable only for invocations of wait in the current shell execution environment; see 2.13 Shell Execution Environment. The exit status of wait shall be determined by the exit status of the last pipeline to be executed. Note: The job ID type of pid is only available on systems supporting the User Portability Utilities option or supporting non-job-control background jobs.

## Context

**Behavior:** This obligation details the pid operand semantics for `wait`. Key points:

1. **Numeric PID:** An unsigned decimal integer specifying the process ID of a child process to wait for.

2. **Job ID:** A job identifier (e.g., `%1`) as defined in XBD 3.182. For job-control background jobs, the job ID identifies a process group. For non-job-control background jobs, it identifies a single PID.

3. **Scope restriction:** Job ID notation is only valid within the current shell execution environment. It cannot be used in subshells or via `find -exec` etc.

4. **Pipeline exit status:** When waiting for a job that is a pipeline (e.g., `a | b &`), the exit status of `wait` shall be determined by the exit status of the last command in the pipeline.

5. **Availability note:** Job ID support requires the User Portability Utilities option or non-job-control background job support.

**Spec reference:** Section OPERANDS (tag_20_147_05) in `utilities/wait.html`, lines 59-75.

## Implementation Notes

*(to be filled during implementation)*
