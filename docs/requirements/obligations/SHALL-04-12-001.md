---
id: SHALL-04-12-001
category: shall
section: "4.12 File Times Update"
anchor: tag_04_12
source_file: basedefs/V1_chap04.html
source_lines: [234, 234]
keywords: ["unspecified", "shall"]
tier: 2
status: enriched
refs: []
related_sections: []
tests: []
spec_tested: blocked
testing_blockers: "File timestamp update rules are OS-level guarantees; shell relies on OS syscalls for timestamps"
---

## Extracted Text

> Each function or utility in POSIX.1-2024 that reads or writes data (even if the data does not change) or performs an operation to change file status (even if the file status does not change) indicates which of the appropriate timestamps shall be marked for update. If an implementation of such a function or utility marks for update one of these timestamps in a place or time not specified by POSIX.1-2024, this shall be documented, except that any changes caused by pathname resolution need not be documented. For the other functions or utilities in POSIX.1-2024 (those that are not explicitly required to read or write file data or change file status, but that in some implementations happen to do so), the effect is unspecified.

## Context

When shell builtins or utilities read/write files (e.g., redirections, `.` command sourcing a file, `read` builtin reading from a file), the appropriate file timestamps (atime, mtime, ctime) must be marked for update as specified by each operation. The shell itself does not need to manually update timestamps — the underlying OS syscalls handle this. However, if the shell's implementation marks timestamps at non-standard times, this must be documented. For shell operations that aren't explicitly defined as reading/writing data, any timestamp side effects are unspecified.

This is primarily an OS-level guarantee the shell relies on; no special shell code is needed.

See: `basedefs/V1_chap04.html` §4.12 File Times Update.

## Implementation Notes

*(to be filled during implementation)*
