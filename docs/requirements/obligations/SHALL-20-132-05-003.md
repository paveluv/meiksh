---
id: SHALL-20-132-05-003
category: shall
section: "OPERANDS"
anchor: tag_20_132_05
source_file: utilities/umask.html
source_lines: [69, 80]
keywords: ["unspecified", "shall"]
tier: 2
status: enriched
spec_tested: true
refs: ["../utilities/chmod.html"]
related_sections: []
tests: []
---

## Extracted Text

> The following operand shall be supported:: A string specifying the new file mode creation mask. The string is treated in the same way as the mode operand described in the EXTENDED DESCRIPTION section for chmod. For a symbolic_mode value, the new value of the file mode creation mask shall be the logical complement of the file permission bits portion of the file mode specified by the symbolic_mode string. In a symbolic_mode value, the permissions op characters '+' and '-' shall be interpreted relative to the current file mode creation mask; '+' shall cause the bits for the indicated permissions to be cleared in the mask; '-' shall cause the bits for the indicated permissions to be set in the mask. The interpretation of mode values that specify file mode bits other than the file permission bits is unspecified. In the octal integer form of mode, the specified bits are set in the file mode creation mask. The file mode creation mask shall be set to the resulting numeric value. The default output of a prior invocation of umask on the same system with no operand also shall be recognized as a mask operand.

## Context

**Behavior:** The `mask` operand specifies the new file mode creation mask. It is parsed using the same mode syntax as `chmod`'s EXTENDED DESCRIPTION, but with inverted semantics for symbolic modes:

1. **Symbolic mode:** The new mask is the logical complement of the file permission bits specified by the symbolic_mode string. The `+` operator **clears** bits in the mask (granting permissions), and `-` **sets** bits in the mask (revoking permissions). This inversion is because the mask specifies which bits to *remove* from new files.
   - Example: `umask u=rwx,g=rx,o=rx` sets mask to 022 (complement of rwxr-xr-x).
   - Example: `umask g-w` sets the group-write bit in the mask (removes group-write from new files).

2. **Octal integer form:** The specified octal value is set directly as the mask. E.g., `umask 077` sets mask to 077.

3. **Round-trip from default output:** The default output of a prior `umask` invocation (no operand) on the same system shall also be recognized as a valid mask operand.

**Unspecified:** The interpretation of mode values specifying bits other than file permission bits (e.g., setuid, setgid, sticky) is unspecified.

**Spec reference:** Section OPERANDS (tag_20_132_05) in `utilities/umask.html`, lines 69-80. See also `chmod` EXTENDED DESCRIPTION.

## Implementation Notes

*(to be filled during implementation)*
