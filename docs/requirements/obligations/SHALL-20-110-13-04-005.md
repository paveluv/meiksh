---
id: SHALL-20-110-13-04-005
category: shall
section: "vi Line Editing Insert Mode"
anchor: tag_20_110_13_04
source_file: utilities/sh.html
source_lines: [373, 374]
keywords: ["unspecified", "shall"]
tier: 1
status: enriched
refs: []
related_sections: []
tests: []
spec_tested: blocked
testing_blockers: "requires interactive terminal with vi-mode EOF handling at line start; cannot test in batch mode"
reviewed: GPT-5.4
---

## Extracted Text

> Interpreted as the end of input in sh. This interpretation shall occur only at the beginning of an input line. If end-of-file is entered other than at the beginning of the line, the results are unspecified.

## Context

This obligation defines the behavior of the **terminal `end-of-file` character in vi insert mode** (typically Ctrl-D, as configured by `stty`) for the interactive command line editor.

**Behavior required:**
1. When the `end-of-file` character is entered at the **beginning of an input line**, it shall be interpreted as end of input in `sh`.
2. When `end-of-file` is entered at any position **other than** the beginning of the line, the results are **unspecified**.

**Conditions:** Only applies when the shell is interactive, vi editing mode is enabled (`set -o vi`), and the shell is in insert mode.

**"Beginning of an input line":** This means the point identified in the vi-mode definitions before any command text has been entered on the line.

## Implementation Notes

GPT-5.4 review: This quote matches `docs/posix/issue8/sh-utility.html#tag_20_110_13_04`. I tightened the commentary to avoid overclaiming consequences that are not stated in this sentence itself. Canonical coverage requires an interactive terminal session that can deliver the configured EOF character at different cursor states.
