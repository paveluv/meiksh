---
id: SHALL-09-04-02-001
category: shall
section: "9.4.2 ERE Ordinary Characters"
anchor: tag_09_04_02
source_file: basedefs/V1_chap09.html
source_lines: [495, 499]
keywords: ["shall"]
tier: 2
status: enriched
refs: ["#tag_09_04_03"]
related_sections: []
tests: []
---

## Extracted Text

> An ordinary character is an ERE that matches itself. An ordinary character is any character in the supported character set, except for the ERE special characters listed in 9.4.3 ERE Special Characters. When not inside a bracket expression, the interpretation of an ordinary character preceded by an unescaped <backslash> is undefined, except for the ']' and '}' characters; "\]" and "\}" shall match the ']' and '}' characters, respectively.

## Context

An ERE ordinary character is defined as any character from the supported character set that is NOT one of the ERE special characters (`.`, `[`, `\`, `(`, `)`, `*`, `+`, `?`, `{`, `|`, `^`, `$`). An ordinary character used as an ERE atom must match exactly itself. When an ordinary character is preceded by an unescaped backslash outside a bracket expression, the behavior is undefined—except for `\]` and `\}`, which must match the literal `]` and `}` characters respectively. The implementation must not assign meaning to backslash-escaped ordinary characters (e.g., `\a`, `\n` in ERE context) unless a specific utility overrides this. The sequences `\]` and `\}` are the only mandatory backslash-ordinary-character escapes.

## Implementation Notes

*(to be filled during implementation)*
