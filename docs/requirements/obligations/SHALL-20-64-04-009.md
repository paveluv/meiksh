---
id: SHALL-20-64-04-009
category: shall
section: "OPTIONS"
anchor: tag_20_64_04
source_file: utilities/kill.html
source_lines: [88, 90]
keywords: ["shall"]
tier: 3
spec_tested: true
status: enriched
refs: []
related_sections: []
tests: []
reviewed: GPT-5.4
---

## Extracted Text

> [XSI] Specify a non-negative decimal integer, signal_number, representing the signal to be used instead of SIGTERM, as the sig argument in the effective call to kill().

## Context

**Behavior:** The `kill` built-in must support the `-signal_number` option form (XSI extension). This allows specifying the signal as a numeric option, e.g., `kill -9 pid`. See SHALL-20-64-04-010 and SHALL-20-64-04-013 for the defined signal number mappings and disambiguation rules.

**Conditions:** This is an XSI option. The signal_number is a non-negative decimal integer.

**Spec reference:** Section OPTIONS (tag_20_64_04) in `utilities/kill.html`, lines 88-90.

GPT-5.4 review: The prior extracted quote also merged the option-list heading with the option tag and did not match the cited source line. The corrected quote now uses the actual normative sentence from `docs/posix/utilities/kill.html#tag_20_64_04`, which makes the substantive requirement clear: `-signal_number` is an XSI option that accepts a non-negative decimal signal number and uses it as the effective `kill()` signal argument.

## Implementation Notes

GPT-5.4 review of `tests/spec/SHALL-20-64-04-009.sh`: the revised script focuses on the support of the numeric option form itself and now checks both successful acceptance and successful completion of a defined `-signal_number` case from `docs/posix/utilities/kill.html#tag_20_64_04`.
