---
id: SHALL-20-53-11-002
category: shall
section: "STDERR"
anchor: tag_20_53_11
source_file: utilities/getopts.html
source_lines: [172, 179]
keywords: ["unspecified", "shall", "may"]
tier: 2
status: enriched
refs: ["../utilities/V3_chap02.html#tag_19_05_02"]
related_sections: []
tests: []
---

## Extracted Text

> Whenever an error is detected and the first character in the optstring operand is not a <colon> (':'), a diagnostic message shall be written to standard error with the following information in an unspecified format:: The invoking program name shall be identified in the message. The invoking program name shall be the value of the shell special parameter 0 (see 2.5.2 Special Parameters) at the time the getopts utility is invoked. A name equivalent to: basename "$0" may be used.

## Context

**Behavior:** When getopts writes a diagnostic to stderr (verbose mode), the message must identify the invoking program name. The program name shall be the value of `$0` (the shell special parameter 0) at the time getopts is invoked. Alternatively, a name equivalent to `basename "$0"` may be used (i.e., the trailing component of the path).

**Key detail:** The program name is `$0`, not the name of the script function or the word "getopts". This means diagnostic messages look like they come from the calling script/program.

**Spec reference:** POSIX.1-2024, `getopts` STDERR, lines 172-179 of `utilities/getopts.html`; see also Section 2.5.2 Special Parameters.

## Implementation Notes

*(to be filled during implementation)*
