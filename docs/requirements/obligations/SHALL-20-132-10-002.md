---
id: SHALL-20-132-10-002
category: shall
section: "STDOUT"
anchor: tag_20_132_10
source_file: utilities/umask.html
source_lines: [123, 126]
keywords: ["shall"]
tier: 2
status: enriched
spec_tested: true
refs: []
related_sections: []
tests:
  - ../../../tests/spec/SHALL-20-132-10-002.sh
  - ../../../tests/spec/SHALL-20-132-04-004.sh
reviewed: GPT-5.4
---

## Extracted Text

> If -S is specified, the message shall be in the following format: "u=%s,g=%s,o=%s\n", <owner permissions>, <group permissions>, <other permissions>

## Context

**Behavior:** When `umask` is invoked with `-S` and no mask operand, the output shall be in the symbolic format: `"u=%s,g=%s,o=%s\n"` where each `%s` is a combination of letters from the set {r, w, x}.

**Output format:** The format string is `u=%s,g=%s,o=%s\n` with:
- `%s` for owner permissions: combination of r, w, x
- `%s` for group permissions: combination of r, w, x
- `%s` for other permissions: combination of r, w, x

**Example:** For mask 022, output is `u=rwx,g=rx,o=rx\n`.

**Spec reference:** Section STDOUT (tag_20_132_10) in `utilities/umask.html`, line 123.

GPT-5.4 review: The earlier extracted text stopped before the actual format string and therefore did not fully quote the requirement from `docs/posix/utilities/umask.html#tag_20_132_10`. The corrected quote now includes the normative format itself and the matching source span. The enrichment is accurate, though the immediately following POSIX sentence further constrains what each `%s` may contain.

## Implementation Notes

GPT-5.4 review of `tests/spec/SHALL-20-132-10-002.sh`: the strengthened script now checks the exact `u=...,g=...,o=...` shape more explicitly. `tests/spec/SHALL-20-132-04-004.sh` also covers the same `-S` symbolic-output behavior with a concrete value check.
