---
id: SHALL-20-132-08-009
category: shall
section: "ENVIRONMENT VARIABLES"
anchor: tag_20_132_08
source_file: utilities/umask.html
source_lines: [107, 108]
keywords: ["shall"]
tier: 2
status: enriched
spec_tested: blocked
testing_blockers: "LC_MESSAGES description — requires non-C locales; cannot portably test"
refs: []
related_sections: []
tests: []
reviewed: GPT-5.4
---

## Extracted Text

> Determine the locale that should be used to affect the format and contents of diagnostic messages written to standard error.

## Context

**Behavior:** `LC_MESSAGES` determines the locale that affects the format and contents of diagnostic messages written to standard error.

**Conditions:** Applicable whenever `umask` needs to write a diagnostic message to stderr.

**Spec reference:** Section ENVIRONMENT VARIABLES (tag_20_132_08) in `utilities/umask.html`, line 107.

GPT-5.4 review: The earlier extracted text merged the section heading with the `LC_MESSAGES` description and also cited too narrow a source span. The corrected quote now matches the actual POSIX description in `docs/posix/utilities/umask.html#tag_20_132_08`. The enrichment is accurate, and the blocked-test rationale remains reasonable because this diagnostic localization behavior is delegated to locale infrastructure rather than shell-specific `umask` logic.

## Implementation Notes

GPT-5.4 review: no dedicated `tests/spec` script exists for this locale-framework row.
