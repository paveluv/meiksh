---
id: SHALL-20-110-06-004
category: shall
section: "STDIN"
anchor: tag_20_110_06
source_file: utilities/sh.html
source_lines: [140, 142]
keywords: ["shall"]
tier: 1
status: enriched
refs: ["../utilities/read.html"]
related_sections: []
tests:
  - ../../../tests/spec/SHALL-20-110-06-004.sh
spec_tested: true
reviewed: GPT-5.4
---

## Extracted Text

> The script executes one or more commands that require input from standard input (such as a read command that does not redirect its input).

## Context

**Behavior:** Even when the shell is reading commands from a script file (not stdin), individual commands within that script may still read from standard input. For example, the `read` builtin without input redirection reads from stdin.

**Conditions:** This condition means stdin is "used" by the script's commands, not by the shell's command parser. The shell must not interfere with or consume stdin data that is intended for child commands.

**Applies to:** Commands like `read`, `cat` (without file args), or any command that reads stdin. The shell itself is reading its command stream from a file, but stdin is still available to child processes.

**Spec reference:** STDIN section of `utilities/sh.html`, line 140–142. See also `utilities/read.html` for the `read` builtin.

## Implementation Notes

GPT-5.4 review: The prior extracted text incorrectly merged the STDIN parent sentence with the third bullet. The corrected quote now matches `docs/posix/issue8/sh-utility.html#tag_20_110_06`. The script is a good direct demonstration that a script read from a file can still pass stdin through to a command inside that script.
