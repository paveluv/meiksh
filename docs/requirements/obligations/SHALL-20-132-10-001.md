---
id: SHALL-20-132-10-001
category: shall
section: "STDOUT"
anchor: tag_20_132_10
source_file: utilities/umask.html
source_lines: [121, 122]
keywords: ["shall"]
tier: 2
status: enriched
spec_tested: true
refs: []
related_sections: []
tests: []
---

## Extracted Text

> When the mask operand is not specified, the umask utility shall write a message to standard output that can later be used as a umask mask operand.

## Context

**Behavior:** When `umask` is invoked in query mode (no mask operand), it shall write the current file mode creation mask to stdout. The output must be a string that is valid as a `mask` operand to a subsequent `umask` invocation on the same system, restoring the same mask value.

**Output format:** The format depends on whether `-S` is specified. Without `-S`, the format is implementation-defined (typically octal, e.g., `0022`). With `-S`, symbolic format is used.

**Round-trip requirement:** The key requirement is that the output can be fed back to `umask` as an operand. This means the output must be a syntactically valid mask string.

**Spec reference:** Section STDOUT (tag_20_132_10) in `utilities/umask.html`, lines 121-122.

## Implementation Notes

*(to be filled during implementation)*
