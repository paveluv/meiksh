---
id: SHALL-20-64-05-003
category: shall
section: "OPERANDS"
anchor: tag_20_64_05
source_file: utilities/kill.html
source_lines: [124, 144]
keywords: ["shall"]
tier: 3
status: enriched
refs: ["../functions/kill.html", "../functions/kill.html", "../basedefs/V1_chap03.html#tag_03_182", "../utilities/V3_chap02.html#tag_19_13"]
related_sections: []
tests: []
---

## Extracted Text

> The following operands shall be supported:: One of the following: A decimal integer specifying a process or process group to be signaled. The process or processes selected by positive, negative, and zero values of the pid operand shall be as described for the kill() function. If process number 0 is specified, all processes in the current process group shall be signaled. For the effects of negative pid numbers, see the kill() function defined in the System Interfaces volume of POSIX.1-2024. If the first pid operand is negative, it should be preceded by "--" to keep it from being interpreted as an option. A job ID (see XBD 3.182 Job ID) that identifies a process group in the case of a job-control background job, or a process ID in the case of a non-job-control background job (if supported), to be signaled. The job ID notation is applicable only for invocations of kill in the current shell execution environment; see 2.13 Shell Execution Environment. Note: The job ID type of pid is only available on systems supporting the User Portability Utilities option or supporting non-job-control background jobs.

## Context

**Behavior:** The `pid` operand accepts two forms:

1. **Decimal integer (process/process-group ID):**
   - **Positive value:** Signals the process with that PID (equivalent to `kill(pid, sig)`).
   - **Zero (0):** Signals all processes in the current process group (equivalent to `kill(0, sig)`).
   - **Negative value:** Signals all processes in the process group whose PGID is the absolute value (equivalent to `kill(-pgid, sig)`). A negative first pid operand should be preceded by `--` to avoid option parsing ambiguity.

2. **Job ID (e.g., `%1`, `%foo`):**
   - For job-control background jobs: identifies the process group.
   - For non-job-control background jobs (if supported): identifies the process ID.
   - Job ID notation only works within the current shell execution environment (not across exec boundaries or subshells running as separate utilities).

**Error handling:** If a pid operand does not match a known process/process group/job, the implementation should report an error and continue processing remaining operands.

**Spec reference:** Section OPERANDS (tag_20_64_05) in `utilities/kill.html`, lines 124–144. See also XBD 3.182 Job ID, XCU 2.13 Shell Execution Environment, and XSH kill().

## Implementation Notes

*(to be filled during implementation)*
