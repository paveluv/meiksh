---
id: SHALL-20-110-14-007
category: shall
section: "EXIT STATUS"
anchor: tag_20_110_14
source_file: utilities/sh.html
source_lines: [687, 688]
keywords: ["shall"]
tier: 1
status: enriched
spec_tested: true
refs: ["../utilities/V3_chap02.html#tag_19_09_01_04"]
related_sections: []
tests:
  - ../../../tests/spec/SHALL-20-110-14-007.sh
reviewed: GPT-5.4
---

## Extracted Text

> A specified command_file could not be executed due to an [ENOEXEC] error (see 2.9.1.4 Command Search and Execution, item 2).

## Context

**Behavior:** The `sh` utility shall return exit status 126 when a specified command_file could not be executed because the system returned an ENOEXEC error. Per 2.9.1.4 Command Search and Execution item 2:
- The file was found.
- An attempt was made to execute it (e.g., via `execve()`).
- The system returned ENOEXEC, meaning the file is not in a recognized executable format.

**Applies to:** Non-interactive `sh` invocations with a command_file operand (e.g., `sh myscript.sh` where myscript.sh triggers ENOEXEC).

**Details:** In practice, this situation can arise when:
- A script has a `#!` line pointing to a non-existent interpreter.
- A binary file is for a different architecture.
- The file has execute permission but is not a valid script or binary.

**Cross-reference:** See 2.9.1.4 Command Search and Execution (`utilities/V3_chap02.html#tag_19_09_01_04`), item 2.

**Spec reference:** Section "sh" EXIT STATUS, lines 687-688 in `utilities/sh.html`.

## Implementation Notes

GPT-5.4 review: The prior extracted text incorrectly merged the EXIT STATUS heading with the condition text for exit status `126`. The corrected quote now matches `docs/posix/issue8/sh-utility.html#tag_20_110_14`. I also removed the speculative note about how normal command-execution fallback interacts with this row, because that overreached beyond the cited EXIT STATUS text.
