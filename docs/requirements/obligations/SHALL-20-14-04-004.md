---
id: SHALL-20-14-04-004
category: shall
section: "OPTIONS"
anchor: tag_20_14_04
source_file: utilities/cd.html
source_lines: [138, 139]
keywords: ["shall"]
tier: 2
spec_tested: blocked
testing_blockers: "Requires creating a directory where cd -eP succeeds but PWD cannot be determined (needs permission manipulation on parent directories)"
status: enriched
refs: []
related_sections: []
tests: []
---

## Extracted Text

> The following options shall be supported by the implementation:: If the -P option is in effect, the current working directory is successfully changed, and the correct value of the PWD environment variable cannot be determined, exit with exit status 1.

## Context

When `-e` is combined with `-P` and the working directory is successfully changed but the correct value of `PWD` cannot be determined (e.g., due to insufficient permissions on parent directories), `cd` **shall** exit with exit status 1. The directory change itself succeeds — the process is now in the new directory — but the shell cannot determine the absolute physical path, so it signals this with a non-zero exit. Reference: cd utility OPTIONS.

## Implementation Notes

*(to be filled during implementation)*
