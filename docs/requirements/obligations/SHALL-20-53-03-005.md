---
id: SHALL-20-53-03-005
category: shall
section: "DESCRIPTION"
anchor: tag_20_53_03
source_file: utilities/getopts.html
source_lines: [54, 56]
keywords: ["unspecified", "shall"]
tier: 2
status: enriched
refs: []
related_sections: []
tests: []
spec_tested: true
---

## Extracted Text

> When the shell is first invoked, the shell variable OPTIND shall be initialized to 1. Each time getopts is invoked, it shall place the value of the next option found in the parameter list in the shell variable specified by the name operand and the shell variable OPTIND shall be set as follows:: In all other cases, the value of OPTIND is unspecified, but shall encode the information needed for the next invocation of getopts to resume parsing options after the option just parsed.

## Context

In cases other than "successful parse with option-argument" and "end of options" — specifically, when parsing an option WITHOUT an option-argument, or when encountering an unknown option character — the value of `OPTIND` is unspecified but must encode enough state for the next `getopts` call to resume correctly. This is important because multiple single-character options can be grouped in one parameter (e.g., `-abc`), so `OPTIND` may need to encode both the parameter index and the position within that parameter. The implementation may use sub-indexing or any other mechanism, as long as sequential `getopts` calls correctly parse all options. Reference: POSIX.1-2024 §getopts, DESCRIPTION (tag_20_53_03).

## Implementation Notes

*(to be filled during implementation)*
