---
id: SHALL-20-110-13-05-079
category: shall
section: "vi Line Editing Command Mode"
anchor: tag_20_110_13_05
source_file: utilities/sh.html
source_lines: [567, 569]
keywords: ["shall"]
tier: 1
status: enriched
refs: []
related_sections: []
tests: []
spec_tested: blocked
testing_blockers: "vi command mode replace-char (r) requires interactive TTY simulation"
---

## Extracted Text

> The following commands shall be recognized in command mode:: Replace the current character with the character 'c'. With a number count, replace the current and the following count-1 characters. After this command, the current cursor position shall be on the last character that was changed. If the count is larger than the number of characters after the cursor, this shall not be considered an error; all of the remaining characters shall be changed.

## Context

**Behavior:** In vi command mode, `rc` replaces the current character with character `c`. With a count, replace the current and the following count-1 characters (total of count characters). After replacement, cursor is positioned on the last character that was changed.

**Edge cases:** If count exceeds remaining characters after cursor: NOT an error; all remaining characters are replaced.

**Implementation:** Replace characters from cursor to cursor+count-1 (clamped to line end) with `c`. Position cursor on the last replaced character.

**Spec reference:** vi Line Editing Command Mode in `utilities/sh.html`, line 567–570.

## Implementation Notes

*(to be filled during implementation)*
