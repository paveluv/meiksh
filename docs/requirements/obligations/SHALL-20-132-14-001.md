---
id: SHALL-20-132-14-001
category: shall
section: "EXIT STATUS"
anchor: tag_20_132_14
source_file: utilities/umask.html
source_lines: [146, 146]
keywords: ["shall"]
tier: 2
status: enriched
spec_tested: blocked
testing_blockers: "Enumeration preamble for exit values — tested via SHALL-20-132-14-002 and SHALL-20-132-14-004"
refs: []
related_sections: []
tests:
  - ../../../tests/spec/SHALL-20-132-14-002.sh
  - ../../../tests/spec/SHALL-20-132-14-004.sh
reviewed: GPT-5.4
---

## Extracted Text

> The following exit values shall be returned:

## Context

**Behavior:** The `umask` utility shall return exit status 0 on success (mask changed or displayed) and >0 on error. This is an enumeration preamble for the exit status values.

**Exit codes:**
- 0: The file mode creation mask was successfully changed, or no mask operand was supplied (query mode succeeded).
- >0: An error occurred (e.g., invalid mask operand, invalid option).

**Spec reference:** Section EXIT STATUS (tag_20_132_14) in `utilities/umask.html`, line 146.

GPT-5.4 review: The extracted quote is faithful to `docs/posix/utilities/umask.html#tag_20_132_14`. This row is correctly blocked as an exit-status preamble rather than an independently testable behavior. The operative success and error meanings live in the subordinate rows that follow.

## Implementation Notes

GPT-5.4 review: no separate `tests/spec` script is needed for this framing row; the relevant behavior is covered by `tests/spec/SHALL-20-132-14-002.sh` and `tests/spec/SHALL-20-132-14-004.sh`.
