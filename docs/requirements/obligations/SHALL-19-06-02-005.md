---
id: SHALL-19-06-02-005
category: shall
section: "2.6.2 Parameter Expansion"
anchor: tag_19_06_02
source_file: utilities/V3_chap02.html
source_lines: [799, 804]
keywords: ["unspecified", "shall"]
tier: 1
spec_tested: true
status: enriched
refs: ["#tag_19_02_03"]
related_sections: []
tests: []
---

## Extracted Text

> In addition, a parameter expansion can be modified by using one of the following formats. In each case that a value of word is needed (based on the state of parameter, as described below), word shall be subjected to tilde expansion, parameter expansion, command substitution, arithmetic expansion, and quote removal. If word is not needed, it shall not be expanded. The '}' character that delimits the following parameter expansion modifications shall be determined as described previously in this section and in 2.2.3 Double-Quotes. If parameter is '*' or '@', the result of the expansion is unspecified.

## Context

**Behavior:** This obligation governs the modifier forms of parameter expansion (`${param:-word}`, `${param:=word}`, `${param:?word}`, `${param:+word}`). Key rules:

1. **Conditional expansion of `word`:** The `word` part is only expanded when needed based on the parameter's state. When expansion occurs, `word` undergoes: tilde expansion → parameter expansion → command substitution → arithmetic expansion → quote removal (in that order, the standard expansion sequence minus field splitting and pathname expansion).
2. **Lazy evaluation:** If `word` is not needed (e.g., parameter is set and non-null for `:-`), `word` must NOT be expanded at all — no side effects from command substitutions or assignments in `word`.
3. **Closing brace determination:** The matching `}` follows the same nesting rules as SHALL-19-06-02-001 and the quoting rules from 2.2.3 Double-Quotes.
4. **`*` and `@` restriction:** Using `*` or `@` as the parameter in these modifier forms produces unspecified results.

**Conditions:** Applies to all four modifier forms: `:-`, `:=`, `:?`, `:+` (and their no-colon variants `-`, `=`, `?`, `+`).

**Spec reference:** See 2.6.2 Parameter Expansion in utilities/V3_chap02.html, lines 799–804. See 2.2.3 Double-Quotes (#tag_19_02_03) for quoting interaction.

## Implementation Notes

*(to be filled during implementation)*
