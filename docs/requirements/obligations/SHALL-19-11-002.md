---
id: SHALL-19-11-002
category: shall
section: "2.11 Job Control"
anchor: tag_19_11
source_file: utilities/V3_chap02.html
source_lines: [2743, 2743]
keywords: ["shall", "may"]
tier: 1
status: enriched
refs: []
related_sections: []
tests: []
spec_tested: blocked
testing_blockers: "Requires controlling terminal with foreground process group manipulation; cannot test setpgid/tcsetpgrp in non-interactive script"
---

## Extracted Text

> If the shell has a controlling terminal and it is the controlling process for the terminal session, it shall initially set the foreground process group ID associated with the terminal to its own process group ID. Otherwise, if it has a controlling terminal, it shall initially perform the following steps if interactive and may perform them if non-interactive:: If its process group is the foreground process group associated with the terminal, the shell shall set its process group ID to its process ID (if they are not already equal) and set the foreground process group ID associated with the terminal to its process group ID.

## Context

**Job control initialization — foreground process group step (parent obligation):** When the shell is not the session leader but has a controlling terminal, and its process group IS the foreground process group, the shell shall:
1. Call `setpgid(0, getpid())` to set its process group ID to its own PID (making it a process group leader), if they are not already equal.
2. Call `tcsetpgrp(fd, getpgrp())` to set the terminal's foreground process group ID to its (new) process group ID.

This step is mandatory for interactive shells and optional for non-interactive shells. It ensures the shell owns the terminal for job control purposes. The combination of SHALL-19-11-002 and SHALL-19-11-003 forms a loop: check if foreground → claim terminal, else stop and retry after SIGCONT.

## Implementation Notes

*(to be filled during implementation)*
