---
id: SHALL-20-64-10-003
category: shall
section: "STDOUT"
anchor: tag_20_64_10
source_file: utilities/kill.html
source_lines: [193, 194]
keywords: ["shall"]
tier: 3
spec_tested: true
status: enriched
refs: []
related_sections: []
tests: []
reviewed: GPT-5.4
---

## Extracted Text

> where the <signal_name> is in uppercase, without the SIG prefix, and the <separator> shall be either a <newline> or a <space>. For the last signal written, <separator> shall be a <newline>.

## Context

**Behavior:** When listing all signals (`kill -l` with no operand):
1. Each `<signal_name>` must be written in **uppercase** and **without** the `SIG` prefix (e.g., `HUP` not `SIGHUP`).
2. The `<separator>` between signal names must be either a newline (`\n`) or a space (` `). The implementation may choose either, and may mix them (e.g., spaces within a line, newlines between lines).
3. The **last** signal name written must be followed by a **newline** (not a space). This ensures the output is properly terminated.

**Spec reference:** Section STDOUT (tag_20_64_10) in `utilities/kill.html`, lines 193–194.

GPT-5.4 review: The extracted quote matches `docs/posix/utilities/kill.html#tag_20_64_10`. One useful precision point is that this sentence completes the immediately preceding `"%s%c"` format rule in the same POSIX section. That means review and testing should check not only uppercase names and the absence of `SIG`, but also that the separators are limited to spaces or newlines and that the final separator is a newline.

## Implementation Notes

GPT-5.4 review of `tests/spec/SHALL-20-64-10-003.sh`: the strengthened script now checks the allowed output character set more directly, which gives better coverage of the separator and uppercase constraints from `docs/posix/utilities/kill.html#tag_20_64_10`.
