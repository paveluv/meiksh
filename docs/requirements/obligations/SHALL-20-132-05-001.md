---
id: SHALL-20-132-05-001
category: shall
section: "OPERANDS"
anchor: tag_20_132_05
source_file: utilities/umask.html
source_lines: [65, 65]
keywords: ["shall"]
tier: 2
status: enriched
spec_tested: blocked
testing_blockers: "Enumeration preamble ('The following operand shall be supported') — operand tested via SHALL-20-132-05-003 and SHALL-20-132-05-004"
refs: []
related_sections: []
tests:
  - ../../../tests/spec/SHALL-20-132-05-003.sh
  - ../../../tests/spec/SHALL-20-132-05-004.sh
reviewed: GPT-5.4
---

## Extracted Text

> The following operand shall be supported:

## Context

**Behavior:** The `umask` utility shall support the `mask` operand. This is an enumeration preamble indicating the mandatory operand set. The `mask` operand is optional (omitting it triggers query mode).

**Spec reference:** Section OPERANDS (tag_20_132_05) in `utilities/umask.html`, line 65.

GPT-5.4 review: The extracted quote is faithful to `docs/posix/utilities/umask.html#tag_20_132_05`. This row is correctly treated as an operand-list preamble rather than an independently testable behavior. The executable coverage belongs in the substantive `mask` operand rows that follow.

## Implementation Notes

GPT-5.4 review: no separate `tests/spec` script is needed for this framing row; the relevant behavior is covered by `tests/spec/SHALL-20-132-05-003.sh` and `tests/spec/SHALL-20-132-05-004.sh`.
