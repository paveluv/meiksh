---
id: SHALL-19-09-05-002
category: shall
section: "2.9.5 Function Definition Command"
anchor: tag_19_09_05
source_file: utilities/V3_chap02.html
source_lines: [2386, 2389]
keywords: ["shall"]
tier: 1
status: enriched
refs: ["#tag_19_06", "#tag_19_08_01"]
related_sections: []
tests: []
---

## Extracted Text

> When the function is declared, none of the expansions in 2.6 Word Expansions shall be performed on the text in compound-command or io-redirect; all expansions shall be performed as normal each time the function is called. Similarly, the optional io-redirect redirections and any variable assignments within compound-command shall be performed during the execution of the function itself, not the function definition. See 2.8.1 Consequences of Shell Errors for the consequences of failures of these operations on interactive and non-interactive shells.

## Context

**Behavior:** When a function is defined, the shell must **not** perform any word expansions on the function body (`compound-command`) or its `io-redirect` at definition time. All expansions are deferred and performed each time the function is called.

Specifically:
1. Parameter expansion, command substitution, arithmetic expansion, tilde expansion, field splitting, pathname expansion, and quote removal are **not** done on the function body text at definition time.
2. Redirections attached to the function definition (the optional `io-redirect`) are also not processed until the function is invoked.
3. Variable assignments within the function body are executed during function invocation, not definition.

**Error handling:** If expansions or redirections fail during function execution, the consequences are as described in 2.8.1 Consequences of Shell Errors (interactive shells continue; non-interactive shells may exit depending on the error type).

**Applies to:** All shell modes.

**Spec reference:** Section 2.9.5 Function Definition Command in `utilities/V3_chap02.html`, lines 2386-2389. Cross-ref: 2.6 Word Expansions, 2.8.1 Consequences of Shell Errors.

## Implementation Notes

*(to be filled during implementation)*
