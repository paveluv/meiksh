---
id: UNSPEC-18-01-01-07-001
category: unspecified
section: "1.1.1.7 File Contents"
anchor: tag_18_01_01_07
source_file: utilities/V3_chap01.html
source_lines: [746, 746]
keywords: ["unspecified"]
tier: 2
status: enriched
refs: ["../functions/read.html"]
related_sections: []
tests: []
---

## Extracted Text

> If the read() function calls would return an error, it is unspecified whether the contents of the file are considered to include any data from offsets in the file beyond where the error would be returned.

## Context

When reading file contents via the `read()` system call (e.g., sourcing scripts, reading here-documents, processing command substitutions from files), if a `read()` call would return an error at some offset within the file, it is **unspecified** whether the shell considers any data beyond that error offset as part of the file contents. The shell implementation may either stop processing at the error boundary or may have already buffered data beyond it. This affects how the shell handles partial reads from files on unreliable storage or special file systems. No specific behavior is required; the shell need not detect or recover from mid-file read errors in any particular way. Reference: XCU 1.1.1.7 "File Contents".

## Implementation Notes

*(to be filled during implementation)*
