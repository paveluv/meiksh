---
id: SHALL-11-01-07-01-001
category: shall
section: "Case A: MIN>0, TIME>0"
anchor: tag_11_01_07_01
source_file: basedefs/V1_chap11.html
source_lines: [183, 191]
keywords: ["shall"]
tier: 3
status: enriched
refs: ["../functions/read.html", "../functions/read.html"]
related_sections: []
tests: []
---

## Extracted Text

> In case A, TIME serves as an inter-byte timer which shall be activated after the first byte is received. Since it is an inter-byte timer, it shall be reset after a byte is received. The interaction between MIN and TIME is as follows. As soon as one byte is received, the inter-byte timer shall be started. If MIN bytes are received before the inter-byte timer expires (remember that the timer is reset upon receipt of each byte), the read shall be satisfied. If the timer expires before MIN bytes are received, the characters received to that point shall be returned to the user. Note that if TIME expires at least one byte shall be returned because the timer would not have been enabled unless a byte was received. In this case (MIN>0, TIME>0) the read shall block until the MIN and TIME mechanisms are activated by the receipt of the first byte, or a signal is received. If data is in the buffer at the time of the read(), the result shall be as if data has been received immediately after the read().

## Context

**Behavior:** Non-canonical mode Case A (MIN>0, TIME>0):
1. `read()` shall **block** until the first byte arrives or a signal is received.
2. After the first byte, an inter-byte timer of TIME*0.1 seconds shall start.
3. The timer shall be **reset** each time a byte is received.
4. If MIN bytes are received before the timer expires → `read()` returns MIN bytes.
5. If the timer expires before MIN bytes → `read()` returns whatever bytes have been received (at least 1).
6. If data is already in the buffer at `read()` time, behave as if it arrived immediately after the call.

**Conditions:** Applies when ICANON is not set and `c_cc[VMIN] > 0` and `c_cc[VTIME] > 0`.

**Shell relevance:** This mode could be used for multi-byte escape sequence detection in the line editor. For example, setting MIN=1, TIME=1 (0.1s timeout) would let the shell read a single ESC and then wait briefly for the rest of an escape sequence before deciding it was a bare ESC key.

**Spec reference:** POSIX.1-2024, §11.1.7.1 Case A: MIN>0, TIME>0 (tag_11_01_07_01), `basedefs/V1_chap11.html` lines 183–191.

## Implementation Notes

*(to be filled during implementation)*
