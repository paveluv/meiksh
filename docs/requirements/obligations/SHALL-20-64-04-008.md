---
id: SHALL-20-64-04-008
category: shall
section: "OPTIONS"
anchor: tag_20_64_04
source_file: utilities/kill.html
source_lines: [84, 86]
keywords: ["shall"]
tier: 3
spec_tested: true
status: enriched
refs: ["javascript:open_code('XSI')"]
related_sections: []
tests: []
reviewed: GPT-5.4
---

## Extracted Text

> The following options shall be supported:: [XSI] Equivalent to -s signal_name.

## Context

**Behavior:** The `-signal_name` option form (XSI) is exactly equivalent to `-s signal_name`. The implementation must treat `kill -TERM pid` identically to `kill -s TERM pid`.

**Conditions:** This equivalence holds for all signal names recognized by the `-s` option, including the special name `0` and case-independent matching.

**Spec reference:** Section OPTIONS (tag_20_64_04) in `utilities/kill.html`, lines 84–86. XSI extension.

GPT-5.4 review: The extracted quote is exact for `docs/posix/utilities/kill.html#tag_20_64_04`. The enrichment correctly captures the obligation: `-signal_name` is not merely accepted, it is equivalent to `-s signal_name`. Tests should therefore compare behavior across the two forms rather than only proving that each works independently.

## Implementation Notes

GPT-5.4 review of `tests/spec/SHALL-20-64-04-008.sh`: the script already had the right overall idea. The revised version makes the equivalence check more explicit by verifying that both forms produce the same observable effect and both remain silent on stderr for the same named signal.
