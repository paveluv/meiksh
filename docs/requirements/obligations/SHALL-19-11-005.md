---
id: SHALL-19-11-005
category: shall
section: "2.11 Job Control"
anchor: tag_19_11
source_file: utilities/V3_chap02.html
source_lines: [2746, 2746]
keywords: ["shall"]
tier: 1
status: enriched
refs: []
related_sections: []
tests: []
spec_tested: blocked
testing_blockers: "Requires controlling terminal and background process group; cannot test SIGTTIN self-stop in non-interactive script"
---

## Extracted Text

> If its process group is not the foreground process group associated with the terminal (which would result from it being started by a job-control shell as a background job), the shell shall either stop itself by sending itself a SIGTTIN signal or, if interactive, attempt to read from standard input (which generates a SIGTTIN signal if standard input is the controlling terminal). If it is stopped, then when it continues execution (after receiving a SIGCONT signal) it shall repeat these steps.

## Context

**Job control initialization — stop self when not foreground (detail):** This is the specific step when the shell's process group is NOT the foreground group. The shell shall either send itself SIGTTIN or attempt to read from stdin (triggering SIGTTIN). If stopped, upon SIGCONT the shell shall re-execute the initialization steps from the beginning.

This is a detail obligation from the same source line as SHALL-19-11-003. The retry loop continues until the shell successfully becomes the foreground process group.

## Implementation Notes

*(to be filled during implementation)*
