---
id: SHALL-20-110-13-05-049
category: shall
section: "vi Line Editing Command Mode"
anchor: tag_20_110_13_05
source_file: utilities/sh.html
source_lines: [504, 504]
keywords: ["shall"]
tier: 1
status: enriched
refs: []
related_sections: []
tests: []
---

## Extracted Text

> The following commands shall be recognized in command mode:: Move to the countth character position on the current command line. If no number is specified, move to the first position. The first character position shall be numbered 1. If the count is larger than the number of characters on the line, this shall not be considered an error; the cursor shall be placed on the last character on the line.

## Context

**Behavior:** In vi command mode, `|` moves the cursor to the count-th character position (1-indexed). If no count is specified, moves to position 1 (the first character). The first character position is numbered 1.

**Edge cases:** If count exceeds the number of characters on the line, NOT an error; cursor is placed on the last character.

**Implementation:** Set cursor to `min(count - 1, len(line) - 1)`. Default count is 1 (no preceding digits). This is a motion command.

**Spec reference:** vi Line Editing Command Mode in `utilities/sh.html`, line 504–506.

## Implementation Notes

*(to be filled during implementation)*
