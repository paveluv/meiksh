---
id: SHALL-20-64-03-005
category: shall
section: "DESCRIPTION"
anchor: tag_20_64_03
source_file: utilities/kill.html
source_lines: [49, 50]
keywords: ["shall"]
tier: 3
spec_tested: blocked
testing_blockers: "Duplicate of SHALL-20-64-03-003; same requirement re-extracted from same source lines"
status: enriched
refs: []
related_sections: []
tests:
  - ../../../tests/spec/SHALL-20-64-03-003.sh
reviewed: GPT-5.4
---

## Extracted Text

> The value of the pid operand shall be used as the pid argument.

## Context

**Behavior:** The pid operand value from the command line **shall** be passed directly as the `pid` argument to the `kill()` system call. This is a restatement/duplicate of SHALL-20-64-03-003.

**Conditions:** The operative requirement is that the `pid` operand value becomes the `kill()` `pid` argument.

**Shell relevance:** See SHALL-20-64-03-003 for full details. The `kill` builtin must correctly parse and pass the pid value. Special values: 0 signals the current process group, -1 signals all permissible processes, negative values signal specific process groups.

**Spec reference:** POSIX.1-2024, `kill` DESCRIPTION (tag_20_64_03), `utilities/kill.html` lines 49–50.

GPT-5.4 review: This extracted quote duplicates the same POSIX sentence already reviewed under `SHALL-20-64-03-003`, and the duplicate classification is correct. The enrichment is mostly fine; the only useful tightening is to avoid overcommitting to a particular parsing description when the quoted sentence itself is about operand-to-argument propagation.

## Implementation Notes

GPT-5.4 review: no separate `tests/spec/SHALL-20-64-03-005.sh` exists. The relevant behavior is already reviewed and tested by the canonical duplicate row `SHALL-20-64-03-003` in `tests/spec/SHALL-20-64-03-003.sh`.
