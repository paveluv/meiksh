---
id: SHALL-04-12-002
category: shall
section: "4.12 File Times Update"
anchor: tag_04_12
source_file: basedefs/V1_chap04.html
source_lines: [240, 248]
keywords: ["unspecified", "shall", "may"]
tier: 2
status: enriched
refs: []
related_sections: []
tests: []
---

## Extracted Text

> An implementation may update timestamps that are marked for update immediately, or it may update such timestamps periodically. At the point in time when an update occurs, any marked timestamps shall be set to the current time and the update marks shall be cleared. All timestamps that are marked for update shall be updated when the file ceases to be open by any process or before a fstat(), fstatat(), fsync(), futimens(), lstat(), stat(), utimensat(), or utimes() is successfully performed on the file. Other times at which updates are done are unspecified. Marks for update, and updates themselves, shall not be done for files on read-only file systems; see 3.295 Read-Only File System.

## Context

This is an OS-level guarantee about when file timestamp updates are flushed. For the shell, this means that after a shell redirection closes a file descriptor, or after a builtin finishes writing to a file, the file's timestamps will reflect the operations performed. The shell can rely on timestamps being current when it subsequently stats a file (e.g., for test `-nt`/`-ot` comparisons). No special shell code is needed — the OS handles timestamp flushing on file close and stat operations. The shell should be aware that files on read-only file systems will not have timestamps updated.

See: `basedefs/V1_chap04.html` §4.12 File Times Update.

## Implementation Notes

*(to be filled during implementation)*
