---
id: SHALL-19-09-04-01-002
category: shall
section: "2.9.4.1 Grouping Commands"
anchor: tag_19_09_04_01
source_file: utilities/V3_chap02.html
source_lines: [2223, 2228]
keywords: ["shall", "may"]
tier: 1
status: enriched
refs: ["#tag_19_13"]
related_sections: []
tests: []
---

## Extracted Text

> Execute compound-list in a subshell environment; see 2.13 Shell Execution Environment. Variable assignments and built-in commands that affect the environment shall not remain in effect after the list finishes. If a character sequence beginning with "((" would be parsed by the shell as an arithmetic expansion if preceded by a '$', shells which implement an extension whereby "((expression))" is evaluated as an arithmetic expression may treat the "((" as introducing as an arithmetic evaluation instead of a grouping command. A conforming application shall ensure that it separates the two leading '(' characters with white space to prevent the shell from performing an arithmetic evaluation.

## Context

**Behavior:** The `{ compound-list; }` brace group executes the compound-list in the **current** shell execution environment (not a subshell). Variable assignments and built-in side effects persist after the brace group finishes.

**Key constraint (same extracted text as -001 but different obligation):** Variable assignments and built-in commands that affect the environment **shall not remain** in effect after the `( )` subshell list finishes. This is the subshell isolation guarantee — the `( )` form creates a copy of the environment and discards changes on exit.

**Contrast with brace group:** `{ list; }` runs in the current environment; `( list )` runs in a subshell environment.

**Applies to:** All shell modes.

**Spec reference:** Section 2.9.4.1 Grouping Commands in `utilities/V3_chap02.html`, lines 2223-2228. Cross-ref: 2.13 Shell Execution Environment.

## Implementation Notes

*(to be filled during implementation)*
