---
id: SHALL-20-64-04-002
category: shall
section: "OPTIONS"
anchor: tag_20_64_04
source_file: utilities/kill.html
source_lines: [65, 65]
keywords: ["shall"]
tier: 3
spec_tested: blocked
testing_blockers: "Framing statement introducing options list; not independently testable beyond sub-obligations"
status: enriched
refs: []
related_sections: []
tests:
  - ../../../tests/spec/SHALL-20-64-04-003.sh
  - ../../../tests/spec/SHALL-20-64-04-005.sh
  - ../../../tests/spec/SHALL-20-64-04-007.sh
  - ../../../tests/spec/SHALL-20-64-04-009.sh
reviewed: GPT-5.4
---

## Extracted Text

> The following options shall be supported:

## Context

**Behavior:** The `kill` utility **shall** support the options defined immediately below this line in the same POSIX section:
- `-l`
- `-s signal_name`
- [XSI] `-signal_name`
- [XSI] `-signal_number`

**Conditions:** The `-s` and `-l` options are base POSIX. The `-signal_number` and `-signal_name` forms are XSI extensions.

**Shell relevance:** The `kill` builtin must implement:
1. `kill -l` → list all signal names (one per line or space-separated)
2. `kill -l exit_status` → map exit status to signal name (e.g., `kill -l 137` → `KILL`)
3. `kill -s SIGNAL pid...` → send named signal
4. `kill -SIGNAL pid...` → XSI shorthand
5. `kill pid...` → send SIGTERM (default)

**Spec reference:** POSIX.1-2024, `kill` OPTIONS (tag_20_64_04), `utilities/kill.html` line 65.

GPT-5.4 review: The extracted quote is faithful to `docs/posix/utilities/kill.html#tag_20_64_04`. This row is correctly treated as a framing statement rather than an independently testable behavior. The earlier enrichment overreached in one place by saying the `SIG` prefix "should also be accepted"; the cited POSIX text here does not say that. The clean review position is that the details belong to the subordinate option rows already reviewed.

## Implementation Notes

GPT-5.4 review: no separate `tests/spec` script is needed for this framing row. The executable coverage belongs in the specific option rows `SHALL-20-64-04-004` through `SHALL-20-64-04-010`, which have already been reviewed where applicable.
