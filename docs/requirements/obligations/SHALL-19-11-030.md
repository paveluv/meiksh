---
id: SHALL-19-11-030
category: shall
section: "2.11 Job Control"
anchor: tag_19_11
source_file: utilities/V3_chap02.html
source_lines: [2843, 2843]
keywords: ["shall"]
tier: 1
status: enriched
spec_tested: blocked
testing_blockers: "Requires deferred notification timing verification for stopped background jobs; needs interactive pty"
refs: ["#set"]
related_sections: []
tests: []
---

## Extracted Text

> When a process associated with a background job is stopped by a SIGSTOP, SIGTSTP, SIGTTIN, or SIGTTOU signal, the shell shall convert the (non-suspended) background job into a suspended job and an interactive shell shall write a message to standard error, formatted as described by the jobs utility (without the -l option) for a suspended job, at the following time:: If set -b is disabled, the message shall be written immediately prior to writing the next prompt for input.

## Context

**Background job stopped notification — `set -b` disabled (parent obligation):** When `set -b` (notify) is disabled (the default) and a background job is stopped by a signal, the suspended-job message shall be written **immediately prior to writing the next prompt for input**. This means the message appears just before the shell's `PS1` prompt, which is the standard deferred notification behavior.

The implementation must queue the notification and display it in the prompt loop, before the prompt string is output.

## Implementation Notes

*(to be filled during implementation)*
