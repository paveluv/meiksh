---
id: SHALL-20-14-03-005
category: shall
section: "DESCRIPTION"
anchor: tag_20_14_03
source_file: utilities/cd.html
source_lines: [104, 110]
keywords: ["unspecified", "shall", "may"]
tier: 2
status: enriched
refs: []
related_sections: []
tests: []
---

## Extracted Text

> If curpath is longer than {PATH_MAX} bytes (including the terminating null) and the directory operand was not longer than {PATH_MAX} bytes (including the terminating null), then curpath shall be converted from an absolute pathname to an equivalent relative pathname if possible. This conversion shall always be considered possible if the value of PWD , with a trailing <slash> added if it does not already have one, is an initial substring of curpath. Whether or not it is considered possible under other circumstances is unspecified. Implementations may also apply this conversion if curpath is not longer than {PATH_MAX} bytes or the directory operand was longer than {PATH_MAX} bytes.

## Context

If `curpath` exceeds `{PATH_MAX}` bytes (including null terminator) but the original directory operand did not, `curpath` **shall** be converted from an absolute to a relative pathname if possible. This conversion is always considered possible when `PWD` (with trailing `/` if needed) is an initial substring of `curpath` — in which case the relative path is obtained by removing the `PWD` prefix. Whether conversion is possible under other circumstances is **unspecified**. The implementation **may** also apply this conversion even when `curpath` does not exceed `{PATH_MAX}` or when the operand was longer. This prevents `chdir()` failures due to long absolute paths. Reference: cd utility DESCRIPTION step 9.

## Implementation Notes

*(to be filled during implementation)*
