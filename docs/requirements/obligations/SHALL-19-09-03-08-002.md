---
id: SHALL-19-09-03-08-002
category: shall
section: "2.9.3.4 OR Lists"
anchor: tag_19_09_03_08
source_file: utilities/V3_chap02.html
source_lines: [2206, 2206]
keywords: ["shall"]
tier: 1
status: enriched
refs: []
related_sections: []
tests: []
spec_tested: true
---

## Extracted Text

> First, command1 shall be executed. If its exit status is non-zero, command2 shall be executed, and so on, until a command has a zero exit status or there are no more commands left to execute.

## Context

**Behavior:** In an OR list (`command1 || command2 || ...`), the shell must execute commands sequentially from left to right. After executing `command1`:
- If its exit status is **non-zero** (failure), execute `command2`.
- If its exit status is **zero** (success), skip all remaining commands in the OR list.
- Continue this pattern for each subsequent `||`-joined command until one succeeds or all have executed.

**Lazy evaluation:** Commands to the right of a succeeding command are never expanded or executed. Word expansions, redirections, and side effects of skipped commands must not occur.

**Exit status:** The exit status of the OR list is the exit status of the last command that was actually executed.

**Interaction with `-e`:** When `set -e` is active, OR list commands other than the last are executed with `-e` ignored (the failure of a non-last command does not cause the shell to exit).

**Applies to:** All shell modes.

**Spec reference:** Section 2.9.3.4 OR Lists in `utilities/V3_chap02.html`, line 2206.

## Implementation Notes

*(to be filled during implementation)*
