---
id: SHALL-19-16-03-004
category: shall
section: "DESCRIPTION"
anchor: tag_19_16_03
source_file: utilities/V3_chap02.html
source_lines: [3142, 3143]
keywords: ["shall"]
tier: 1
status: enriched
refs: []
related_sections: []
tests: []
---

## Extracted Text

> A loop shall enclose a break or continue command if the loop lexically encloses the command. A loop lexically encloses a break or continue command if the command is:: Contained in a compound-list associated with the loop (either in the compound-list of the loop's do-group or, if the loop is a while or until loop, in the compound-list following the while or until reserved word), and

## Context

**Behavior:** Second condition for lexical enclosure: the `break`/`continue` command must be contained in a compound-list associated with the loop. This includes:
- The compound-list of the loop's do-group (the body), OR
- For `while`/`until` loops, the compound-list following the `while`/`until` reserved word (the condition).

This means `break` can appear in either the condition or body of a `while`/`until` loop and is considered enclosed.

**Applies to:** All shell modes.

**Spec reference:** `break` DESCRIPTION in `utilities/V3_chap02.html`, lines 3142-3143.

## Implementation Notes

*(to be filled during implementation)*
