---
id: SHALL-20-64-04-012
category: shall
section: "OPTIONS"
anchor: tag_20_64_04
source_file: utilities/kill.html
source_lines: [78, 81]
keywords: ["shall"]
tier: 3
spec_tested: blocked
testing_blockers: "Duplicate of SHALL-20-64-04-006; same -s signal_name description from same source lines"
status: enriched
refs: ["../basedefs/signal.h.html"]
related_sections: []
tests:
  - ../../../tests/spec/SHALL-20-64-04-006.sh
reviewed: GPT-5.4
---

## Extracted Text

> Specify the signal to send, using one of the symbolic names defined in the <signal.h> header. Values of signal_name shall be recognized in a case-independent fashion, without the SIG prefix. In addition, the symbolic name 0 shall be recognized, representing the signal value zero. The corresponding signal shall be sent instead of SIGTERM.

## Context

**Behavior:** Duplicate extraction of the `-s signal_name` option description (same content as SHALL-20-64-04-006). Requirements:
1. Signal names from `<signal.h>` are recognized **case-insensitively** and **without** the `SIG` prefix.
2. The symbolic name `0` must be recognized to represent signal value zero (used for process existence testing via `kill(pid, 0)`).
3. The specified signal replaces SIGTERM as the signal sent to the target process(es).

**Spec reference:** Section OPTIONS (tag_20_64_04) in `utilities/kill.html`, lines 78–81.

GPT-5.4 review: The extracted quote matches the same POSIX source text already reviewed under `SHALL-20-64-04-006`, and the duplicate classification is correct. The enrichment is accurate. The key normative points remain recognition of `<signal.h>` names, case-independent matching, no required `SIG` prefix, and acceptance of the symbolic name `0` as signal value zero.

## Implementation Notes

GPT-5.4 review: no separate `tests/spec/SHALL-20-64-04-012.sh` exists. The corresponding behavior has already been reviewed and exercised in the canonical duplicate row `SHALL-20-64-04-006` via `tests/spec/SHALL-20-64-04-006.sh`.
