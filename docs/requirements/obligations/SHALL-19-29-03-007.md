---
id: SHALL-19-29-03-007
category: shall
section: "DESCRIPTION"
anchor: tag_19_29_03
source_file: utilities/V3_chap02.html
source_lines: [5490, 5494]
keywords: ["shall", "may"]
tier: 1
spec_tested: true
status: enriched
refs: ["#trap", "#trap"]
related_sections: []
tests: []
---

## Extracted Text

> When a subshell is entered, traps that are not being ignored shall be set to the default actions, except in the case of a command substitution containing only a single trap command, when the traps need not be altered. Implementations may check for this case using only lexical analysis; for example, if `trap` and $( trap -- ) do not alter the traps in the subshell, cases such as assigning var=trap and then using $($var) may still alter them. This does not imply that the trap command cannot be used within the subshell to set new traps.

## Context

**Behavior:** Subshell trap handling:
1. When a subshell is entered, traps that are **not being ignored** must be reset to their **default** actions.
2. **Exception:** In a command substitution containing only a single `trap` command, the traps need not be altered (to allow `$(trap)` to report the parent's traps).
3. Implementations may check for this exception using only **lexical analysis** (e.g., `trap` and `$( trap -- )` qualify, but `var=trap; $($var)` may not).
4. Setting new traps within a subshell is still allowed.

**Applies to:** All shell modes.

**Spec reference:** `trap` DESCRIPTION in `utilities/V3_chap02.html`, lines 5490-5494.

## Implementation Notes

*(to be filled during implementation)*
