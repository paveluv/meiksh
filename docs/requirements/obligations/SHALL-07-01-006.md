---
id: SHALL-07-01-006
category: shall
section: "7.1 General"
anchor: tag_07_01
source_file: basedefs/V1_chap07.html
source_lines: [69, 77]
keywords: ["implementation-defined", "shall"]
tier: 2
status: enriched
refs: ["../utilities/localedef.html", "../utilities/localedef.html", "#tag_07_03", "../utilities/localedef.html", "#tag_07_02"]
related_sections: []
tests: []
---

## Extracted Text

> Locales other than those supplied by the implementation can be created via the localedef utility, provided that the _POSIX2_LOCALEDEF symbol is defined on the system. Even if localedef is not provided, all implementations conforming to the System Interfaces volume of POSIX.1-2024 shall provide one or more locales that behave as described in this chapter. The value that is used to specify a locale when using environment variables shall be the string specified as the name operand to the localedef utility when the locale was created. The strings "C" and "POSIX" are reserved as identifiers for the POSIX locale (see 7.2 POSIX Locale). When the value of a locale environment variable begins with a <slash> ('/'), it shall be interpreted as the pathname of the locale definition; the type of file (regular, directory, and so on) used to store the locale definition is implementation-defined. If the value does not begin with a <slash>, the mechanism used to locate the locale is implementation-defined.

## Context

**Behavior:** Multiple locale-related requirements:
1. The system must provide at least one locale conforming to Chapter 7.
2. Locale names in environment variables must match the names given to `localedef`.
3. `"C"` and `"POSIX"` are reserved names for the POSIX locale.
4. If a locale environment variable value starts with `/`, it is a pathname to the locale definition.
5. If it doesn't start with `/`, the locale lookup mechanism is implementation-defined.

**Shell relevance:** The shell must:
- Recognize `LANG`, `LC_ALL`, and individual `LC_*` variables as locale selectors.
- Pass locale environment variables to child processes.
- Update its own locale when these variables are assigned (via `setlocale()`).
- The `"C"` and `"POSIX"` locale names must always be valid values for locale variables.

**Cross-references:** `#tag_07_02` (POSIX Locale) and `#tag_07_03` (Locale Definition) are within `basedefs/V1_chap07.html`. The `localedef.html` refs are for an external utility — not shell-relevant for extraction.

**Spec reference:** Section 7.1, lines 69-77 in `basedefs/V1_chap07.html`.

## Implementation Notes

*(to be filled during implementation)*
