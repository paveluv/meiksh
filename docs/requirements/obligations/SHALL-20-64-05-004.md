---
id: SHALL-20-64-05-004
category: shall
section: "OPERANDS"
anchor: tag_20_64_05
source_file: utilities/kill.html
source_lines: [147, 147]
keywords: ["shall"]
tier: 3
spec_tested: blocked
testing_blockers: "Framing statement naming the exit_status operand; detailed semantics tested via SHALL-20-64-05-005"
status: enriched
refs: []
related_sections: []
tests:
  - ../../../tests/spec/SHALL-20-64-05-005.sh
reviewed: GPT-5.4
---

## Extracted Text

> exit_status

## Context

**Behavior:** The `kill` built-in must accept the `exit_status` operand when the `-l` option is specified. This operand is a decimal integer representing either a signal number or the exit status of a process terminated by a signal. See SHALL-20-64-05-005 for the detailed semantics.

**Conditions:** The detailed meaning of `exit_status` is given by the following subordinate row and by the `-l` option text in the same POSIX utility description.

**Spec reference:** Section OPERANDS (tag_20_64_05) in `utilities/kill.html`, line 147.

GPT-5.4 review: The prior extracted text incorrectly merged the operand-list heading with the operand label. The actual POSIX text at `docs/posix/utilities/kill.html#tag_20_64_05` is simply the operand label `exit_status`, with the substantive meaning supplied by the following row already reviewed under `SHALL-20-64-05-005`. This row should therefore remain a framing/value-label entry.

## Implementation Notes

GPT-5.4 review: no separate `tests/spec/SHALL-20-64-05-004.sh` exists, and none is needed. The relevant executable behavior is already covered by the reviewed canonical row `SHALL-20-64-05-005` and `tests/spec/SHALL-20-64-05-005.sh`.
