---
id: SHALL-20-64-14-005
category: shall
section: "EXIT STATUS"
anchor: tag_20_64_14
source_file: utilities/kill.html
source_lines: [222, 222]
keywords: ["shall"]
tier: 3
spec_tested: true
status: enriched
refs: []
related_sections: []
tests: []
reviewed: GPT-5.4
---

## Extracted Text

> An error occurred.

## Context

**Behavior:** Exit status >0 is returned when an error occurred. Examples include:
- Invalid signal name or number
- No matching process found for a pid operand
- Permission denied when sending the signal
- Invalid pid operand syntax
- Write failure during `-l` output
- Any other error condition

The specific value >0 is not further defined by the spec. Many implementations use `1` for general errors, but this is informative rather than part of the quoted POSIX requirement.

**Spec reference:** Section EXIT STATUS (tag_20_64_14) in `utilities/kill.html`, line 222.

GPT-5.4 review: The earlier extracted text again merged the EXIT STATUS heading with the exit-value description. The exact quoted requirement in `docs/posix/utilities/kill.html#tag_20_64_14` is simply "An error occurred." The examples in the enrichment are useful, but they should remain clearly non-exhaustive and non-normative unless tied to other specific POSIX sections.

## Implementation Notes

GPT-5.4 review of `tests/spec/SHALL-20-64-14-005.sh`: the script remains intentionally narrow and verifies one concrete error path that must produce a non-zero exit status, which is sufficient for this specific EXIT STATUS obligation.
