---
id: SHALL-20-53-05-008
category: shall
section: "OPERANDS"
anchor: tag_20_53_05
source_file: utilities/getopts.html
source_lines: [121, 124]
keywords: ["shall"]
tier: 2
status: enriched
refs: []
related_sections: []
tests: []
spec_tested: true
---

## Extracted Text

> By default, the list of parameters parsed by the getopts utility shall be the positional parameters currently set in the invoking shell environment ("$@"). If param operands are given, they shall be parsed instead of the positional parameters. Note that the next element of the parameter list need not exist; in this case, OPTIND will be set to $#+1 or the number of param operands plus 1.

## Context

**Behavior:** The parameter list that `getopts` parses depends on whether `param` operands are provided:

1. **No `param` operands (default):** `getopts` parses the positional parameters (`"$@"`) of the invoking shell or function.
2. **With `param` operands:** `getopts optstring name param1 param2 ...` — the `param` arguments are parsed instead of positional parameters.

**OPTIND at end:** When options are exhausted, `OPTIND` is set to the index of the first non-option argument, or `$# + 1` (for positional parameters) / number of `param` operands + 1 (for explicit params) if there are no remaining operands.

**Synopsis:** `getopts optstring name [param...]`

**Spec reference:** POSIX.1-2024, `getopts` OPERANDS, lines 121-124 of `utilities/getopts.html`.

## Implementation Notes

*(to be filled during implementation)*
