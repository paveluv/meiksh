---
id: SHALL-09-04-03-007
category: shall
section: "9.4.3 ERE Special Characters"
anchor: tag_09_04_03
source_file: basedefs/V1_chap09.html
source_lines: [515, 521]
keywords: ["shall"]
tier: 2
status: enriched
refs: ["#tag_09_03_05", "#tag_09_04_06"]
related_sections: []
tests: []
---

## Extracted Text

> An ERE special character has special properties in certain contexts. Outside those contexts, or when preceded by an unescaped <backslash>, such a character shall be an ERE that matches the special character itself. The extended regular expression special characters and the contexts in which they shall have their special meaning are as follows:: The <asterisk>, <plus-sign>, <question-mark>, and <left-brace> shall be special except when used in a bracket expression (see 9.3.5 RE Bracket Expression). Any of the following uses produce undefined results: If these characters appear first in an ERE, or immediately following an unescaped <vertical-line>, <circumflex>, <dollar-sign>, or <left-parenthesis> If a <left-brace> is not part of a valid interval expression (see 9.4.6 EREs Matching Multiple Characters)

## Context

The duplication operators `*`, `+`, `?`, and `{` must be treated as special outside bracket expressions and as literal inside bracket expressions. The following conditions produce undefined results (the implementation may reject or accept them arbitrarily): (1) Any of `*`, `+`, `?`, `{` appearing as the first token in an ERE (e.g., `+abc`). (2) Any of them appearing immediately after an unescaped `|`, `^`, `$`, or `(` (e.g., `(+)`, `|*`, `^?`). (3) A `{` that does not begin a valid interval expression `{m}`, `{m,}`, or `{m,n}`. The ERE engine should detect these cases during parsing and may report an error or silently accept them. A robust implementation should report a syntax error.

## Implementation Notes

*(to be filled during implementation)*
