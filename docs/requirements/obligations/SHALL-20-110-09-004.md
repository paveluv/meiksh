---
id: SHALL-20-110-09-004
category: shall
section: "ASYNCHRONOUS EVENTS"
anchor: tag_20_110_09
source_file: utilities/sh.html
source_lines: [271, 272]
keywords: ["shall"]
tier: 1
status: enriched
refs: []
related_sections: []
tests: []
spec_tested: blocked
testing_blockers: "requires interactive shell with -m (job control) and terminal; cannot test in batch mode"
reviewed: GPT-5.4
---

## Extracted Text

> If the -m option is in effect, SIGTTIN, SIGTTOU, and SIGTSTP signals shall be ignored.

## Context

**Behavior:** When the shell is **interactive** and the `-m` (monitor/job control) option is **enabled**, SIGTTIN, SIGTTOU, and SIGTSTP signals must be **ignored** (set to SIG_IGN). This is necessary for the shell to manage job control properly — the shell itself should not be stopped by terminal signals while managing foreground/background jobs.

**Implementation:** This row constrains the interactive shell's signal disposition when `-m` is in effect.

**Applies to:** Interactive shell with `-m` option enabled.

**Spec reference:** ASYNCHRONOUS EVENTS section in `utilities/sh.html`, lines 271-272.

GPT-5.4 review: The prior extracted text incorrectly merged the interactive-shell lead-in with the `-m` bullet. The corrected quote now matches `docs/posix/issue8/sh-utility.html#tag_20_110_09`. Keeping the row blocked is appropriate because authoritative coverage needs a real interactive job-control session on a terminal.

## Implementation Notes

GPT-5.4 review: the duplicate row `SHALL-20-110-09-007` should point back to this canonical behavior.
