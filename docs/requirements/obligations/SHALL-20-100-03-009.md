---
id: SHALL-20-100-03-009
category: shall
section: "DESCRIPTION"
anchor: tag_20_100_03
source_file: utilities/read.html
source_lines: [61, 65]
keywords: ["shall"]
tier: 2
status: enriched
refs: ["../utilities/V3_chap02.html#tag_19_06_05"]
related_sections: []
tests: []
---

## Extracted Text

> The loop over the contents of that initial field shall cease when either the input is empty or n output fields have been generated, where n is one less than the number of var operands passed to the read utility. Any remaining input in the original field being processed shall be returned to the read utility "unsplit"; that is, unmodified except that any leading or trailing IFS white space, as defined in 2.6.5 Field Splitting, shall be removed.

## Context

**Behavior required:** Same text as SHALL-20-100-03-007, extracted as a standalone obligation. The field splitting loop terminates when either:
- Input is exhausted, OR
- `(num_vars - 1)` output fields have been produced

Any remaining unsplit input has its leading/trailing IFS whitespace stripped and is returned as-is to `read` for assignment to the last variable.

**IFS whitespace definition (per 2.6.5):** IFS whitespace characters are `<space>`, `<tab>`, and `<newline>` that appear in the IFS value. Non-whitespace IFS characters (e.g., `:`) are NOT stripped from the remainder's leading/trailing positions.

**Example:** With `IFS=': '` and `read a b` on input `" : x : y : z "`:
- Field 1: `""` (empty, from leading delimiter) or `"x"` depending on whitespace handling → assigned to `a`
- Remainder with leading/trailing IFS whitespace stripped → assigned to `b`

**Spec reference:** Section DESCRIPTION of `utilities/read.html`, lines 61-65; Section 2.6.5.

## Implementation Notes

*(to be filled during implementation)*
