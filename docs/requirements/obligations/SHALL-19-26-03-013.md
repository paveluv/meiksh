---
id: SHALL-19-26-03-013
category: shall
section: "DESCRIPTION"
anchor: tag_19_26_03
source_file: utilities/V3_chap02.html
source_lines: [4848, 4892]
keywords: ["shall", "may"]
tier: 1
spec_tested: true
status: enriched
refs: ["#exit", "javascript:open_code('OB')", "../utilities/sh.html#tag_20_110_13_01", "#tag_19_09_02", "../utilities/vi.html", "../utilities/vi.html", "../utilities/vi.html"]
related_sections: []
tests: []
---

## Extracted Text

> Set various options, many of which shall be equivalent to the single option letters. The following values of option shall be supported: allexport Equivalent to -a. errexit Equivalent to -e. ignoreeof Prevent an interactive shell from exiting on end-of-file. This setting prevents accidental logouts when <control>-D is entered. A user shall explicitly exit to leave the interactive shell. This option shall be supported if the system supports the User Portability Utilities option. monitor Equivalent to -m. This option shall be supported if the system supports the User Portability Utilities option. noclobber Equivalent to -C (uppercase C). noglob Equivalent to -f. noexec Equivalent to -n. nolog [OB] Prevent the entry of function definitions into the command history; see Command History List. This option may have no effect; it is kept for compatibility with previous versions of the standard. This option shall be supported if the system supports the User Portability Utilities option. notify Equivalent to -b. nounset Equivalent to -u. pipefail Derive the exit status of a pipeline from the exit statuses of all of the commands in the pipeline, not just the last (rightmost) command, as described in 2.9.2 Pipelines. verbose Equivalent to -v. vi Allow shell command line editing using the built-in vi editor. Enabling vi mode shall disable any other command line editing mode provided as an implementation extension. This option shall be supported if the system supports the User Portability Utilities option. It need not be possible to set vi mode on for certain block-mode terminals. xtrace Equivalent to -x.

## Context

**Behavior:** The `set -o option` form sets various named options, many equivalent to single-letter flags. Required options:

- **allexport** — Equivalent to `-a`.
- **errexit** — Equivalent to `-e`.
- **ignoreeof** — Prevent interactive shell from exiting on EOF (Ctrl-D). User must explicitly type `exit`. Required if User Portability Utilities supported.
- **monitor** — Equivalent to `-m`. Required if User Portability Utilities supported.
- **noclobber** — Equivalent to `-C`.
- **noglob** — Equivalent to `-f`.
- **noexec** — Equivalent to `-n`.
- **nolog** — [OB] Prevent function definitions from entering command history. May have no effect. Required if User Portability Utilities supported.
- **notify** — Equivalent to `-b`.
- **nounset** — Equivalent to `-u`.
- **pipefail** — Derive pipeline exit status from all commands in the pipeline, not just the last. See 2.9.2 Pipelines.
- **verbose** — Equivalent to `-v`.
- **vi** — Enable vi command line editing. Disables any other editing mode. Required if User Portability Utilities supported.
- **xtrace** — Equivalent to `-x`.

**Applies to:** All shell modes.

**Spec reference:** `set` DESCRIPTION in `utilities/V3_chap02.html`, lines 4848-4892. Cross-ref: 2.9.2 Pipelines, `utilities/sh.html` Command History List, `utilities/vi.html`.

## Implementation Notes

*(to be filled during implementation)*
