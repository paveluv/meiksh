---
id: SHALL-20-110-13-03-004
category: shall
section: "Command Line Editing (vi-mode)"
anchor: tag_20_110_13_03
source_file: utilities/sh.html
source_lines: [344, 344]
keywords: ["shall"]
tier: 1
status: enriched
refs: []
related_sections: []
tests: []
spec_tested: blocked
testing_blockers: "definitional text for vi-mode terminology; no independently testable behavior"
reviewed: GPT-5.4
---

## Extracted Text

> In the following sections, the phrase "move the cursor to the beginning of the word" shall mean "move the cursor to the first character of the current word" and the phrase "move the cursor to the end of the word" shall mean "move the cursor to the last character of the current word". The phrase "beginning of the command line" indicates the point between the end of the prompt string issued by the shell (or the beginning of the terminal line, if there is no prompt string) and the first character of the command text.

## Context

**Behavior:** Definitions for vi-mode editing terminology:

1. **"Move cursor to beginning of word"** means move the cursor to the **first character** of the current word.
2. **"Move cursor to end of word"** means move the cursor to the **last character** of the current word.
3. **"Beginning of the command line"** means the point between the end of the prompt string (or the beginning of the terminal line if there is no prompt) and the first character of the command text.

**Implementation:** These definitions affect the behavior of word-motion commands (`w`, `b`, `e`, `W`, `B`, `E`) and line-motion commands (`0`, `^`). The "beginning of command line" definition is important for the `0` command — it must not move into the prompt area.

**Applies to:** Interactive shell in vi editing mode.

**Spec reference:** Command Line Editing (vi-mode) section in `utilities/sh.html`, line 344.

## Implementation Notes

GPT-5.4 review: This quote matches `docs/posix/issue8/sh-utility.html#tag_20_110_13_03`. It is definitional text that constrains later vi-mode motion commands, but it is not a good standalone batch-script target; its effect should instead be reflected when the concrete word-motion and line-motion rows are reviewed.
