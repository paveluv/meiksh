---
id: SHALL-19-24-03-001
category: shall
section: "DESCRIPTION"
anchor: tag_19_24_03
source_file: utilities/V3_chap02.html
source_lines: [4429, 4436]
keywords: ["shall", "may"]
tier: 1
status: enriched
refs: ["#readonly", "#readonly", "#export", "../utilities/getopts.html", "#readonly", "../utilities/read.html", "#unset", "../basedefs/V1_chap08.html#tag_08_01"]
related_sections: []
tests: []
---

## Extracted Text

> The variables whose names are specified shall be given the readonly attribute. The values of variables with the readonly attribute cannot be changed by subsequent assignment or use of the export, getopts, readonly, or read utilities, nor can those variables be unset by the unset utility. As described in XBD 8.1 Environment Variable Definition, conforming applications shall not request to mark a variable as readonly if it is documented as being manipulated by a shell built-in utility, as it may render those utilities unable to complete successfully. If the name of a variable is followed by =word, then the value of that variable shall be set to word.

## Context

**Behavior:** The `readonly` special built-in gives the **readonly attribute** to specified variables. Once set, a readonly variable:
1. Cannot be changed by subsequent assignment.
2. Cannot be changed by `export`, `getopts`, `readonly`, or `read` utilities.
3. Cannot be unset by the `unset` utility.
4. If `name=word` is used, the value is set simultaneously with the readonly attribute.
5. Conforming applications should not mark variables that are documented as manipulated by shell built-ins as readonly (e.g., `PATH`, `IFS`), as this may prevent built-ins from working.

**Applies to:** All shell modes. This is a special built-in.

**Spec reference:** `readonly` DESCRIPTION in `utilities/V3_chap02.html`, lines 4429-4436. Cross-ref: `basedefs/V1_chap08.html#tag_08_01`.

## Implementation Notes

*(to be filled during implementation)*
