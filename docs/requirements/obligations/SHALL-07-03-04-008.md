---
id: SHALL-07-03-04-008
category: shall
section: "7.3.4 LC_NUMERIC"
anchor: tag_07_03_04
source_file: basedefs/V1_chap07.html
source_lines: [3347, 3348]
keywords: ["unspecified", "shall"]
tier: 2
status: enriched
refs: []
related_sections: []
tests: []
---

## Extracted Text

> In a locale definition file, the operands are strings, formatted as indicated by the grammar in 7.4 Locale Definition Grammar. For some keywords, the strings can only contain integers. Keywords that are not provided, or integer keywords set to -1, can be used to indicate that the value is not available in the locale. String values set to the empty string ("") can be used to indicate that the value is available and is an empty string, or that the value is not available. The following keywords shall be recognized:: The operand is a string containing the symbol that shall be used as a separator for groups of digits to the left of the decimal delimiter in numeric, non-monetary formatted monetary quantities. In contexts where standards limit the thousands_sep to a single byte, the result of specifying a multi-byte operand shall be unspecified.

## Context

The `thousands_sep` value is the string used to separate digit groups in non-monetary numeric formatting. If a multi-byte character is specified, behavior is unspecified in contexts requiring a single-byte separator. The shell is affected: the `printf` builtin's `%'d` and `%'f` flags use this separator for digit grouping. In the POSIX locale, `thousands_sep` is unavailable (no grouping). The shell must handle both single-byte and potentially multi-byte separators when formatting with the grouping flag.

See: `basedefs/V1_chap07.html` §7.3.4 LC_NUMERIC.

## Implementation Notes

*(to be filled during implementation)*
