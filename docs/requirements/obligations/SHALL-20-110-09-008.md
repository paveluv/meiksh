---
id: SHALL-20-110-09-008
category: shall
section: "ASYNCHRONOUS EVENTS"
anchor: tag_20_110_09
source_file: utilities/sh.html
source_lines: [274, 275]
keywords: ["unspecified", "shall"]
tier: 1
status: enriched
refs: []
related_sections: []
tests: []
spec_tested: blocked
testing_blockers: "duplicate of SHALL-20-110-09-005; canonical behavior itself remains blocked because it is interactive and partly unspecified when -m is not in effect"
reviewed: GPT-5.4
---

## Extracted Text

> If the -m option is not in effect, it is unspecified whether SIGTTIN, SIGTTOU, and SIGTSTP signals are ignored, set to the default action, or caught. If they are caught, the shell shall, in the signal-catching function, set the signal to the default action and raise the signal (after taking any appropriate steps, such as restoring terminal settings).

## Context

**Behavior:** This is a duplicate/complementary extraction of the same spec text as SHALL-20-110-09-005. When `-m` is not in effect and the shell is interactive, handling of SIGTTIN/SIGTTOU/SIGTSTP is unspecified; if caught, the handler must reset to default and re-raise. See SHALL-20-110-09-005 for full context.

**Applies to:** Interactive shell without `-m` option.

**Spec reference:** ASYNCHRONOUS EVENTS section in `utilities/sh.html`, lines 274-275.

GPT-5.4 review: This quote correctly matches the same POSIX source text as `SHALL-20-110-09-005`, making this a duplicate extraction rather than a separate behavior row. The canonical row is still blocked because the behavior is partly unspecified and otherwise requires a real interactive terminal context.

## Implementation Notes

GPT-5.4 review: no separate canonical `tests/spec` script is needed for this duplicate row.
