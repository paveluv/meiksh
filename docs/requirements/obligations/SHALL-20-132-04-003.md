---
id: SHALL-20-132-04-003
category: shall
section: "OPTIONS"
anchor: tag_20_132_04
source_file: utilities/umask.html
source_lines: [57, 57]
keywords: ["shall"]
tier: 2
status: enriched
spec_tested: true
refs: []
related_sections: []
tests:
  - ../../../tests/spec/SHALL-20-132-04-003.sh
reviewed: GPT-5.4
---

## Extracted Text

> -S

## Context

**Behavior:** The `umask` utility shall support the `-S` option. When `-S` is specified, `umask` produces symbolic output instead of the default output style.

**Conditions:** `-S` is meaningful only when `umask` is invoked in query mode (no mask operand). When a mask operand is given, `-S` has no visible effect since no output is produced.

**Spec reference:** Section OPTIONS (tag_20_132_04) in `utilities/umask.html`, line 57.

GPT-5.4 review: The prior extracted text incorrectly merged the option-list heading with the option label. The actual POSIX text at `docs/posix/utilities/umask.html#tag_20_132_04` is simply `-S`, with the substantive meaning provided in the following row. This row is therefore best treated as the supported-option label entry.

## Implementation Notes

GPT-5.4 review of `tests/spec/SHALL-20-132-04-003.sh`: the script appropriately checks that `umask -S` is accepted and produces output, which is the key observable consequence for this option-support row.
