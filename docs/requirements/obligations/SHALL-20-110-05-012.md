---
id: SHALL-20-110-05-012
category: shall
section: "OPERANDS"
anchor: tag_20_110_05
source_file: utilities/sh.html
source_lines: [96, 96]
keywords: ["shall"]
tier: 1
status: enriched
refs: []
related_sections: []
tests:
  - ../../../tests/spec/SHALL-20-110-05-003.sh
spec_tested: blocked
testing_blockers: "Duplicate of SHALL-20-110-05-003; tested there"
reviewed: GPT-5.4
---

## Extracted Text

> A single <hyphen-minus> shall be treated as the first operand and then ignored. If both '-' and "--" are given as arguments, or if other operands precede the single <hyphen-minus>, the results are undefined.

## Context

Duplicate of SHALL-20-110-05-003 (same source line 96). A single `-` (hyphen-minus) operand must be treated as the first operand and then ignored. If both `-` and `--` are given as arguments, or if other operands precede the single `-`, the results are undefined.

See SHALL-20-110-05-003 for the full context. See utilities/sh.html#tag_20_110_05.

GPT-5.4 review: This quote correctly matches the same POSIX text as `SHALL-20-110-05-003`, making this a duplicate extraction rather than a separate behavior row. The canonical executable coverage belongs to `tests/spec/SHALL-20-110-05-003.sh`.

## Implementation Notes

GPT-5.4 review: no separate canonical `tests/spec` script is needed for this duplicate row.
