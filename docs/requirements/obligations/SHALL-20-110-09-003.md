---
id: SHALL-20-110-09-003
category: shall
section: "ASYNCHRONOUS EVENTS"
anchor: tag_20_110_09
source_file: utilities/sh.html
source_lines: [268, 269]
keywords: ["shall"]
tier: 1
status: enriched
refs: []
related_sections: []
tests:
  - ../../../tests/spec/SHALL-20-110-09-003.sh
spec_tested: true
reviewed: GPT-5.4
---

## Extracted Text

> SIGQUIT and SIGTERM signals shall be ignored.

## Context

**Behavior:** When the shell is **interactive**, SIGQUIT and SIGTERM signals must be **ignored** (set to SIG_IGN). This prevents the interactive shell from being accidentally killed by these signals — users must use `exit` or EOF to leave the shell.

**Implementation:** This row constrains the interactive shell's observable signal disposition for SIGQUIT and SIGTERM.

**Applies to:** Interactive shell only.

**Spec reference:** ASYNCHRONOUS EVENTS section in `utilities/sh.html`, lines 268-269.

GPT-5.4 review: The prior extracted text incorrectly merged the introductory sentence with the first interactive-shell bullet. The corrected quote now matches `docs/posix/issue8/sh-utility.html#tag_20_110_09`. The enrichment is accurate once narrowed away from prescribing a particular implementation mechanism.

## Implementation Notes

GPT-5.4 review of `tests/spec/SHALL-20-110-09-003.sh`: the strengthened script now checks both signals named by the SHALL, not just `SIGTERM`.
