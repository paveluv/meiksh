---
id: SHALL-20-100-04-007
category: shall
section: "OPTIONS"
anchor: tag_20_100_04
source_file: utilities/read.html
source_lines: [112, 112]
keywords: ["unspecified", "shall"]
tier: 2
status: enriched
refs: []
related_sections: []
tests: []
---

## Extracted Text

> If delim consists of one single-byte character, that byte shall be used as the logical line delimiter. If delim is the null string, the logical line delimiter shall be the null byte. Otherwise, the behavior is unspecified.

## Context

**Behavior required:** Same text as SHALL-20-100-04-004, extracted as a standalone obligation. The `-d delim` argument defines the line delimiter: single byte → that byte; empty string → null byte; otherwise → unspecified.

**Condition:** When `-d delim` is specified.

**Implementation detail:** See SHALL-20-100-04-004 for full details. The implementation should validate `delim` and use its first (and only) byte as the delimiter, or use `\0` if empty. Multi-byte `delim` values can be handled at the implementation's discretion.

**Spec reference:** Section OPTIONS of `utilities/read.html`, line 112.

## Implementation Notes

*(to be filled during implementation)*
