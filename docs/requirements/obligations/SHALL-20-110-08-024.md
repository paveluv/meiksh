---
id: SHALL-20-110-08-024
category: shall
section: "ENVIRONMENT VARIABLES"
anchor: tag_20_110_08
source_file: utilities/sh.html
source_lines: [230, 230]
keywords: ["shall"]
tier: 1
status: enriched
refs: []
related_sections: []
tests: []
spec_tested: blocked
testing_blockers: "heading-only intro for MAILCHECK; behavior tested via SHALL-20-110-08-025"
reviewed: GPT-5.4
---

## Extracted Text

> MAILCHECK

## Context

**Behavior:** The MAILCHECK environment variable is one of the variables that shall affect the execution of `sh`. This is the variable name heading; the detailed semantics are specified in the subsequent description (see SHALL-20-110-08-025).

**Applies to:** Interactive shells (User Portability [UP] option).

**Details:** MAILCHECK specifies how often (in seconds) the shell checks for new mail in the files specified by MAILPATH or MAIL. Default is 600 seconds. See SHALL-20-110-08-025 for the full behavior specification.

**Spec reference:** Section "sh" ENVIRONMENT VARIABLES, line 230 in `utilities/sh.html`.

## Implementation Notes

GPT-5.4 review: The prior extracted text incorrectly merged the ENVIRONMENT VARIABLES heading with the `MAILCHECK` variable label. The corrected quote now matches `docs/posix/issue8/sh-utility.html#tag_20_110_08`. This is a label row whose substantive behavior is carried by the blocked primary row `SHALL-20-110-08-025`.
