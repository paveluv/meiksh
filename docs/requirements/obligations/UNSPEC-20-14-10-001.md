---
id: UNSPEC-20-14-10-001
category: unspecified
section: "STDOUT"
anchor: tag_20_14_10
source_file: utilities/cd.html
source_lines: [219, 220]
keywords: ["unspecified"]
tier: 2
status: enriched
refs: []
related_sections: []
tests: []
---

## Extracted Text

> If an absolute pathname of the new current working directory cannot be determined, it is unspecified whether nothing is written to the standard output or the value of curpath used in step 10, followed by a <newline>, is written to the standard output.

## Context

**Behavior (unspecified):** When cd would normally output the new directory (due to CDPATH or `-` operand), but the absolute pathname of the new working directory cannot be determined, the implementation may either:
1. Write nothing to stdout, or
2. Write the value of `curpath` as used in step 10 (which may be a relative path), followed by a newline.

**Conditions:** This arises when the `-P` option is in effect and the shell cannot determine the absolute physical pathname (e.g., due to insufficient permissions on parent directories).

**Implementation choice:** The implementation should pick one behavior and document it. Writing curpath is arguably more useful to users.

**Spec reference:** POSIX.1-2024, `utilities/cd.html`, section STDOUT (tag_20_14_10), lines 219-220.

## Implementation Notes

*(to be filled during implementation)*
