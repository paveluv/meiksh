---
id: SHALL-04-11-001
category: shall
section: "4.11 File System Cache"
anchor: tag_04_11
source_file: basedefs/V1_chap04.html
source_lines: [192, 202]
keywords: ["shall", "may"]
tier: 2
status: enriched
refs: ["#tag_04_04", "#tag_04_12"]
related_sections: []
tests: []
spec_tested: blocked
testing_blockers: "File system cache semantics are OS-level; cannot observe or test cache behavior from shell"
---

## Extracted Text

> If the file system is accessed via a memory cache, file-related requirements stated in the rest of this standard shall apply to the cache, except where explicitly stated otherwise: this includes directory atomicity and serializability requirements (see 4.4 Directory Operations), file times update requirements (see 4.12 File Times Update), and read-write serializability requirements (see write()). Cache entries shall be transferred to the underlying storage as the result of successful calls to fdatasync(), fsync(), or aio_fsync(), and may be transferred to storage automatically at other times. Such transfers shall be atomic, with minimum units being directory entries (for directory contents), aligned data blocks of the fundamental file system block size (for regular-file contents; see <sys/statvfs.h>), and all attributes of a single file (for file attributes).

## Context

This is an OS-level requirement about file system caching semantics. For the shell, this means that when the shell writes to files (via redirections, here-documents, or builtins like `echo` or `printf`), the standard file-related guarantees (atomicity of directory operations, file times updates) apply to the cache and are visible to subsequent shell operations without requiring explicit sync. The shell does not need to call fsync() or fdatasync() itself unless implementing specific data-integrity features. The shell can rely on the OS providing these caching guarantees transparently.

This obligation is primarily informational for the shell implementation — no special shell code is needed, as the OS handles caching semantics.

See: `basedefs/V1_chap04.html` §4.11 File System Cache.

## Implementation Notes

*(to be filled during implementation)*
