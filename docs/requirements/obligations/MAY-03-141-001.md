---
id: MAY-03-141-001
category: may
section: "3.141 File Descriptor"
anchor: tag_03_141
source_file: basedefs/V1_chap03.html
source_lines: [803, 810]
keywords: ["may"]
tier: 2
status: enriched
refs: ["../functions/sysconf.html#", "#tag_03_241"]
related_sections: []
tests: []
---

## Extracted Text

> A per-process unique, non-negative integer used to identify an open file for the purpose of file access. The values 0, 1, and 2 have special meaning and conventional uses, and are referred to as standard input, standard output, and standard error, respectively. Programs usually take their input from standard input, and write output on standard output. Diagnostic messages are usually written on standard error. The value of a newly-created file descriptor is from zero to {OPEN_MAX}-1. A file descriptor can have a value greater than or equal to {OPEN_MAX} if the value of {OPEN_MAX} has decreased (see sysconf()) since the file descriptor was opened. File descriptors may also be used to implement message catalog descriptors and directory streams; see also 3.241 Open File Description.

## Context

**Definition — direct shell relevance.** Defines "file descriptor" as a per-process non-negative integer identifying an open file. FDs 0, 1, 2 are standard input, standard output, and standard error. New FDs range from 0 to `{OPEN_MAX}-1` (but may exceed this if `OPEN_MAX` decreased since opening). FDs may also implement message catalogs and directory streams.

**Shell impact:** File descriptors are fundamental to shell operation:
- FDs 0, 1, 2 are stdin/stdout/stderr — used in all I/O redirections
- The shell's redirection operators (`<`, `>`, `>>`, `<&`, `>&`, `n>`, `n<`) manipulate file descriptors
- `exec n>file` opens FD n for the shell process
- `{varname}>file` assigns a new FD ≥10 to a variable (if supported)
- The shell must manage FD inheritance to child processes and close FDs marked with `FD_CLOEXEC`
- FDs ≥10 are conventionally available for user redirection

**Cross-ref note:** refs include `../functions/sysconf.html` (functions/ page — excluded) and `#tag_03_241` (Open File Description, same chapter — internal ref, no extraction needed).

**Spec reference:** basedefs/V1_chap03.html, section 3.141.

## Implementation Notes

*(to be filled during implementation)*
