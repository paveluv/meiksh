---
id: SHALL-09-04-03-009
category: shall
section: "9.4.3 ERE Special Characters"
anchor: tag_09_04_03
source_file: basedefs/V1_chap09.html
source_lines: [526, 526]
keywords: ["shall"]
tier: 2
status: enriched
refs: ["#tag_09_03_05"]
related_sections: []
tests: []
---

## Extracted Text

> An ERE special character has special properties in certain contexts. Outside those contexts, or when preceded by an unescaped <backslash>, such a character shall be an ERE that matches the special character itself. The extended regular expression special characters and the contexts in which they shall have their special meaning are as follows:: The <vertical-line> is special except when used in a bracket expression (see 9.3.5 RE Bracket Expression). A <vertical-line> appearing first or last in an ERE, or immediately following a <vertical-line> or a <left-parenthesis>, or immediately preceding a <right-parenthesis>, produces undefined results.

## Context

The `|` (alternation) operator is special outside bracket expressions. Inside bracket expressions, it matches a literal `|`. The following placements produce undefined results: (1) `|` as the first character of an ERE (e.g., `|abc`). (2) `|` as the last character (e.g., `abc|`). (3) `|` immediately after another `|` (e.g., `a||b`). (4) `|` immediately after `(` (e.g., `(|a)`). (5) `|` immediately before `)` (e.g., `(a|)`). The implementation may reject these as syntax errors or may handle them in an implementation-specific way; a robust implementation should diagnose them.

## Implementation Notes

*(to be filled during implementation)*
