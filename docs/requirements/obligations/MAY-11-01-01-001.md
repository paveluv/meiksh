---
id: MAY-11-01-01-001
category: may
section: "11.1.1 Opening a Terminal Device File"
anchor: tag_11_01_01
source_file: basedefs/V1_chap11.html
source_lines: [47, 53]
keywords: ["may"]
tier: 3
status: enriched
refs: ["#tag_11_02", "../utilities/stty.html"]
related_sections: []
tests: []
---

## Extracted Text

> Cases where applications do open a terminal device are as follows:: Opening a modem or similar piece of equipment connected by a serial line. In this case, the terminal parameters (see 11.2 Parameters that Can be Set) may be initialized to default settings by the implementation in between the last close of the device by any process and the next open of the device, or they may persist from one use to the next. The terminal parameters can be set to values that ensure the terminal behaves in a conforming manner by means of the O_TTY_INIT open flag when opening a terminal device that is not already open in any process, or by executing the stty utility with the operand sane.

## Context

**Behavior (optional):** When a serial terminal device is opened, the implementation may either reset terminal parameters to defaults between the last close and next open, or persist them. Applications can ensure conforming behavior via O_TTY_INIT flag or `stty sane`.

**Conditions:** Applies to serial line terminal device opens. This is a "may" — the implementation chooses the reset-vs-persist behavior.

**Shell relevance:** The shell itself does not typically open raw serial devices, but the `stty` builtin must support the `sane` operand to reset terminal parameters to conforming values. The shell should be aware that terminal parameters may or may not persist across open/close cycles.

**Spec reference:** XBD 11.1.1 "Opening a Terminal Device File" (tag_11_01_01), `basedefs/V1_chap11.html`, lines 47–53.

## Implementation Notes

*(to be filled during implementation)*
