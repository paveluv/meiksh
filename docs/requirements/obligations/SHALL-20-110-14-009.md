---
id: SHALL-20-110-14-009
category: shall
section: "EXIT STATUS"
anchor: tag_20_110_14
source_file: utilities/sh.html
source_lines: [690, 690]
keywords: ["shall"]
tier: 1
status: enriched
spec_tested: true
refs: []
related_sections: []
tests: []
reviewed: GPT-5.4
---

## Extracted Text

> The following exit values shall be returned:: A specified command_file could not be found by a non-interactive shell.

## Context

**Behavior:** The `sh` utility shall return exit status 127 when a specified command_file could not be found by a non-interactive shell.

**Applies to:** Non-interactive shell invocations with a command_file operand (e.g., `sh nonexistent.sh`).

**Details:** This covers the case where:
- `sh` is invoked with a command_file operand.
- The file does not exist at the given path.
- The shell is non-interactive.

The shell should write a diagnostic message to stderr before exiting with status 127 (e.g., "sh: nonexistent.sh: No such file or directory").

**Note:** This applies to the command_file operand of `sh` itself, not to commands within scripts. Command-not-found within scripts is handled by 2.9.1.4 Command Search and Execution.

**Spec reference:** Section "sh" EXIT STATUS, line 690 in `utilities/sh.html`.

GPT-5.4 review: The extracted quote matches `docs/posix/issue8/sh-utility.html#tag_20_110_14`. The enrichment correctly identifies the required exit status `127` for a missing `command_file` in a non-interactive shell, but its statement that the shell "should write a diagnostic message to stderr" is not part of this specific SHALL text. `docs/posix/issue8/sh-utility.html#tag_20_110_14` only normatively fixes the exit value here; any diagnostic requirement has to come from other sections, not from this extracted sentence itself.

## Implementation Notes

GPT-5.4 review of `tests/spec/SHALL-20-110-14-009.sh`: the original script only checked one missing pathname form. The strengthened script now checks both a slash-containing `command_file` pathname and a bare-name `command_file` operand that is absent from both the current working directory and the searched `PATH`, which better covers the command-file lookup model described in `docs/posix/issue8/sh-utility.html#tag_20_110_05` while keeping the asserted result tied to the `127` rule in `#tag_20_110_14`.
