---
id: SHALL-19-14-03-002
category: shall
section: "2.14.3 Patterns Used for Filename Expansion"
anchor: tag_19_14_03
source_file: utilities/V3_chap02.html
source_lines: [3029, 3038]
keywords: ["unspecified", "shall"]
tier: 1
status: enriched
spec_tested: true
refs: []
related_sections: []
tests: []
---

## Extracted Text

> The rules described so far in 2.14.1 Patterns Matching a Single Character and 2.14.2 Patterns Matching Multiple Characters are qualified by the following rules that apply when pattern matching notation is used for filename expansion:: If a filename begins with a <period> ('.'), the <period> shall be explicitly matched by using a <period> as the first character of the pattern or immediately following a <slash> character. The leading <period> shall not be matched by: The <asterisk> or <question-mark> special characters A bracket expression containing a non-matching list, such as "[!a]", a range expression, such as "[%-0]", or a character class expression, such as "[[:punct:]]" It is unspecified whether an explicit <period> in a bracket expression matching list, such as "[.abc]", can match a leading <period> in a filename.

## Context

**Leading dot (`.`) in filenames must be explicitly matched.**

During pathname expansion, if a filename begins with `.` (period), the period **shall** be explicitly matched by:
- A literal `.` as the first character of the pattern, OR
- A literal `.` immediately following a `/` in the pattern

The leading `.` **shall not** be matched by:
- `*` or `?` special characters
- A bracket expression containing a non-matching list (e.g., `[!a]`), a range expression (e.g., `[%-0]`), or a character class (e.g., `[[:punct:]]`)

It is **unspecified** whether an explicit `.` in a bracket expression matching list (e.g., `[.abc]`) can match a leading `.`.

This means `*` does not match `.hidden`, `.*` does match `.hidden`, and `ls *` won't show dotfiles. Implementation: when matching the first character of a filename (or the first character after a `/` in a path), check if the filename character is `.` and if so, require the pattern to also have a literal `.` at that position.

## Implementation Notes

*(to be filled during implementation)*
