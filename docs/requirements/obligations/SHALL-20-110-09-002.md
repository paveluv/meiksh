---
id: SHALL-20-110-09-002
category: shall
section: "ASYNCHRONOUS EVENTS"
anchor: tag_20_110_09
source_file: utilities/sh.html
source_lines: [264, 265]
keywords: ["shall"]
tier: 1
status: enriched
refs: []
related_sections: []
tests: []
spec_tested: blocked
testing_blockers: "requires interactive shell with line editing and SIGINT delivery during editing; cannot test in batch mode"
reviewed: GPT-5.4
---

## Extracted Text

> If the shell is interactive, SIGINT signals received during command line editing shall be handled as described in the EXTENDED DESCRIPTION, and SIGINT signals received at other times shall be caught but no action performed.

## Context

**Behavior:** When the shell is **interactive**:
- SIGINT signals received **during command line editing** must be handled as described in the EXTENDED DESCRIPTION (vi-mode editing: abort current edit line, reissue prompt, reset history — see SHALL-20-110-13-03-003).
- SIGINT signals received **at other times** (e.g., while waiting for a foreground job) must be caught but **no action performed** (the signal is absorbed without terminating the shell).

**Implementation:** This row constrains two different interactive contexts: line editing and all other interactive times.

**Applies to:** Interactive shell only.

**Spec reference:** ASYNCHRONOUS EVENTS section in `utilities/sh.html`, lines 264-265.

GPT-5.4 review: The extracted quote matches `docs/posix/issue8/sh-utility.html#tag_20_110_09`. Keeping this row blocked is appropriate because authoritative coverage would require a true interactive line-editing session plus signal injection during editing, which the current batch harness does not provide.

## Implementation Notes

GPT-5.4 review: no portable non-interactive `tests/spec` script is kept for this interactive-line-editing row.
