---
id: MAY-19-29-03-001
category: may
section: "DESCRIPTION"
anchor: tag_19_29_03
source_file: utilities/V3_chap02.html
source_lines: [5471, 5473]
keywords: ["may"]
tier: 1
status: enriched
refs: ["../basedefs/signal.h.html", "../basedefs/V1_chap14.html#tag_14"]
related_sections: []
tests: []
---

## Extracted Text

> The condition can be EXIT, 0 (equivalent to EXIT), or a signal specified using a symbolic name, without the SIG prefix, as listed in the tables of signal names in the <signal.h> header defined in XBD 14. Headers; for example, HUP, INT, QUIT, TERM. Implementations may permit names with the SIG prefix or ignore case in signal names as an extension. Setting a trap for SIGKILL or SIGSTOP produces undefined results.

## Context

**Behavior:** Trap condition names:
- `EXIT` or `0` (equivalent to EXIT).
- Signal symbolic names **without** the `SIG` prefix: `HUP`, `INT`, `QUIT`, `TERM`, etc., as listed in `<signal.h>`.
- Implementations **may** permit names with the `SIG` prefix (e.g., `SIGINT`) or case-insensitive names as extensions.
- Setting a trap for `SIGKILL` or `SIGSTOP` produces **undefined results**.

**Applies to:** All shell modes.

**Spec reference:** `trap` DESCRIPTION in `utilities/V3_chap02.html`, lines 5471-5473. Cross-ref: `basedefs/signal.h.html`.

## Implementation Notes

*(to be filled during implementation)*
