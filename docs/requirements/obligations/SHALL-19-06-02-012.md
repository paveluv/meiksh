---
id: SHALL-19-06-02-012
category: shall
section: "2.6.2 Parameter Expansion"
anchor: tag_19_06_02
source_file: utilities/V3_chap02.html
source_lines: [966, 971]
keywords: ["unspecified", "shall"]
tier: 1
status: enriched
refs: ["#tag_19_14", "#set"]
related_sections: []
tests: []
---

## Extracted Text

> The following four varieties of parameter expansion provide for character substring processing. In each case, pattern matching notation (see 2.14 Pattern Matching Notation), rather than regular expression notation, shall be used to evaluate the patterns. If parameter is '#', '*', or '@', the result of the expansion is unspecified. If parameter is unset and set -u is in effect, the expansion shall fail. Enclosing the full parameter expansion string in double-quotes shall not cause the following four varieties of pattern characters to be quoted, whereas quoting characters within the braces shall have this effect. In each variety, if word is omitted, the empty pattern shall be used.

## Context

**Behavior:** This obligation provides the common rules for the four substring/pattern parameter expansions (`${param%word}`, `${param%%word}`, `${param#word}`, `${param##word}`):

1. **Pattern notation:** The `word` part uses **shell pattern matching notation** (glob patterns per 2.14 Pattern Matching Notation: `*`, `?`, `[...]`), NOT regular expressions.
2. **Double-quote transparency:** Enclosing the entire expansion in double-quotes (e.g., `"${param%pattern}"`) does NOT quote the pattern characters in `word`. However, quoting characters **inside** the braces (e.g., `${param%"pattern"}`) DOES quote them (making `*` and `?` literal).
3. **Empty pattern default:** If `word` is omitted entirely (e.g., `${param%}` or `${param#}`), the empty pattern is used, which matches nothing — resulting in `parameter` unchanged.
4. **Restricted parameters:** Using `#`, `*`, or `@` as the parameter is **unspecified**.
5. **Nounset interaction:** If `parameter` is unset and `set -u` is active, the expansion must **fail**.

**Spec reference:** See 2.6.2 Parameter Expansion in utilities/V3_chap02.html, lines 966–971. See 2.14 Pattern Matching Notation (#tag_19_14) for pattern syntax.

## Implementation Notes

*(to be filled during implementation)*
