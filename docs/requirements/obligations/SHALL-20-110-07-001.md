---
id: SHALL-20-110-07-001
category: shall
section: "INPUT FILES"
anchor: tag_20_110_07
source_file: utilities/sh.html
source_lines: [157, 160]
keywords: ["shall"]
tier: 1
status: enriched
refs: ["../utilities/V3_chap02.html#tag_19_10_02"]
related_sections: []
tests: []
spec_tested: true
---

## Extracted Text

> The input file can be of any type, but the initial portion of the file intended to be parsed according to the shell grammar (see 2.10.2 Shell Grammar Rules) shall consist of characters and shall not contain the NUL character. The shell shall not enforce any line length limits. If the input file consists solely of zero or more blank lines and comments, sh shall exit with a zero exit status.

## Context

The shell must handle its input files (scripts, `-c` strings, stdin) according to these rules:

1. **Character content requirement**: The portion of the input file that is parsed according to the shell grammar (see 2.10.2 Shell Grammar Rules in utilities/V3_chap02.html#tag_19_10_02) must consist of valid characters and must **not** contain the NUL character (`\0`). The file itself can be of any type (regular, FIFO, etc.).

2. **No line length limits**: The shell must **not** enforce any maximum line length. It must be able to read and parse arbitrarily long lines without truncation or error.

3. **Empty/comment-only files**: If the input file consists solely of zero or more blank lines and comments (lines starting with `#`), the shell must exit with a **zero** exit status. No error should be produced for an empty script.

These constraints apply to all input modes: `-c` command strings, `-s` stdin reading, and `command_file` scripts.

See utilities/sh.html#tag_20_110_07 for the INPUT FILES section.

## Implementation Notes

*(to be filled during implementation)*
