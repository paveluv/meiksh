---
id: MAY-11-01-02-002
category: may
section: "11.1.2 Process Groups"
anchor: tag_11_01_02
source_file: basedefs/V1_chap11.html
source_lines: [64, 69]
keywords: ["may"]
tier: 3
status: enriched
refs: ["../functions/tcgetpgrp.html", "../functions/tcsetpgrp.html", "#tag_11_01_04"]
related_sections: []
tests: []
---

## Extracted Text

> A command interpreter process supporting job control can allocate the terminal to different jobs, or process groups, by placing related processes in a single process group and associating this process group with the terminal. A terminal's foreground process group may be set or examined by a process, assuming the permission requirements are met; see tcgetpgrp() and tcsetpgrp(). The terminal interface aids in this allocation by restricting access to the terminal by processes that are not in the current process group; see 11.1.4 Terminal Access Control.

## Context

**Behavior (optional):** A job-control shell allocates the terminal to different process groups (jobs) by calling tcsetpgrp() to set the foreground process group. Background process groups are restricted from terminal access (see 11.1.4). The shell can query the current foreground group via tcgetpgrp().

**Conditions:** Applies when the shell supports job control. The shell must have appropriate permissions (be in the foreground process group or be the session leader) to call tcsetpgrp().

**Shell relevance:** Directly describes the shell's job control mechanism. The shell must:
1. Place pipeline processes into a common process group
2. Call tcsetpgrp() to give the foreground job terminal access
3. Restore its own process group as foreground when the job completes/suspends

**Spec reference:** XBD 11.1.2 "Process Groups" (tag_11_01_02), `basedefs/V1_chap11.html`, lines 64–69.

## Implementation Notes

*(to be filled during implementation)*
