---
id: SHALL-19-09-02-001
category: shall
section: "2.9.2 Pipelines"
anchor: tag_19_09_02
source_file: utilities/V3_chap02.html
source_lines: [2011, 2011]
keywords: ["shall"]
tier: 1
status: enriched
refs: []
related_sections: []
tests: []
spec_tested: true
---

## Extracted Text

> A pipeline is a sequence of one or more commands separated by the control operator '|'. For each command but the last, the shell shall connect the standard output of the command to the standard input of the next command as if by creating a pipe and passing the write end of the pipe as the standard output of the command and the read end of the pipe as the standard input of the next command.

## Context

**Behavior:** A pipeline is one or more commands separated by `|`. For each command except the last, the shell MUST connect its stdout to the stdin of the next command using a pipe. Specifically: create a pipe, pass the write end as stdout of the current command, and the read end as stdin of the next command.

**Applies to:** All pipelines (`cmd1 | cmd2 | cmd3`). A single command without `|` is a degenerate pipeline.

**Implementation detail:** For an N-command pipeline, create N-1 pipes. For each command i (0 to N-2): dup the write end of pipe[i] to fd 1 in child i, and dup the read end of pipe[i] to fd 0 in child i+1. Close unused pipe ends in each child. All commands in the pipeline should be started before waiting for any to complete.

**Spec reference:** Section 2.9.2, line 2011 in `utilities/V3_chap02.html`.

## Implementation Notes

*(to be filled during implementation)*
