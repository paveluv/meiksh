---
id: SHALL-20-110-14-002
category: shall
section: "EXIT STATUS"
anchor: tag_20_110_14
source_file: utilities/sh.html
source_lines: [679, 679]
keywords: ["shall"]
tier: 1
status: enriched
spec_tested: true
refs: []
related_sections: []
tests:
  - ../../../tests/spec/SHALL-20-110-14-002.sh
reviewed: GPT-5.4
---

## Extracted Text

> 0

## Context

**Behavior:** The `sh` utility shall return exit status 0 under the condition described in SHALL-20-110-14-003. This is the exit value label "0" in the exit status definition list.

**Applies to:** The `sh` utility when the script contains only blank lines, comments, or both.

**Details:** Exit status 0 is returned when the shell successfully processes a script that contains no executable commands — only whitespace lines and/or comment lines. This means the shell parsed the input successfully but found nothing to execute.

**Spec reference:** Section "sh" EXIT STATUS, line 679 in `utilities/sh.html`.

## Implementation Notes

GPT-5.4 review: The prior extracted text incorrectly merged the EXIT STATUS heading with the `0` table entry. The corrected quote now matches `docs/posix/issue8/sh-utility.html#tag_20_110_14`. This is the value-label row for the condition described in `SHALL-20-110-14-003`, and the paired script appropriately checks that label's observable consequence.
