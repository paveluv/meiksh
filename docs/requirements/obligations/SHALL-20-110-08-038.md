---
id: SHALL-20-110-08-038
category: shall
section: "ENVIRONMENT VARIABLES"
anchor: tag_20_110_08
source_file: utilities/sh.html
source_lines: [223, 228]
keywords: ["unspecified", "shall"]
tier: 1
status: enriched
refs: ["javascript:open_code('UP')"]
related_sections: []
tests: []
spec_tested: blocked
testing_blockers: "duplicate of SHALL-20-110-08-023; requires interactive shell with mail monitoring"
reviewed: GPT-5.4
---

## Extracted Text

> [UP] Determine a pathname of the user's mailbox file for purposes of incoming mail notification. If this variable is set, the shell shall inform the user if the file named by the variable is created or if its modification time has changed. Informing the user shall be accomplished by writing a string of unspecified format to standard error prior to the writing of the next primary prompt string. Such check shall be performed only after the completion of the interval defined by the MAILCHECK variable after the last such check. The user shall be informed only if MAIL is set and MAILPATH is not set.

## Context

**Behavior:** This is a duplicate extraction of the MAIL variable description (same source lines as SHALL-20-110-08-023). The full behavior specification is identical:

1. **When MAIL is set and MAILPATH is not set:** Monitor the named file for creation or modification time changes.
2. **Notification:** Write an unspecified-format string to stderr before the next PS1 prompt.
3. **Check interval:** Only check after MAILCHECK seconds have elapsed since the last check.
4. **Precedence:** Only active when MAIL is set AND MAILPATH is not set.

**Feature gate:** [UP] (User Portability) option.

**See also:** SHALL-20-110-08-023 for the primary obligation covering this same text.

**Spec reference:** Section "sh" ENVIRONMENT VARIABLES, lines 223-228 in `utilities/sh.html`.

GPT-5.4 review: This quote correctly matches the same POSIX source text as `SHALL-20-110-08-023`, making this a duplicate extraction rather than a separate behavior row. The canonical behavior remains blocked because authoritative coverage would require interactive mail-notification behavior.

## Implementation Notes

GPT-5.4 review: no separate canonical `tests/spec` script is needed for this duplicate row.
