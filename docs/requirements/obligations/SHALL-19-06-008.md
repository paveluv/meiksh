---
id: SHALL-19-06-008
category: shall
section: "2.6 Word Expansions"
anchor: tag_19_06
source_file: utilities/V3_chap02.html
source_lines: [696, 705]
keywords: ["implementation-defined", "shall", "may"]
tier: 1
spec_tested: true
status: enriched
refs: []
related_sections: []
tests: []
---

## Extracted Text

> Tilde expansions, parameter expansions, command substitutions, arithmetic expansions, and quote removals that occur within a single word shall expand to a single field, except as described below. The shell shall create multiple fields or no fields from a single word only as a result of field splitting, pathname expansion, or the following cases:: When the expansion occurs in a context where field splitting will be performed, a word that contains all of the following somewhere within it, before any expansions are applied, in the order specified: an unquoted <left-curly-bracket> ('{') that is not immediately preceded by an unquoted <dollar-sign> ('$') one or more unquoted <comma> (',') characters or a sequence that consists of two adjacent <period> ('.') characters surrounded by other characters (which can also be <period> characters) an unquoted <right-curly-bracket> ('}') may be subject to an additional implementation-defined form of expansion that can create multiple fields from a single word. This expansion, if supported, shall be applied before all the other word expansions are applied. The other expansions shall then be applied to each field that results from this expansion.

## Context

This is the second exception to the single-field rule: brace expansion (implementation-defined).

**Required behavior:**
- In a field-splitting context, a word containing ALL of the following (in order, before any expansions):
  1. An unquoted `{` NOT immediately preceded by an unquoted `$`
  2. One or more unquoted `,` characters OR a sequence of two adjacent `.` characters surrounded by other characters (e.g., `{a..z}`)
  3. An unquoted `}`
  ...MAY be subject to an additional **implementation-defined** brace expansion that can create multiple fields from a single word.
- If brace expansion is supported:
  - It must be applied BEFORE all other word expansions.
  - The other expansions (tilde, parameter, command, arithmetic, field splitting, pathname, quote removal) are then applied to each field resulting from brace expansion.

**Conditions:** This is optional (implementation-defined). If meiksh implements brace expansion, it must follow this ordering rule. Brace expansion only applies in field-splitting contexts (not assignments).

**Implementation note:** Brace expansion is widely expected by users (bash, zsh, ksh93 all support it). Common forms: `{a,b,c}`, `{1..10}`, `{a..z}`.

See 2.6 "Word Expansions" in `utilities/V3_chap02.html`.

## Implementation Notes

*(to be filled during implementation)*
