---
id: SHALL-20-110-05-001
category: shall
section: "OPERANDS"
anchor: tag_20_110_05
source_file: utilities/sh.html
source_lines: [92, 92]
keywords: ["shall"]
tier: 1
status: enriched
refs: []
related_sections: []
tests:
  - ../../../tests/spec/SHALL-20-110-05-003.sh
  - ../../../tests/spec/SHALL-20-110-05-005.sh
  - ../../../tests/spec/SHALL-20-110-05-007.sh
  - ../../../tests/spec/SHALL-20-110-05-009.sh
  - ../../../tests/spec/SHALL-20-110-05-011.sh
spec_tested: blocked
testing_blockers: "Preamble listing operands; no standalone behavior to test"
reviewed: GPT-5.4
---

## Extracted Text

> The following operands shall be supported:

## Context

This is a structural preamble obligation: the `sh` utility must support the operands defined in the OPERANDS section. The specific operands are: `-` (single hyphen-minus), `argument`, `command_file`, `command_name`, and `command_string`. Each is detailed in subsequent obligations (SHALL-20-110-05-002 through SHALL-20-110-05-016).

The shell's argument parsing must correctly distinguish between options (like `-c`, `-s`, `-i`) and operands, following the Utility Syntax Guidelines (XBD 12.2). The `--` end-of-options marker separates options from operands.

See utilities/sh.html#tag_20_110_05 for the full OPERANDS section.

GPT-5.4 review: The extracted quote matches `docs/posix/issue8/sh-utility.html#tag_20_110_05`. This is the structural preamble for the operand list rather than a separate behavior row. The operative behavior belongs to the subordinate operand rows already reviewed and script-covered.

## Implementation Notes

GPT-5.4 review: no separate canonical `tests/spec` script is needed for this preamble row.
