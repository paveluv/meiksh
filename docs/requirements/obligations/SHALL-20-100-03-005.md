---
id: SHALL-20-100-03-005
category: shall
section: "DESCRIPTION"
anchor: tag_20_100_03
source_file: utilities/read.html
source_lines: [51, 52]
keywords: ["shall"]
tier: 2
status: enriched
refs: ["../utilities/V3_chap02.html#tag_19_06_05"]
related_sections: []
tests: []
spec_tested: true
---

## Extracted Text

> If IFS is unset, or is set to any non-empty value, then a modified version of the field splitting algorithm specified in 2.6.5 Field Splitting shall be applied, with the modifications as follows:

## Context

**Behavior required:** When `IFS` is either unset or set to a non-empty value, `read` must perform field splitting on the input using a modified version of the standard field splitting algorithm from Section 2.6.5.

**Condition:** `IFS` is unset (defaults to `<space><tab><newline>`) OR `IFS` is set to a non-empty string.

**Modifications to standard field splitting (detailed in subsequent obligations):**
1. The input is treated as a single initial field where escaped bytes (from backslash processing) behave as if quoted, and unescaped bytes behave as if unquoted.
2. The splitting loop stops early when n-1 fields have been generated (where n is the number of var operands), and the remainder is returned unsplit with leading/trailing IFS whitespace removed.

**Implementation detail:** When `IFS` is unset, use the default value `" \t\n"`. Then apply the modified field splitting algorithm described in SHALL-20-100-03-006 through SHALL-20-100-03-009.

**Spec reference:** Section DESCRIPTION of `utilities/read.html`, lines 51-52; Section 2.6.5.

## Implementation Notes

*(to be filled during implementation)*
