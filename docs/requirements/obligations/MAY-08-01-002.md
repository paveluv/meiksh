---
id: MAY-08-01-002
category: may
section: "8.1 Environment Variable Definition"
anchor: tag_08_01
source_file: basedefs/V1_chap08.html
source_lines: [61, 65]
keywords: ["may"]
tier: 2
status: enriched
refs: ["../functions/setenv.html", "../functions/unsetenv.html", "javascript:open_code('XSI')", "../functions/putenv.html"]
related_sections: []
tests: []
---

## Extracted Text

> Other name=value pairs may be placed in the environment by, for example, calling any of the setenv(), unsetenv(), [XSI] or putenv() functions, assigning a new value to the environ variable, or by using envp arguments when creating a process; see exec in the System Interfaces volume of POSIX.1-2024.

## Context

Additional `name=value` pairs may be placed in the environment by calling `setenv()`, `unsetenv()`, `putenv()` (XSI), reassigning `environ`, or using `envp` arguments in `exec`. This is a permissive statement about how environment variables can be added.

**Shell relevance:** The shell modifies the environment via its `export` and `unset` builtins, and through variable assignment in command prefixes. Internally, the shell manipulates `environ` or uses `setenv()`/`putenv()`. See XBD 8.1.

## Implementation Notes

*(to be filled during implementation)*
