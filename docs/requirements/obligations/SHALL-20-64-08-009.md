---
id: SHALL-20-64-08-009
category: shall
section: "ENVIRONMENT VARIABLES"
anchor: tag_20_64_08
source_file: utilities/kill.html
source_lines: [174, 175]
keywords: ["shall"]
tier: 3
spec_tested: blocked
testing_blockers: "LC_MESSAGES diagnostic format is libc locale infrastructure; no shell-specific behavior to test"
status: enriched
refs: []
related_sections: []
tests: []
reviewed: GPT-5.4
---

## Extracted Text

> Determine the locale that should be used to affect the format and contents of diagnostic messages written to standard error.

## Context

**Behavior:** `LC_MESSAGES` determines the locale used for the format and contents of diagnostic messages written to stderr by the `kill` built-in. This affects error messages such as "No such process" or "Invalid signal name".

**Spec reference:** Section ENVIRONMENT VARIABLES (tag_20_64_08) in `utilities/kill.html`, line 174.

GPT-5.4 review: The earlier extracted text merged the section heading with the `LC_MESSAGES` description and also cited too narrow a source span. The corrected quote now matches the actual POSIX description in `docs/posix/utilities/kill.html#tag_20_64_08`. The enrichment is accurate, and the blocked-test rationale is reasonable because this diagnostic-message localization behavior is delegated to locale infrastructure rather than shell-specific `kill` logic.

## Implementation Notes

GPT-5.4 review: no dedicated `tests/spec` script exists for this locale-framework row.
