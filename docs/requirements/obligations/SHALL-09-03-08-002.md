---
id: SHALL-09-03-08-002
category: shall
section: "9.3.8 BRE Expression Anchoring"
anchor: tag_09_03_08
source_file: basedefs/V1_chap09.html
source_lines: [448, 460]
keywords: ["shall", "may"]
tier: 2
status: enriched
refs: []
related_sections: []
tests: []
---

## Extracted Text

> A BRE can be limited to matching expressions that begin or end a string; this is called "anchoring". The <circumflex> and <dollar-sign> special characters shall be considered BRE anchors in the following contexts:: A <circumflex> ('^') shall be an anchor when used as the first character of an entire BRE and, if the implementation does not match the escape sequence "\|" to the literal character '|', when used immediately following a "\|" escape sequence that is not inside a subexpression. The implementation may also treat a <circumflex> as an anchor when used inside a subexpression; in this case it shall be an anchor only when either of the following is true: It is the first character of the subexpression. It immediately follows a "\|" escape sequence and the implementation does not match the escape sequence "\|" to the literal character '|'. The <circumflex> shall anchor the expression (or optionally subexpression) to the beginning of a string; only sequences starting at the first character of a string shall be matched by the BRE. For example, the BRE "^ab" matches "ab" in the string "abcdef", but fails to match in the string "cdefab". The BRE "\(^ab\)" may match the former string. A portable BRE shall escape a leading <circumflex> in a subexpression to match a literal <circumflex>.

## Context

**Behavior:** `^` shall be a BRE anchor when: (1) used as the first character of the entire BRE, or (2) immediately following a `\|` escape sequence that is not inside a subexpression (if `\|` is not treated as literal). The implementation may also treat `^` as an anchor inside subexpressions (first character or after `\|`). `^` anchors the expression to the beginning of the string. A portable BRE shall escape a leading `^` in a subexpression to match a literal `^`.

**Shell relevance:** BRE anchoring does not apply to shell pattern matching. Shell patterns do not use `^` as an anchor — `^` is an ordinary character in shell patterns except inside bracket expressions where it negates the list. This obligation is BRE-specific.

**Spec reference:** Section 9.3.8, lines 448-460 in `basedefs/V1_chap09.html`.

## Implementation Notes

*(to be filled during implementation)*
