---
id: SHALL-08-03-008
category: shall
section: "8.3 Other Environment Variables"
anchor: tag_08_03
source_file: basedefs/V1_chap08.html
source_lines: [721, 722]
keywords: ["shall", "may"]
tier: 2
status: enriched
spec_tested: true
refs: ["../utilities/V3_chap02.html#tag_19"]
related_sections: []
tests: []
---

## Extracted Text

> This variable shall represent a pathname of the user's preferred command language interpreter. If this interpreter does not conform to the Shell Command Language in XCU 2. Shell Command Language, utilities may behave differently from those described in POSIX.1-2024.

## Context

**Behavior:** The `SHELL` environment variable shall represent a pathname of the user's preferred command language interpreter. If this interpreter does not conform to the Shell Command Language (XCU section 2), utilities may behave differently from POSIX.1-2024 descriptions.

**Shell relevance:** The shell must propagate `SHELL` to child processes. The shell should not modify `SHELL` — it is set by the login process, not by the shell itself. Utilities like `vi`, `make`, and `system()` may use `SHELL` to determine which shell to invoke. `meiksh` should not assume `SHELL` points to itself.

**Spec reference:** Section 8.3 "Other Environment Variables", lines 721-722 in `basedefs/V1_chap08.html`.

## Implementation Notes

*(to be filled during implementation)*
