---
id: SHALL-20-110-13-05-018
category: shall
section: "vi Line Editing Command Mode"
anchor: tag_20_110_13_05
source_file: utilities/sh.html
source_lines: [436, 438]
keywords: ["shall", "may"]
tier: 1
status: enriched
refs: []
related_sections: []
tests: []
spec_tested: blocked
testing_blockers: "vi command mode @letter alias macro requires interactive TTY simulation"
reviewed: GPT-5.4
---

## Extracted Text

> Insert the value of the alias named _letter. The symbol letter represents a single alphabetic character from the portable character set; implementations may support additional characters as an extension. If the alias _letter contains other editing commands, these commands shall be performed as part of the insertion. If no alias _letter is enabled, this command shall have no effect.

## Context

**Behavior:** In vi command mode, `@letter` shall insert the value of the alias named `_letter` (underscore prefix + the letter). The letter must be a single alphabetic character from the portable character set. If the alias value contains editing commands, those commands shall be executed as part of the insertion (macro expansion). If no alias `_letter` is defined/enabled, the command has no effect.

**Implementation:** Look up alias `_<letter>` in the alias table. If found, feed its value back into the command mode input as if the user typed those characters (macro replay). This enables user-defined editing macros.

**Spec reference:** vi Line Editing Command Mode in `utilities/sh.html`, line 436–439.

## Implementation Notes

GPT-5.4 review: The prior extracted text incorrectly merged the command-list heading with the command semantics. The corrected quote now matches `docs/posix/issue8/sh-utility.html#tag_20_110_13_05`. Canonical coverage requires interactive command-mode macro behavior and alias-enabled editing commands.
