---
id: SHALL-20-64-08-011
category: shall
section: "ENVIRONMENT VARIABLES"
anchor: tag_20_64_08
source_file: utilities/kill.html
source_lines: [177, 178]
keywords: ["shall"]
tier: 3
spec_tested: blocked
testing_blockers: "NLSPATH message catalog location is libc locale infrastructure; no shell-specific behavior to test"
status: enriched
refs: ["javascript:open_code('XSI')"]
related_sections: []
tests: []
reviewed: GPT-5.4
---

## Extracted Text

> [XSI] Determine the location of messages objects and message catalogs.

## Context

**Behavior:** `NLSPATH` (XSI extension) determines the location of message objects and message catalogs for the `kill` built-in. This is used in conjunction with `LC_MESSAGES` to find locale-specific diagnostic message files.

**Conditions:** Only applicable on systems supporting the XSI option. On non-XSI systems, this variable may be ignored.

**Spec reference:** Section ENVIRONMENT VARIABLES (tag_20_64_08) in `utilities/kill.html`, lines 177–178. XSI extension.

GPT-5.4 review: The earlier extracted text merged the section heading with the `NLSPATH` description. The corrected quote now matches the actual XSI-marked POSIX description in `docs/posix/utilities/kill.html#tag_20_64_08`. The enrichment is accurate, and the blocked-test rationale is reasonable because message-catalog lookup is delegated to locale/message-catalog infrastructure rather than shell-specific `kill` logic.

## Implementation Notes

GPT-5.4 review: no dedicated `tests/spec` script exists for this locale-framework row.
