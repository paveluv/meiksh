---
id: SHALL-19-11-029
category: shall
section: "2.11 Job Control"
anchor: tag_19_11
source_file: utilities/V3_chap02.html
source_lines: [2841, 2841]
keywords: ["shall"]
tier: 1
status: enriched
spec_tested: blocked
testing_blockers: "Requires set -b with stopped background job notification timing verification; needs interactive pty"
refs: ["#set"]
related_sections: []
tests: []
---

## Extracted Text

> When a process associated with a background job is stopped by a SIGSTOP, SIGTSTP, SIGTTIN, or SIGTTOU signal, the shell shall convert the (non-suspended) background job into a suspended job and an interactive shell shall write a message to standard error, formatted as described by the jobs utility (without the -l option) for a suspended job, at the following time:: If set -b is enabled, the message shall be written either immediately after the job became suspended or immediately prior to writing the next prompt for input.

## Context

**Background job stopped notification — `set -b` enabled (parent obligation):** When `set -b` (notify) is enabled and a background job is stopped by a signal, the suspended-job message shall be written **either** immediately after the job became suspended **or** immediately prior to writing the next prompt for input.

`set -b` enables asynchronous job notifications. The implementation may choose either timing; "immediately" means before the next prompt or as soon as the shell's main loop can process the event. See the `set` builtin description for the `-b` option.

## Implementation Notes

*(to be filled during implementation)*
