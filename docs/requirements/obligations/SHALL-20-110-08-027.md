---
id: SHALL-20-110-08-027
category: shall
section: "ENVIRONMENT VARIABLES"
anchor: tag_20_110_08
source_file: utilities/sh.html
source_lines: [237, 244]
keywords: ["unspecified", "shall"]
tier: 1
status: enriched
refs: ["javascript:open_code('UP')"]
related_sections: []
tests: []
spec_tested: blocked
testing_blockers: "requires interactive shell with MAILPATH monitoring and prompt loop; cannot test in batch mode"
reviewed: GPT-5.4
---

## Extracted Text

> [UP] Provide a list of pathnames and optional messages separated by <colon> characters. If this variable is set, the shell shall inform the user if any of the files named by the variable are created or if any of their modification times change. (See the preceding entry for MAIL for descriptions of mail arrival and user informing.) Each pathname can be followed by '%' and a string that shall be subjected to parameter expansion and written to standard error when the modification time changes. If a '%' character in the pathname is preceded by a <backslash>, it shall be treated as a literal '%' in the pathname. The default message is unspecified. The MAILPATH environment variable takes precedence over the MAIL variable.

## Context

**Behavior:** MAILPATH provides multi-mailbox monitoring with custom messages:

1. **Format:** A colon-separated list of entries. Each entry is: `pathname[%message]`
2. **Monitoring:** The shell shall inform the user if any named file is created or its modification time changes. The same timing rules as MAIL apply (check at MAILCHECK intervals, notify before next PS1 prompt, write to stderr).
3. **Custom messages:** Each pathname can be followed by `%` and a message string. This message string shall be subjected to parameter expansion and written to stderr when the file's modification time changes.
4. **Escaping:** A `%` character preceded by a backslash (`\%`) is treated as a literal `%` in the pathname, not as the message delimiter.
5. **Default message:** If no `%message` is given for a pathname, the shell uses an unspecified default notification message.
6. **Precedence:** MAILPATH takes precedence over MAIL. When MAILPATH is set, MAIL is ignored for notification purposes.

**Applies to:** Interactive shells only (User Portability [UP] option).

**Parsing example:** `MAILPATH="/var/mail/user%You have new mail:/var/spool/other"` — two mailboxes, first with a custom message, second with the default message.

**Spec reference:** Section "sh" ENVIRONMENT VARIABLES, lines 237-244 in `utilities/sh.html`.

## Implementation Notes

GPT-5.4 review: The prior extracted text incorrectly included the section heading. The corrected quote now matches `docs/posix/issue8/sh-utility.html#tag_20_110_08`. This remains doc-only because authoritative coverage would require an interactive prompt loop plus MAILPATH-specific monitoring and message expansion behavior.
