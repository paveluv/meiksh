---
id: SHALL-20-132-14-002
category: shall
section: "EXIT STATUS"
anchor: tag_20_132_14
source_file: utilities/umask.html
source_lines: [149, 149]
keywords: ["shall"]
tier: 2
status: enriched
spec_tested: true
refs: []
related_sections: []
tests:
  - ../../../tests/spec/SHALL-20-132-14-002.sh
reviewed: GPT-5.4
---

## Extracted Text

> 0

## Context

**Behavior:** The `umask` utility shall return exit status 0 when it successfully completes. This covers both modes: when the mask was successfully changed (set mode) and when no mask operand was supplied (query mode — the current mask was displayed).

**Spec reference:** Section EXIT STATUS (tag_20_132_14) in `utilities/umask.html`, line 149.

GPT-5.4 review: The prior extracted text incorrectly merged the EXIT STATUS heading with the exit-value label. The actual POSIX text at `docs/posix/utilities/umask.html#tag_20_132_14` is simply the table entry `0`, with the substantive meaning provided by the following row already reviewed under `SHALL-20-132-14-003`. This row is best treated as the success value-label entry.

## Implementation Notes

GPT-5.4 review of `tests/spec/SHALL-20-132-14-002.sh`: the script appropriately checks exit status `0` for both successful set mode and successful query mode.
