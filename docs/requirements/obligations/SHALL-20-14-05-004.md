---
id: SHALL-20-14-05-004
category: shall
section: "OPERANDS"
anchor: tag_20_14_05
source_file: utilities/cd.html
source_lines: [156, 161]
keywords: ["unspecified", "shall"]
tier: 2
status: enriched
refs: []
related_sections: []
tests: []
---

## Extracted Text

> An absolute or relative pathname of the directory that shall become the new working directory. The interpretation of a relative pathname by cd depends on the -L option and the CDPATH and PWD environment variables. If directory is an empty string, cd shall write a diagnostic message to standard error and exit with non-zero status. If directory consists of a single '-' (<hyphen-minus>) character, the cd utility shall behave as if directory contained the value of the OLDPWD environment variable, except that after it sets the value of PWD it shall write the new value to standard output. The behavior is unspecified if OLDPWD does not start with a <slash> character.

## Context

**Behavior:** Duplicate extraction of the same spec text as SHALL-20-14-05-003. The `directory` operand must become the new working directory. Key requirements:

1. The directory operand is an absolute or relative pathname of the target directory.
2. Relative pathname resolution depends on `-L`/`-P` options and `CDPATH`/`PWD` environment variables.
3. Empty string directory: must write diagnostic to stderr and exit non-zero.
4. Single `-` directory: behave as if `$OLDPWD` was the operand, but also print the new PWD to stdout after changing.
5. Unspecified behavior when OLDPWD does not start with `/`.

**Inputs:** The `directory` operand string.

**Outputs:** On success, the working directory is changed. On `-`, the new directory path is written to stdout. On error (empty string, nonexistent directory), diagnostic to stderr and non-zero exit.

**Spec reference:** POSIX.1-2024, `utilities/cd.html`, section OPERANDS (tag_20_14_05), lines 156-161.

## Implementation Notes

*(to be filled during implementation)*
