---
id: SHALL-20-132-03-002
category: shall
section: "DESCRIPTION"
anchor: tag_20_132_03
source_file: utilities/umask.html
source_lines: [46, 46]
keywords: ["shall"]
tier: 2
status: enriched
spec_tested: true
refs: []
related_sections: []
tests:
  - ../../../tests/spec/SHALL-20-132-03-002.sh
reviewed: GPT-5.4
---

## Extracted Text

> it shall not affect the file mode creation mask of the caller's environment.

## Context

**Behavior:** When `umask` is invoked in a subshell or a separate utility execution environment (e.g., `(umask 002)`, `nohup umask ...`, `find . -exec umask ... \;`), the file mode creation mask change shall NOT propagate to the caller's (parent) shell environment.

**Conditions:** This applies whenever `umask` runs in a context that creates a new execution environment separate from the current shell, such as the examples given in `docs/posix/utilities/umask.html#tag_20_132_03`.

**Implementation detail:** Since subshells inherit a copy of the parent environment, changes to the umask in a child process naturally do not affect the parent. The shell must ensure it does not provide any mechanism for subshell umask changes to propagate upward.

**Spec reference:** Section DESCRIPTION (tag_20_132_03) in `utilities/umask.html`, line 46.

GPT-5.4 review: The extracted quote matches `docs/posix/utilities/umask.html#tag_20_132_03`. The earlier enrichment was slightly broader than the cited text because it listed additional contexts not named in the extracted example block. The tightened wording above stays closer to the POSIX language while preserving the key normative point: a separate execution environment must not change the caller's mask.

## Implementation Notes

GPT-5.4 review of `tests/spec/SHALL-20-132-03-002.sh`: the script cleanly checks the most direct required case, namely that a subshell `umask` change does not leak back into the parent shell environment.
