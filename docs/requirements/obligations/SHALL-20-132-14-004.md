---
id: SHALL-20-132-14-004
category: shall
section: "EXIT STATUS"
anchor: tag_20_132_14
source_file: utilities/umask.html
source_lines: [151, 151]
keywords: ["shall"]
tier: 2
status: enriched
spec_tested: true
refs: []
related_sections: []
tests:
  - ../../../tests/spec/SHALL-20-132-14-004.sh
reviewed: GPT-5.4
---

## Extracted Text

> >0

## Context

**Behavior:** The `umask` utility shall return an exit status greater than 0 when an error occurs. Errors include invalid mask operands (malformed symbolic or octal strings), unrecognized options, or other failures.

**Spec reference:** Section EXIT STATUS (tag_20_132_14) in `utilities/umask.html`, line 151.

GPT-5.4 review: The prior extracted text incorrectly merged the EXIT STATUS heading with the exit-value label. The actual POSIX text at `docs/posix/utilities/umask.html#tag_20_132_14` is simply the table entry `>0`, with the substantive meaning given by the following row already reviewed under `SHALL-20-132-14-005`. This row is best treated as the error value-label entry.

## Implementation Notes

GPT-5.4 review of `tests/spec/SHALL-20-132-14-004.sh`: the script appropriately checks that an invalid operand yields a non-zero exit status, which is the key observable consequence for this entry.
