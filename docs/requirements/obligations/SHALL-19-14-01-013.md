---
id: SHALL-19-14-01-013
category: shall
section: "2.14.1 Patterns Matching a Single Character"
anchor: tag_19_14_01
source_file: utilities/V3_chap02.html
source_lines: [2999, 3001]
keywords: ["unspecified", "shall"]
tier: 1
status: enriched
refs: ["../basedefs/V1_chap09.html#tag_09_03_05"]
related_sections: []
tests: []
---

## Extracted Text

> When unquoted, unescaped, and not inside a bracket expression, the following three characters shall have special meaning in the specification of patterns:: A <left-square-bracket> shall introduce a bracket expression if the characters following it meet the requirements for bracket expressions stated in XBD 9.3.5 RE Bracket Expression, except that the <exclamation-mark> character ('!') shall replace the <circumflex> character ('^') in its role in a non-matching list in the regular expression notation. A bracket expression starting with an unquoted <circumflex> character produces unspecified results. A <left-square-bracket> that does not introduce a valid bracket expression shall match the character itself.

## Context

**`[` introduces a bracket expression; `!` replaces `^` for negation; invalid `[` matches itself.**

An unquoted, unescaped `[` **shall** introduce a bracket expression if the characters following it satisfy the requirements of XBD 9.3.5 RE Bracket Expression (see basedefs/V1_chap09.html#tag_09_03_05), with the following shell-specific modifications:

1. **Negation**: `!` replaces `^` for non-matching lists. `[!abc]` matches any character NOT in {a, b, c}. A bracket expression starting with `^` (e.g., `[^abc]`) produces **unspecified** results.

2. **Invalid bracket expression**: If `[` does not introduce a valid bracket expression (e.g., no matching `]`, or the contents don't form a valid expression), the `[` **shall** match the literal character `[` itself.

Bracket expressions support:
- Matching lists: `[abc]` matches a, b, or c
- Non-matching lists: `[!abc]` matches anything except a, b, c
- Range expressions: `[a-z]`
- Character classes: `[[:alpha:]]`, `[[:digit:]]`, etc.
- Collating symbols and equivalence classes per locale

See basedefs/V1_chap09.html#tag_09_03_05 for the full bracket expression grammar.

## Implementation Notes

*(to be filled during implementation)*
