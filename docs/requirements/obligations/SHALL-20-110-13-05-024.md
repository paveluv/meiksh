---
id: SHALL-20-110-13-05-024
category: shall
section: "vi Line Editing Command Mode"
anchor: tag_20_110_13_05
source_file: utilities/sh.html
source_lines: [454, 455]
keywords: ["shall"]
tier: 1
status: enriched
refs: ["../utilities/vi.html"]
related_sections: []
tests: []
spec_tested: blocked
testing_blockers: "vi command mode v editor invocation requires interactive TTY simulation"
reviewed: GPT-5.4
---

## Extracted Text

> Invoke the vi editor to edit the current command line in a temporary file. When the editor exits, the commands in the temporary file shall be executed and placed in the command history. If a number is included, it specifies the command number in the command history to be edited, rather than the current command line.

## Context

**Behavior:** In vi command mode, `v` shall invoke the `vi` editor to edit the current command line in a temporary file. When the editor exits, the commands in the temporary file shall be executed and placed in the command history. If a number is provided, it specifies the command history number to edit rather than the current command line.

GPT-5.4 review: This sentence requires invocation of the `vi` editor on a temporary file and defines what happens after the editor exits. It does not itself specify editor-selection fallback rules beyond naming `vi`.

**Spec reference:** vi Line Editing Command Mode in `utilities/sh.html`, line 454–456. See `utilities/vi.html` for the vi editor.

## Implementation Notes

GPT-5.4 review: The prior extracted text incorrectly merged the command-list heading with the command semantics. The corrected quote now matches `docs/posix/issue8/sh-utility.html#tag_20_110_13_05`. Canonical coverage would require an interactive session plus external editor orchestration and history observation.
