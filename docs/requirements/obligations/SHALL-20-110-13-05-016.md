---
id: SHALL-20-110-13-05-016
category: shall
section: "vi Line Editing Command Mode"
anchor: tag_20_110_13_05
source_file: utilities/sh.html
source_lines: [429, 434]
keywords: ["shall"]
tier: 1
status: enriched
refs: []
related_sections: []
tests: []
spec_tested: blocked
testing_blockers: "vi command mode asterisk pathname expansion requires interactive TTY simulation"
reviewed: GPT-5.4
---

## Extracted Text

> Perform pathname expansion on the current bigword and insert all expansions into the command to replace the current bigword, with each expansion separated by a single <space>. If at the end of the line, the current cursor position shall be moved to the first column position following the expansions and sh shall be placed in insert mode. Otherwise, the current cursor position shall be the last column position of the first character after the expansions and sh shall be placed in insert mode. If the current bigword contains none of the characters '?', '*', or '[', before the operation, an <asterisk> ('*') shall be implicitly assumed at the end.

## Context

**Behavior:** In vi command mode, `*` shall perform pathname expansion on the current bigword and insert ALL expansions into the command line to replace the bigword, separated by single spaces.

**Details:**
- If the bigword contains none of `?`, `*`, or `[`, an `*` is implicitly appended before expansion.
- If at the end of the line: cursor moves to the first column position following the expansions, shell enters insert mode.
- If not at end of line: cursor is placed on the last character of the first character after the expansions, shell enters insert mode.

**Implementation:** Extract bigword, perform glob, join all results with spaces, replace bigword in buffer, position cursor appropriately, switch to insert mode.

**Spec reference:** vi Line Editing Command Mode in `utilities/sh.html`, line 429–434.

## Implementation Notes

GPT-5.4 review: The prior extracted text incorrectly merged the command-list heading with the command semantics. The corrected quote now matches `docs/posix/issue8/sh-utility.html#tag_20_110_13_05`. Canonical coverage requires interactive command-mode behavior that inspects full expansion insertion, resulting cursor placement, and mode switching.
