---
id: SHALL-20-110-08-001
category: shall
section: "ENVIRONMENT VARIABLES"
anchor: tag_20_110_08
source_file: utilities/sh.html
source_lines: [164, 164]
keywords: ["shall"]
tier: 1
status: enriched
refs: []
related_sections: []
tests:
  - ../../../tests/spec/SHALL-20-110-08-011.sh
  - ../../../tests/spec/SHALL-20-110-08-031.sh
  - ../../../tests/spec/SHALL-20-110-08-033.sh
spec_tested: blocked
testing_blockers: "Enumeration preamble for environment-variable rows; covered through subordinate canonical variables rather than a standalone behavior"
reviewed: GPT-5.4
---

## Extracted Text

> The following environment variables shall affect the execution of sh:

## Context

**Behavior:** This is the introductory statement for the `sh` utility's ENVIRONMENT VARIABLES section. The shell is affected by a specific set of environment variables: ENV, FCEDIT, HISTFILE, HISTSIZE, HOME, LANG, LC_ALL, LC_COLLATE, LC_CTYPE, LC_MESSAGES, MAIL, MAILCHECK, MAILPATH, NLSPATH, PATH, and PWD.

**Applies to:** All invocations of `sh`, both interactive and non-interactive.

**Details:** The shell must recognize and honor each of these environment variables according to their individual specifications. Some are conditional on interactive mode (ENV, MAIL, MAILCHECK, MAILPATH), some are conditional on the UP (User Portability) option (ENV, FCEDIT, HISTFILE, HISTSIZE, MAIL, MAILCHECK, MAILPATH), and some on XSI (NLSPATH). The shell must read these from the inherited environment at startup and respond to changes as specified by each variable's individual rules.

**Spec reference:** Section "sh" ENVIRONMENT VARIABLES, line 164 in `utilities/sh.html`.

GPT-5.4 review: The extracted quote matches `docs/posix/issue8/sh-utility.html#tag_20_110_08`. This is a framing row introducing the variable list, not an independently testable behavior. The meaningful executable coverage belongs to subordinate canonical rows such as `HOME`, `PATH`, and `PWD`.

## Implementation Notes

GPT-5.4 review: no separate canonical `tests/spec` script is needed for this introductory row.
