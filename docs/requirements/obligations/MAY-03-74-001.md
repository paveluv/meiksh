---
id: MAY-03-74-001
category: may
section: "3.74 Collation Sequence"
anchor: tag_03_74
source_file: basedefs/V1_chap03.html
source_lines: [446, 449]
keywords: ["may"]
tier: 2
status: enriched
refs: ["#tag_03_126"]
related_sections: []
tests: []
---

## Extracted Text

> Multi-level sorting is accomplished by assigning elements one or more collation weights, up to the limit {COLL_WEIGHTS_MAX}. On each level, elements may be given the same weight (at the primary level, called an equivalence class; see also 3.126 Equivalence Class) or be omitted from the sequence. Strings that collate equally using the first assigned weight (primary ordering) are then compared using the next assigned weight (secondary ordering), and so on.

## Context

**Definition — low shell relevance.** Defines "collation sequence" and multi-level sorting. Elements are assigned collation weights at multiple levels. Elements may share weights (equivalence classes at the primary level) or be omitted. Strings equal at the primary level are compared at secondary levels, and so on, up to `{COLL_WEIGHTS_MAX}`.

**Shell impact:** Collation sequence affects shell pattern matching with `[` bracket expressions and the `case` statement when `LC_COLLATE` is set. The `sort` utility (external) uses collation heavily. For the shell itself, collation primarily affects range expressions like `[a-z]` in filename expansion and `case` patterns. The shell delegates to the locale's collation tables via system APIs (`strcoll`, `fnmatch`).

**Spec reference:** basedefs/V1_chap03.html, section 3.74; see also 3.126 Equivalence Class.

## Implementation Notes

*(to be filled during implementation)*
