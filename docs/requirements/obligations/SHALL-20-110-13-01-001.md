---
id: SHALL-20-110-13-01-001
category: shall
section: "Command History List"
anchor: tag_20_110_13_01
source_file: utilities/sh.html
source_lines: [306, 309]
keywords: ["unspecified", "shall"]
tier: 1
status: enriched
refs: []
related_sections: []
tests: []
---

## Extracted Text

> When the sh utility is being used interactively, it shall maintain a list of commands previously entered from the terminal in the file named by the HISTFILE environment variable. The type, size, and internal format of this file are unspecified. Multiple sh processes can share access to the file for a user, if file access permissions allow this; see the description of the HISTFILE environment variable.

## Context

**Behavior:** When the shell is interactive, it must maintain a command history list — a list of commands previously entered from the terminal. This list must be stored in the file named by the `HISTFILE` environment variable.

**Requirements:**
1. The history file's type, size, and internal format are unspecified (implementation may choose any format).
2. Multiple `sh` processes can share access to the same history file if file permissions allow (the implementation should handle concurrent access gracefully).
3. The history file is only maintained in interactive mode.

**Key variables:** `HISTFILE` (path to history file), `HISTSIZE` (max entries to keep in memory).

**Applies to:** Interactive shell only.

**Spec reference:** Command History List section in `utilities/sh.html`, lines 306-309.

## Implementation Notes

*(to be filled during implementation)*
