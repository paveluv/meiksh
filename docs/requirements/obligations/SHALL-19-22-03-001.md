---
id: SHALL-19-22-03-001
category: shall
section: "DESCRIPTION"
anchor: tag_19_22_03
source_file: utilities/V3_chap02.html
source_lines: [4040, 4043]
keywords: ["shall"]
tier: 1
status: enriched
refs: ["#exit"]
related_sections: []
tests: []
---

## Extracted Text

> The exit utility shall cause the shell to exit from its current execution environment. If the current execution environment is a subshell environment, the shell shall exit from the subshell environment and continue in the environment from which that subshell environment was invoked; otherwise, the shell utility shall terminate. The wait status of the shell or subshell shall be determined by the unsigned decimal integer n, if specified.

## Context

**Behavior:** The `exit` special built-in causes the shell to exit from its current execution environment:
1. If in a **subshell environment**, exit from the subshell and continue in the parent environment.
2. If in the **main shell environment** (not a subshell), terminate the shell utility.
3. The wait status (exit code) is determined by the unsigned decimal integer `n`, if specified.

**Applies to:** All shell modes. This is a special built-in.

**Spec reference:** `exit` DESCRIPTION in `utilities/V3_chap02.html`, lines 4040-4043.

## Implementation Notes

*(to be filled during implementation)*
