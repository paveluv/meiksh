---
id: SHALL-11-02-02-006
category: shall
section: "11.2.2 Input Modes"
anchor: tag_11_02_02
source_file: basedefs/V1_chap11.html
source_lines: [516, 518]
keywords: ["shall"]
tier: 3
status: enriched
refs: ["#tag_11_02_04"]
related_sections: []
tests: []
---

## Extracted Text

> If INPCK is set, input parity checking shall be enabled. If INPCK is not set, input parity checking shall be disabled, allowing output parity generation without input parity errors. Note that whether input parity checking is enabled or disabled is independent of whether parity detection is enabled or disabled (see 11.2.4 Control Modes). If parity detection is enabled but input parity checking is disabled, the hardware to which the terminal is connected shall recognize the parity bit, but the terminal special file shall not check whether or not this bit is correctly set.

## Context

**Behavior:** When the terminal input mode flag INPCK is set in `c_iflag`, the terminal driver shall perform parity checking on incoming bytes. When INPCK is not set, input parity checking shall be disabled — this allows parity generation on output without raising parity errors on input.

**Conditions:** This applies when the shell (via `stty` or `tcsetattr()`) configures a terminal's `c_iflag`. Parity detection (enabled by PARENB in `c_cflag`, see §11.2.4) is independent of INPCK: the hardware may detect parity bits even when INPCK is off, but the terminal special file will not flag incorrectly-set parity bits as errors.

**Relevance to meiksh:** The `stty` builtin must correctly set/clear the INPCK flag. The shell does not implement parity checking itself — it delegates to the OS terminal driver — but must correctly pass through flag values.

**Spec reference:** POSIX.1-2024, §11.2.2 Input Modes (tag_11_02_02), `basedefs/V1_chap11.html` lines 516–518. See also §11.2.4 Control Modes for PARENB.

## Implementation Notes

*(to be filled during implementation)*
