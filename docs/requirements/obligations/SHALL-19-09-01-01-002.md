---
id: SHALL-19-09-01-01-002
category: shall
section: "2.9.1.1 Order of Processing"
anchor: tag_19_09_01_01
source_file: utilities/V3_chap02.html
source_lines: [1737, 1737]
keywords: ["shall"]
tier: 1
status: enriched
spec_tested: true
refs: ["#tag_19_10_02"]
related_sections: []
tests: []
---

## Extracted Text

> When a given simple command is required to be executed (that is, when any conditional construct such as an AND-OR list or a case statement has not bypassed the simple command), the following expansions, assignments, and redirections shall all be performed from the beginning of the command text to the end:: The words that are recognized as variable assignments or redirections according to 2.10.2 Shell Grammar Rules are saved for processing in steps 3 and 4.

## Context

**Behavior:** Step 1 of simple command processing: The shell MUST identify words that are variable assignments or redirections (according to the grammar rules in 2.10.2 Shell Grammar Rules) and save them for later processing in steps 3 (redirections) and 4 (variable assignments). These words are NOT expanded yet at this stage — they are merely identified and set aside.

**Applies to:** All simple commands during execution. The grammar rules in 2.10.2 define that a word is a variable assignment if it matches `NAME=value` at the start of the command (before any non-assignment, non-redirection word), and redirections are `>`, `<`, `>>`, `<<`, etc.

**Implementation detail:** Before expanding anything, scan the word list left-to-right. Leading words matching `NAME=...` pattern and redirection operators with their targets are saved. The first word that is neither an assignment nor a redirection starts step 2.

**Spec reference:** Section 2.9.1.1, line 1737 in `utilities/V3_chap02.html`. Cross-ref: 2.10.2 Shell Grammar Rules.

## Implementation Notes

*(to be filled during implementation)*
