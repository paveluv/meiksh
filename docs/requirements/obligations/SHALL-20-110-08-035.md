---
id: SHALL-20-110-08-035
category: shall
section: "ENVIRONMENT VARIABLES"
anchor: tag_20_110_08
source_file: utilities/sh.html
source_lines: [177, 180]
keywords: ["shall"]
tier: 1
status: enriched
refs: ["javascript:open_code('UP')", "../utilities/ed.html"]
related_sections: []
tests: []
spec_tested: blocked
testing_blockers: "duplicate of SHALL-20-110-08-005; requires interactive fc with editor"
reviewed: GPT-5.4
---

## Extracted Text

> [UP] This variable, when expanded by the shell, shall determine the default value for the -e editor option's editor option-argument. If FCEDIT is null or unset, ed shall be used as the editor.

## Context

**Behavior:** This is a duplicate extraction of the FCEDIT variable description (same source lines as SHALL-20-110-08-005). The full behavior specification is identical:

1. **Default editor for fc:** When the `fc` built-in is invoked without an explicit `-e editor` argument, the shell expands FCEDIT and uses the result as the editor command.
2. **Fallback:** If FCEDIT is null (empty string) or unset, `ed` shall be used as the default editor.

**Feature gate:** [UP] (User Portability) option.

**See also:** SHALL-20-110-08-005 for the primary obligation covering this same text.

**Spec reference:** Section "sh" ENVIRONMENT VARIABLES, lines 177-180 in `utilities/sh.html`.

GPT-5.4 review: This quote correctly matches the same POSIX source text as `SHALL-20-110-08-005`, making this a duplicate extraction rather than a separate behavior row. The canonical behavior remains blocked because authoritative coverage would require interactive `fc` editing behavior.

## Implementation Notes

GPT-5.4 review: no separate canonical `tests/spec` script is needed for this duplicate row.
