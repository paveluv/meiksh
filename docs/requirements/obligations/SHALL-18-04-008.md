---
id: SHALL-18-04-008
category: shall
section: "1.4 Utility Description Defaults"
anchor: tag_18_04
source_file: utilities/V3_chap01.html
source_lines: [1412, 1427]
keywords: ["shall", "may"]
tier: 2
status: enriched
refs: ["../functions/printf.html", "../basedefs/V1_chap05.html#tag_05", "../basedefs/V1_chap03.html#tag_03_107"]
related_sections: []
tests: []
---

## Extracted Text

> The STDOUT section completely describes the standard output of the utility. This section is frequently merely a reference to the following section, OUTPUT FILES, because many utilities treat standard output and output files in the same manner. Use of a terminal for standard output may cause any of the standard utilities that write standard output to stop when used in the background. For this reason, applications should not use interactive features in scripts to be placed in the background. Record formats are described in a notation similar to that used by the C-language function, printf(). See XBD 5. File Format Notation for a description of this notation. The specified standard output of the standard utilities shall not depend on the existence or value of the environment variables defined in this volume of POSIX.1-2024, except as provided by this volume of POSIX.1-2024. Some of the standard utilities describe their output using the verb display, defined in XBD 3.107 Display. Output described in the STDOUT sections of such utilities may be produced using means other than standard output. When standard output is directed to a terminal, the output described shall be written directly to the terminal. Otherwise, the results are undefined. Default Behavior: When this section is listed as "Not used.", it means that the standard output shall not be written when the utility is used as described by this volume of POSIX.1-2024.

## Context

For STDOUT handling defaults: (1) Using a terminal for stdout may cause builtins to stop (SIGTTOU) in the background. (2) Stdout output formats are described using `printf()`-like notation per XBD 5 (File Format Notation). (3) The stdout format **shall not** depend on environment variables except as explicitly specified. (4) Some builtins use the verb "display" (XBD 3.107) meaning output may go to means other than stdout when directed to a terminal, but when stdout is a terminal the described output **shall** be written directly to it; otherwise results are undefined. (5) When STDOUT is "Not used.", stdout **shall not** be written. Reference: XCU 1.4, XBD 5, XBD 3.107.

## Implementation Notes

*(to be filled during implementation)*
