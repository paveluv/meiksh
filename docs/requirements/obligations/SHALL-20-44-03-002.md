---
id: SHALL-20-44-03-002
category: shall
section: "DESCRIPTION"
anchor: tag_20_44_03
source_file: utilities/fc.html
source_lines: [45, 50]
keywords: ["implementation-defined", "shall", "may"]
tier: 2
spec_tested: blocked
testing_blockers: "fc history numbering requires interactive shell with persistent command history; not testable in non-interactive spec test"
status: enriched
refs: []
related_sections: []
tests: []
---

## Extracted Text

> The command history list shall reference commands by number. The first number in the list is selected arbitrarily. The relationship of a number to its command shall not change except when the user logs in and no other process is accessing the list, at which time the system may reset the numbering to start the oldest retained command at another number (usually 1). When the number reaches an implementation-defined upper limit, which shall be no smaller than the value in HISTSIZE or 32767 (whichever is greater), the shell may wrap the numbers, starting the next command with a lower number (usually 1). However, despite this optional wrapping of numbers, fc shall maintain the time-ordering sequence of the commands. For example, if four commands in sequence are given the numbers 32766, 32767, 1 (wrapped), and 2 as they are executed, command 32767 is considered the command previous to 1, even though its number is higher.

## Context

The shell's command history list must assign sequential numbers to each command. Key requirements: (1) Each command gets a unique number. (2) The number-to-command mapping is stable during a session. (3) The numbering may only reset at login when no other process accesses the history. (4) The upper limit for command numbers is implementation-defined but must be at least max(HISTSIZE, 32767). (5) When wrapping occurs, `fc` must preserve time-ordering — commands are always ordered by when they were executed, not by their assigned number. The implementation must handle wrap-around correctly so that, e.g., `fc -l` and range selections work across the wrap boundary. Reference: POSIX.1-2024 §fc, DESCRIPTION (tag_20_44_03).

## Implementation Notes

*(to be filled during implementation)*
