---
id: SHALL-19-05-03-032
category: shall
section: "2.5.3 Shell Variables"
anchor: tag_19_05_03
source_file: utilities/V3_chap02.html
source_lines: [647, 654]
keywords: ["unspecified", "shall"]
tier: 1
spec_tested: true
status: enriched
refs: ["javascript:open_code('UP')", "#set", "#tag_19_06_02", "#tag_19_06_03", "#tag_19_06_04"]
related_sections: []
tests: []
---

## Extracted Text

> The following variables shall affect the execution of the shell:: [UP] The processing of the PS4 shell variable shall be supported if the system supports the User Portability Utilities option. When an execution trace (set -x) is being performed, before each line in the execution trace, the value of this variable shall be subjected to parameter expansion (see 2.6.2 Parameter Expansion). Whether the value is also subjected to command substitution (see 2.6.3 Command Substitution) or arithmetic expansion (see 2.6.4 Arithmetic Expansion) or both is unspecified. After expansion, the value shall be written to standard error. The default value shall be "+ ".

## Context

This defines the full semantics of the `PS4` variable (execution trace prefix). Tagged `[UP]`.

**Required behavior:**
- Processing of `PS4` must be supported (meiksh targets UP compliance).
- When execution tracing is active (`set -x`), before each traced line in the execution trace, the value of `PS4` is expanded and written to standard error.
- **Expansion rules:**
  - PS4 must be subjected to parameter expansion (2.6.2).
  - Whether command substitution (2.6.3) and/or arithmetic expansion (2.6.4) are also performed is unspecified (implementation choice).
- **Default value:** `"+ "`.
- **Output:** Written to standard error (fd 2), preceding each traced command.

**Conditions:** Active only when `set -x` (xtrace) is in effect. Applies in both interactive and non-interactive shells.

**Implementation note:** A common pattern is `PS4='+ ${LINENO}: '` to include line numbers in traces. The shell should expand PS4 in the current execution environment's context for each traced command.

See 2.5.3 "Shell Variables" (`PS4`), `set` built-in, and 2.6.2–2.6.4 in `utilities/V3_chap02.html`.

## Implementation Notes

*(to be filled during implementation)*
