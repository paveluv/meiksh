---
id: SHALL-19-02-04-002
category: shall
section: "2.2.4 Dollar-Single-Quotes"
anchor: tag_19_02_04
source_file: utilities/V3_chap02.html
source_lines: [168, 169]
keywords: ["shall"]
tier: 1
spec_tested: true
status: enriched
refs: []
related_sections: []
tests: []
---

## Extracted Text

> A sequence of characters starting with a <dollar-sign> immediately followed by a single-quote ($') shall preserve the literal value of all characters up to an unescaped terminating single-quote ('), with the exception of certain <backslash>-escape sequences, as follows:: \" yields a <quotation-mark> (double-quote) character, but note that <quotation-mark> can be included unescaped.

## Context

**Behavior:** Inside `$'...'`, the escape sequence `\"` yields a literal double-quote character (`"`). However, an unescaped double-quote can also be included directly without escaping—the `\"` form is provided for convenience/compatibility but is not required.

**Applies to:** All shell modes during `$'...'` escape processing.

**Implementation detail:** When parsing a `$'...'` string and encountering `\`, check if the next character is `"`. If so, emit `"` (0x22) and advance past both characters. An unescaped `"` is also valid and produces the same byte.

**Spec reference:** Section 2.2.4, lines 168-169 in `utilities/V3_chap02.html`.

## Implementation Notes

*(to be filled during implementation)*
