---
id: SHALL-20-132-14-003
category: shall
section: "EXIT STATUS"
anchor: tag_20_132_14
source_file: utilities/umask.html
source_lines: [150, 150]
keywords: ["shall"]
tier: 2
status: enriched
spec_tested: true
refs: []
related_sections: []
tests:
  - ../../../tests/spec/SHALL-20-132-14-003.sh
reviewed: GPT-5.4
---

## Extracted Text

> The file mode creation mask was successfully changed, or no mask operand was supplied.

## Context

**Behavior:** This is the description for exit status 0 of `umask`. Exit status 0 shall be returned when:
1. A mask operand was provided and the file mode creation mask was successfully changed to the specified value, OR
2. No mask operand was provided (query mode) and the current mask value was successfully written to stdout.

**Spec reference:** Section EXIT STATUS (tag_20_132_14) in `utilities/umask.html`, line 150.

GPT-5.4 review: The prior extracted text incorrectly merged the EXIT STATUS heading with the exit-0 description. The corrected quote now matches the actual POSIX success description in `docs/posix/utilities/umask.html#tag_20_132_14`. The enrichment is accurate and usefully distinguishes successful set mode from successful query mode.

## Implementation Notes

GPT-5.4 review of `tests/spec/SHALL-20-132-14-003.sh`: the script covers successful query mode, octal set mode, and symbolic set mode, which is a strong match for the success description in this row.
