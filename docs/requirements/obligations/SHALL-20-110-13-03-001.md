---
id: SHALL-20-110-13-03-001
category: shall
section: "Command Line Editing (vi-mode)"
anchor: tag_20_110_13_03
source_file: utilities/sh.html
source_lines: [328, 328]
keywords: ["shall"]
tier: 1
status: enriched
refs: ["../utilities/vi.html"]
related_sections: []
tests: []
spec_tested: blocked
testing_blockers: "requires interactive terminal with vi-mode line editing; cannot test in batch mode"
reviewed: GPT-5.4
---

## Extracted Text

> In vi editing mode, there shall be a distinguished line, the edit line. All the editing operations which modify a line affect the edit line. The edit line is always the newest line in the command history buffer.

## Context

**Behavior:** In vi editing mode, there is a distinguished line called the **edit line**. All editing operations that modify a line affect the edit line. The edit line is always the newest line in the command history buffer (i.e., the line currently being composed or edited).

**Implementation:** The line editor must maintain an "edit line" concept — this is the current input buffer. When the user navigates history (e.g., with `k`/`j` in command mode), the edit line is replaced with the historical entry, but the edit line always represents the newest position in the history buffer. Modifications always affect this line.

**Applies to:** Interactive shell in vi editing mode.

**Spec reference:** Command Line Editing (vi-mode) section in `utilities/sh.html`, line 328.

## Implementation Notes

GPT-5.4 review: This quote matches `docs/posix/issue8/sh-utility.html#tag_20_110_13_03`. It defines the vi-mode edit line and ties it to the newest line in the command history buffer, so a PTY-driven interactive harness is needed for canonical verification.
