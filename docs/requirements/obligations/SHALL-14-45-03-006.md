---
id: SHALL-14-45-03-006
category: shall
section: "DESCRIPTION"
anchor: tag_14_45_03
source_file: basedefs/signal.h.html
source_lines: [65, 65]
keywords: ["shall"]
tier: 2
status: enriched
refs: []
related_sections: []
tests: []
---

## Extracted Text

> The <signal.h> header shall define the following data types:: Possibly volatile-qualified integer type of an object that can be accessed as an atomic entity, even in the presence of asynchronous interrupts.

## Context

This describes `sig_atomic_t` semantics: it must be an integer type (possibly volatile-qualified) that supports atomic access even during asynchronous interrupts. The shell's signal handlers must use `volatile sig_atomic_t` for any variables shared between signal handler context and normal execution context. This guarantees that reads and writes to such variables are not torn or reordered. The type need not support compound operations (increment, etc.)—only simple load and store are guaranteed atomic.

## Implementation Notes

*(to be filled during implementation)*
