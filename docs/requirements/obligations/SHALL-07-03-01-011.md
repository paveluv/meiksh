---
id: SHALL-07-03-01-011
category: shall
section: "7.3.1 LC_CTYPE"
anchor: tag_07_03_01
source_file: basedefs/V1_chap07.html
source_lines: [315, 319]
keywords: ["shall"]
tier: 2
status: enriched
refs: []
related_sections: []
tests: []
spec_tested: true
---

## Extracted Text

> Define characters to be classified as punctuation characters. In the POSIX locale, neither the <space> nor any characters in classes alpha, digit, or cntrl shall be included. In a locale definition file, no character specified for the keywords upper, lower, alpha, digit, cntrl, xdigit, or as the <space> shall be specified.

## Context

**Behavior:** The `punct` keyword defines the set of punctuation characters:
- In the POSIX locale: characters that are printable but not space, alpha, digit, or cntrl. This includes: `! " # $ % & ' ( ) * + , - . / : ; < = > ? @ [ \ ] ^ _ ` { | } ~`.
- Constraint: No character in `punct` may also be in `upper`, `lower`, `alpha`, `digit`, `cntrl`, `xdigit`, or be <space>.

**Shell relevance:** The shell uses the `punct` class for:
- `[[:punct:]]` bracket expression in pattern matching.
- Many punctuation characters have special meaning in shell syntax (`$`, `|`, `&`, `;`, etc.), so the shell processes them as operators/metacharacters before character classification applies.

**Spec reference:** Section 7.3.1, lines 315-319 in `basedefs/V1_chap07.html`.

## Implementation Notes

*(to be filled during implementation)*
