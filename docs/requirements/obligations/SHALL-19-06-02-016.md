---
id: SHALL-19-06-02-016
category: shall
section: "2.6.2 Parameter Expansion"
anchor: tag_19_06_02
source_file: utilities/V3_chap02.html
source_lines: [987, 988]
keywords: ["shall"]
tier: 1
status: enriched
refs: []
related_sections: []
tests: []
---

## Extracted Text

> Remove Largest Prefix Pattern. The word shall be expanded to produce a pattern. The parameter expansion shall then result in parameter, with the largest portion of the prefix matched by the pattern deleted.

## Context

**Behavior:** The `${parameter##word}` expansion (Remove Largest Prefix Pattern):
1. Expand `word` to produce a pattern (using pattern matching notation per 2.14).
2. Match the pattern against the **prefix** (beginning) of the value of `parameter`.
3. Find the **largest** (longest) matching prefix.
4. Return the value of `parameter` with that largest prefix removed.

**Inputs/Outputs:**
- `x="/usr/local/bin"; echo ${x##*/}` → `bin` (removes longest prefix matching `*/`, which is `/usr/local/`)
- `x="file.tar.gz"; echo ${x##*.}` → `gz` (removes `file.tar.`, the longest prefix ending in `.`)

**Spec reference:** See 2.6.2 Parameter Expansion in utilities/V3_chap02.html, lines 986–988.

## Implementation Notes

*(to be filled during implementation)*
