---
id: SHALL-20-64-08-007
category: shall
section: "ENVIRONMENT VARIABLES"
anchor: tag_20_64_08
source_file: utilities/kill.html
source_lines: [171, 172]
keywords: ["shall"]
tier: 3
spec_tested: blocked
testing_blockers: "LC_CTYPE byte-sequence interpretation is libc locale infrastructure; no shell-specific behavior to test"
status: enriched
refs: []
related_sections: []
tests: []
reviewed: GPT-5.4
---

## Extracted Text

> Determine the locale for the interpretation of sequences of bytes of text data as characters (for example, single-byte as opposed to multi-byte characters in arguments).

## Context

**Behavior:** `LC_CTYPE` determines the locale for interpreting byte sequences in arguments as characters, including single-byte versus multi-byte handling.

**Spec reference:** Section ENVIRONMENT VARIABLES (tag_20_64_08) in `utilities/kill.html`, lines 171-172.

GPT-5.4 review: The earlier extracted text merged the section heading with the `LC_CTYPE` description and also truncated the source span. The corrected quote now matches the actual POSIX description in `docs/posix/utilities/kill.html#tag_20_64_08`. The previous commentary also went a bit beyond the cited text by speculating about specific impacts on `kill`; the tighter wording above stays closer to the POSIX locale description.

## Implementation Notes

GPT-5.4 review: no dedicated `tests/spec` script exists for this locale-framework row.
