---
id: SHALL-09-04-03-003
category: shall
section: "9.4.3 ERE Special Characters"
anchor: tag_09_04_03
source_file: basedefs/V1_chap09.html
source_lines: [507, 508]
keywords: ["shall"]
tier: 2
status: enriched
refs: ["#tag_09_03_05"]
related_sections: []
tests: []
---

## Extracted Text

> An ERE special character has special properties in certain contexts. Outside those contexts, or when preceded by an unescaped <backslash>, such a character shall be an ERE that matches the special character itself. The extended regular expression special characters and the contexts in which they shall have their special meaning are as follows:: The <period>, <left-square-bracket>, <backslash>, and <left-parenthesis> shall be special except when used in a bracket expression (see 9.3.5 RE Bracket Expression). When not inside a bracket expression, an unescaped <left-parenthesis> immediately followed by a <right-parenthesis> produces undefined results. A <left-square-bracket> that is unescaped and is not part of a bracket expression also produces undefined results.

## Context

The characters `.`, `[`, `\`, and `(` must be treated as special (with their ERE semantics) everywhere except inside a bracket expression `[...]`. Inside bracket expressions, they match themselves literally. Two undefined-result cases apply: (1) An unescaped `(` immediately followed by `)` (i.e., `()`) outside a bracket expression produces undefined results—the implementation may reject it, treat it as matching the empty string, or do anything else. (2) An unescaped `[` that does not begin a valid bracket expression also produces undefined results. The ERE engine should handle or reject these gracefully but is not required to produce any specific behavior.

## Implementation Notes

*(to be filled during implementation)*
