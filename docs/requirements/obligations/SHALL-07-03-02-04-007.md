---
id: SHALL-07-03-02-04-007
category: shall
section: "7.3.2.4 Collation Order"
anchor: tag_07_03_02_04
source_file: basedefs/V1_chap07.html
source_lines: [2543, 2547]
keywords: ["shall"]
tier: 2
status: enriched
refs: []
related_sections: []
tests: []
---

## Extracted Text

> The optional operands for each collation-element shall be used to define the primary, secondary, or subsequent weights for the collating element. The first operand specifies the relative primary weight, the second the relative secondary weight, and so on. Two or more collation-elements can be assigned the same weight; they belong to the same "equivalence class" if they have the same primary weight. Collation shall behave as if, for each weight level, elements subject to IGNORE are removed, unless the position collation directive is specified for the corresponding level with the order_start keyword. Then each successive pair of elements shall be compared according to the relative weights for the elements. If the two strings compare equal, the process shall be repeated for the next weight level, up to the limit {COLL_WEIGHTS_MAX}.

## Context

Each collating element in the LC_COLLATE order section can have optional weight operands specifying primary, secondary, and subsequent weights. Elements with the same primary weight form an equivalence class (shell bracket expressions can reference these via `[=a=]` syntax). Multi-level collation works as follows: (1) for each weight level, IGNORE elements are removed (unless `position` is active); (2) successive element pairs are compared by their weights at that level; (3) if equal, the next weight level is tried, up to {COLL_WEIGHTS_MAX} levels. The shell is affected because equivalence classes in bracket expressions and collation-based range expressions depend on this weight system. The shell uses system collation functions (`strcoll()`) that implement this multi-level comparison.

See: `basedefs/V1_chap07.html` §7.3.2.4 Collation Order.

## Implementation Notes

*(to be filled during implementation)*
