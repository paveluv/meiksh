---
id: SHALL-20-64-10-002
category: shall
section: "STDOUT"
anchor: tag_20_64_10
source_file: utilities/kill.html
source_lines: [189, 189]
keywords: ["shall"]
tier: 3
spec_tested: true
status: enriched
refs: []
related_sections: []
tests: []
reviewed: GPT-5.4
---

## Extracted Text

> When the -l option is specified, the symbolic name of each signal shall be written in the following format:

## Context

**Behavior:** When `kill -l` is specified without an `exit_status` operand (list-all mode), each supported signal name must be written to stdout in this format:

```
"%s%c", <signal_name>, <separator>
```

Where `<signal_name>` is uppercase without the `SIG` prefix, and `<separator>` is either a space or newline (see SHALL-20-64-10-003 for separator rules).

**Output:** One entry per signal, with signal names like `HUP`, `INT`, `QUIT`, `KILL`, `TERM`, etc.

**Spec reference:** Section STDOUT (tag_20_64_10) in `utilities/kill.html`, line 189.

GPT-5.4 review: The extracted quote is faithful to `docs/posix/utilities/kill.html#tag_20_64_10`. The enrichment is sound, but the precise format constraints are completed by the immediately following text in the same POSIX section: `"%s%c"` output, uppercase names, no `SIG` prefix, and newline-or-space separators with a final newline. This obligation is therefore best read together with the nearby STDOUT requirements rather than in isolation.

## Implementation Notes

GPT-5.4 review of `tests/spec/SHALL-20-64-10-002.sh`: the revised script still checks that `kill -l` writes names, and now also asserts successful completion and rejects `SIG`-prefixed output as a cheap guard that the output is in the POSIX signal-name form from `docs/posix/utilities/kill.html#tag_20_64_10`.
