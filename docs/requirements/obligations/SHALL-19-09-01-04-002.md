---
id: SHALL-19-09-01-04-002
category: shall
section: "2.9.1.4 Command Search and Execution"
anchor: tag_19_09_01_04
source_file: utilities/V3_chap02.html
source_lines: [1815, 1926]
keywords: ["unspecified", "shall", "may"]
tier: 1
status: enriched
spec_tested: true
refs: ["#tag_19_09_05", "#unset", "../utilities/V3_chap01.html#tag_18_07", "../basedefs/V1_chap08.html#tag_08", "#tag_19_09_01_06"]
related_sections: []
tests: []
---

## Extracted Text

> If a simple command has a command name and an optional list of arguments after word expansion (see 2.9.1.1 Order of Processing), the following actions shall be performed:: If the command name does not contain any <slash> characters, the first successful step in the following sequence shall occur: If the command name matches the name of a special built-in utility, that special built-in utility shall be invoked. If the command name matches the name of a utility listed in the following table, the results are unspecified. alloc autoload bind bindkey builtin bye caller cap chdir clone comparguments compcall compctl compdescribe compfiles compgen compgroups complete compound compquote comptags comptry compvalues declare dirs disable disown dosh echotc echoti enum float help history hist integer let local login logout map mapfile nameref popd print pushd readarray repeat savehistory source shopt stop suspend typeset whence If the command name matches the name of a function known to this shell, the function shall be invoked as described in 2.9.5 Function Definition Command. If the implementation has provided a standard utility in the form of a function, and that function definition still exists (i.e. has not been removed using unset -f or replaced via another function definition with the same name), it shall not be recognized at this point. It shall be invoked in conjunction with the path search in step 1e. If the command name matches the name of an intrinsic utility (see 1.7 Intrinsic Utilities), that utility shall be invoked. Otherwise, the command shall be searched for using the PATH environment variable as described in XBD 8. Environment Variables: If the search is successful: If the system has implemented the utility as a built-in or as a shell function, and the built-in or function is associated with the directory that was most recently tested during the successful PATH search, that built-in or function shall be invoked. Otherwise, the shell shall execute a non-built-in utility as described in 2.9.1.6 Non-built-in Utility Execution. Once a utility has been searched for and found (either as a result of this specific search or as part of an unspecified shell start-up activity), an implementation may remember its location and need not search for the utility again unless the PATH variable has been the subject of an assignment. If the remembered location fails for a subsequent invocation, the shell shall repeat the search to find the new location for the utility, if any. If the search is unsuccessful, the command shall fail with an exit status of 127 and the shell shall write an error message.

## Context

**Behavior:** Complete command search algorithm for commands WITHOUT slashes. The shell MUST try these steps in order, using the first that succeeds:

1. **Special builtins:** If the name matches a special built-in (break, colon, continue, dot, eval, exec, exit, export, readonly, return, set, shift, times, trap, unset), invoke it.
2. **Reserved names:** If the name matches one of ~60 reserved utility names (alloc, autoload, bind, builtin, declare, dirs, disown, help, history, let, local, login, logout, popd, pushd, shopt, source, suspend, typeset, whence, etc.), results are UNSPECIFIED.
3. **Functions:** If the name matches a known shell function, invoke it per 2.9.5. EXCEPTION: if the implementation provided a standard utility as a function and it still exists (not removed via `unset -f`), it is NOT recognized here — it's found during PATH search in step 1e instead.
4. **Intrinsic utilities:** If the name matches an intrinsic utility (see 1.7), invoke it.
5. **PATH search:** Search PATH per XBD 8 Environment Variables:
   - If found and the system implements it as a builtin/function associated with the matched directory, invoke that.
   - Otherwise, execute as non-builtin per 2.9.1.6.
   - The shell MAY cache PATH lookup results and reuse them until PATH is reassigned. If a cached location fails, repeat the search.
   - If not found, fail with exit status 127 and write an error message.

**Applies to:** All simple commands where the command name contains no `/` character.

**Implementation detail:** The command lookup must implement this priority chain. The hash table (PATH cache) is optional but common. The reserved-name list establishes names that meiksh need not implement but must handle (unspecified behavior is acceptable).

**Spec reference:** Section 2.9.1.4, lines 1815-1926 in `utilities/V3_chap02.html`. Cross-refs: 2.9.5 Function Definition, 1.7 Intrinsic Utilities (`utilities/V3_chap01.html`), XBD 8 Environment Variables (`basedefs/V1_chap08.html`), 2.9.1.6 Non-built-in Utility Execution.

## Implementation Notes

*(to be filled during implementation)*
