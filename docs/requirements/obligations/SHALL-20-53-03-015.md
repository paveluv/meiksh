---
id: SHALL-20-53-03-015
category: shall
section: "DESCRIPTION"
anchor: tag_20_53_03
source_file: utilities/getopts.html
source_lines: [80, 85]
keywords: ["shall"]
tier: 2
status: enriched
refs: []
related_sections: []
tests: []
---

## Extracted Text

> When the end of options is encountered, the getopts utility shall exit with a return value of one; the shell variable OPTIND shall be set to the index of the argument containing the first operand in the parameter list, or the value 1 plus the number of elements in the parameter list if there are no operands in the parameter list; the name variable shall be set to the <question-mark> character. Any of the following shall identify the end of options: the first "--" element of the parameter list that is not an option-argument, finding an element of the parameter list that is not an option-argument and does not begin with a '-', or encountering an error.

## Context

**Behavior:** When getopts reaches the end of options, it must:
1. Return exit status **1** (not 0).
2. Set `OPTIND` to the index of the first non-option operand in the parameter list, or `$# + 1` (or param count + 1) if there are no operands.
3. Set the `name` variable to `?`.

**End-of-options is triggered by any of:**
- The first `--` argument that is not itself an option-argument (e.g., `cmd -a -- foo` — `--` ends options).
- An argument that does not start with `-` and is not an option-argument (e.g., `cmd -a foo` — `foo` ends options).
- Encountering an error condition.

**Key detail:** After end-of-options, scripts typically use `shift $((OPTIND - 1))` to remove processed options, leaving remaining operands in `$@`.

**Spec reference:** POSIX.1-2024, `getopts` DESCRIPTION, lines 80-85 of `utilities/getopts.html`.

## Implementation Notes

*(to be filled during implementation)*
