---
id: SHALL-07-03-02-004
category: shall
section: "7.3.2 LC_COLLATE"
anchor: tag_07_03_02
source_file: basedefs/V1_chap07.html
source_lines: [2406, 2407]
keywords: ["shall"]
tier: 2
status: enriched
refs: ["../basedefs/limits.h.html"]
related_sections: []
tests: []
---

## Extracted Text

> A collation sequence definition shall define the relative order between collating elements (characters and multi-character collating elements) in the locale. This order is expressed in terms of collation values; that is, by assigning each element one or more collation values (also known as collation weights). This does not imply that implementations shall assign such values, but that ordering of strings using the resultant collation definition in the locale behaves as if such assignment is done and used in the collation process. At least the following capabilities are provided:: Multiple weights and equivalence classes. Collating elements can be assigned one or more (up to the limit {COLL_WEIGHTS_MAX}, as defined in <limits.h>) collating weights for use in sorting. The first weight is hereafter referred to as the primary weight.

## Context

**Behavior:** LC_COLLATE must support multiple collation weights per element, up to `{COLL_WEIGHTS_MAX}` (defined in `<limits.h>`). The first weight is the "primary weight." Multiple weights enable multi-level sorting (e.g., first by base character, then by accent, then by case).

**Shell relevance:** The shell's bracket expression range matching uses the basic collation sequence (primary weight). Multiple weights primarily affect `strcoll()` string comparison and sorting utilities. The shell itself doesn't typically use secondary/tertiary weights directly, but the `[=x=]` equivalence class notation in bracket expressions is related to primary weight equality.

**Cross-references:** `basedefs/limits.h.html` — a C header file, not shell-relevant for extraction.

**Spec reference:** Section 7.3.2, lines 2406-2407 in `basedefs/V1_chap07.html`.

## Implementation Notes

*(to be filled during implementation)*
