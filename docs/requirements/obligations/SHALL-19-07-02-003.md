---
id: SHALL-19-07-02-003
category: shall
section: "2.7.2 Redirecting Output"
anchor: tag_19_07_02
source_file: utilities/V3_chap02.html
source_lines: [1379, 1383]
keywords: ["shall"]
tier: 1
status: enriched
refs: ["../functions/open.html"]
related_sections: []
tests: []
---

## Extracted Text

> In all other cases (noclobber not set, redirection using '>' does not fail for the reasons stated above, or redirection using the ">|" format), output redirection shall cause the file whose name results from the expansion of word to be opened for output on the designated file descriptor, or standard output if none is specified. If the file does not exist, it shall be created as an empty file; otherwise, it shall be opened as if the open() function was called with the O_TRUNC flag set.

## Context

Defines the normal (non-noclobber-blocked) behavior of output redirection. See 2.7.2 Redirecting Output in utilities/V3_chap02.html.

**When this applies:** Any of these conditions:
1. The noclobber option is not set (`set +C`), OR
2. Noclobber is set but the `>` redirection did not fail (target is not a regular file or symlink to one), OR
3. The `>|` operator is used (explicit clobber override)

**Behavior required:**
- Open the file named by the expansion of `word` for writing on the designated fd (or fd 1 if `n` is omitted).
- If the file does NOT exist: create it as an empty file (equivalent to `open()` with `O_WRONLY | O_CREAT | O_TRUNC`, mode typically 0666 modified by umask).
- If the file DOES exist: truncate it to zero length (equivalent to `open()` with `O_WRONLY | O_TRUNC`).

**Error conditions:** If the open/create fails, this is a redirection error (see SHALL-19-07-005).

## Implementation Notes

*(to be filled during implementation)*
