---
id: SHALL-20-110-05-015
category: shall
section: "OPERANDS"
anchor: tag_20_110_05
source_file: utilities/sh.html
source_lines: [119, 122]
keywords: ["shall"]
tier: 1
status: enriched
refs: []
related_sections: []
tests:
  - ../../../tests/spec/SHALL-20-110-05-009.sh
spec_tested: blocked
testing_blockers: "Duplicate of SHALL-20-110-05-009; tested there"
reviewed: GPT-5.4
---

## Extracted Text

> A string assigned to special parameter 0 when executing the commands in command_string. If command_name is not specified, special parameter 0 shall be set to the value of the first argument passed to sh from its parent (for example, argv[0] for a C program), which is normally a pathname used to execute the sh utility.

## Context

Duplicate of SHALL-20-110-05-009 (same source lines 119-122). Covers the `command_name` operand and `$0` assignment for `-c` mode.

See SHALL-20-110-05-009 for the full context. See utilities/sh.html#tag_20_110_05.

GPT-5.4 review: This quote correctly matches the same POSIX text as `SHALL-20-110-05-009`, making this a duplicate extraction rather than a separate behavior row. The canonical executable coverage belongs to `tests/spec/SHALL-20-110-05-009.sh`.

## Implementation Notes

GPT-5.4 review: no separate canonical `tests/spec` script is needed for this duplicate row.
