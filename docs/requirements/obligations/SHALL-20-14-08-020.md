---
id: SHALL-20-14-08-020
category: shall
section: "ENVIRONMENT VARIABLES"
anchor: tag_20_14_08
source_file: utilities/cd.html
source_lines: [179, 181]
keywords: ["shall"]
tier: 2
spec_tested: true
status: enriched
refs: []
related_sections: []
tests: []
---

## Extracted Text

> A <colon>-separated list of pathnames that refer to directories. The cd utility shall use this list in its attempt to change the directory, as described in the DESCRIPTION. An empty string in place of a directory pathname represents the current directory. If CDPATH is not set, it shall be treated as if it were an empty string.

## Context

**Behavior:** Duplicate extraction of the CDPATH description (same as SHALL-20-14-08-003). The cd utility must use CDPATH as a colon-separated search path when resolving relative directory operands:

1. Each entry in CDPATH is tested by concatenating it with the directory operand.
2. Empty entries (between colons, or leading/trailing) represent the current directory (`.`).
3. If CDPATH is not set (unset), treat it as an empty string — effectively searching only the current directory.
4. If CDPATH is set to an empty string, the same behavior applies.

**Spec reference:** POSIX.1-2024, `utilities/cd.html`, section ENVIRONMENT VARIABLES (tag_20_14_08), lines 179-181.

## Implementation Notes

*(to be filled during implementation)*
