---
id: SHALL-19-07-02-002
category: shall
section: "2.7.2 Redirecting Output"
anchor: tag_19_07_02
source_file: utilities/V3_chap02.html
source_lines: [1367, 1378]
keywords: ["shall", "may"]
tier: 1
spec_tested: true
status: enriched
refs: ["#set", "../functions/open.html", "../xrat/V4_xcu_chap01.html#tag_23_02_07_02", "../xrat/V4_xcu_chap01.html#tag_23_02_07_02"]
related_sections: []
tests: []
---

## Extracted Text

> Output redirection using the '>' format shall fail if the noclobber option is set (see the description of set -C) and the file named by the expansion of word exists and is either a regular file or a symbolic link that resolves to a regular file; it may also fail if the file is a symbolic link that does not resolve to an existing file. The check for existence, file creation, and open operations shall be performed atomically as is done by the open() function as defined in System Interfaces volume of POSIX.1-2024 when the O_CREAT and O_EXCL flags are set, except that if the file exists and is a symbolic link, the open operation need not fail with [EEXIST] unless the symbolic link resolves to an existing regular file. Performing these operations atomically ensures that the creation of lock files and unique (often temporary) files is reliable, with important caveats detailed in C.2.7.2 Redirecting Output. The check for the type of the file need not be performed atomically with the check for existence, file creation, and open operations. If not, there is a potential race condition that may result in a misleading shell diagnostic message when redirection fails. See XRAT C.2.7.2 Redirecting Output for more details.

## Context

Defines the noclobber (`set -C`) protection for output redirection with `>`. See 2.7.2 Redirecting Output in utilities/V3_chap02.html.

**Behavior required when noclobber is set (`set -C`):**
- `>word` shall FAIL if the expanded filename names an existing regular file.
- `>word` shall FAIL if the expanded filename names a symbolic link that resolves to an existing regular file.
- `>word` MAY also fail if the expanded filename names a symbolic link that does not resolve to an existing file.
- The `>|` operator bypasses noclobber and always proceeds normally.

**Atomicity requirement:**
- The check for file existence, file creation, and open must be performed atomically, equivalent to `open()` with `O_CREAT | O_EXCL` flags.
- Exception: if the file exists and is a symlink, the open need not fail with `EEXIST` unless the symlink resolves to an existing regular file.
- The file type check (regular file vs. other) need NOT be atomic with the existence/create/open check (POSIX acknowledges a potential race condition here).

**Error handling:** If noclobber causes failure, this is a redirection error. Use `>|` to override. The `>|` operator must always be available as an escape hatch from noclobber.

**Key references:** `set -C` option, `open()` with O_CREAT|O_EXCL semantics.

## Implementation Notes

*(to be filled during implementation)*
