---
id: SHALL-20-132-05-004
category: shall
section: "OPERANDS"
anchor: tag_20_132_05
source_file: utilities/umask.html
source_lines: [69, 80]
keywords: ["unspecified", "shall"]
tier: 2
status: enriched
spec_tested: true
refs: ["../utilities/chmod.html"]
related_sections: []
tests:
  - ../../../tests/spec/SHALL-20-132-05-004.sh
reviewed: GPT-5.4
---

## Extracted Text

> A string specifying the new file mode creation mask. The string is treated in the same way as the mode operand described in the EXTENDED DESCRIPTION section for chmod. For a symbolic_mode value, the new value of the file mode creation mask shall be the logical complement of the file permission bits portion of the file mode specified by the symbolic_mode string. In a symbolic_mode value, the permissions op characters '+' and '-' shall be interpreted relative to the current file mode creation mask; '+' shall cause the bits for the indicated permissions to be cleared in the mask; '-' shall cause the bits for the indicated permissions to be set in the mask. The interpretation of mode values that specify file mode bits other than the file permission bits is unspecified. In the octal integer form of mode, the specified bits are set in the file mode creation mask. The file mode creation mask shall be set to the resulting numeric value. The default output of a prior invocation of umask on the same system with no operand also shall be recognized as a mask operand.

## Context

**Behavior:** This obligation details the mask operand semantics for `umask`. Key requirements:

1. **Symbolic mode parsing:** Shall use the same grammar as `chmod`'s mode operand. For symbolic modes, the `+` operator clears the indicated permission bits in the mask (making them allowed) and `-` sets the indicated bits in the mask (making them denied).

2. **Complement relationship:** For a complete symbolic_mode (e.g., `a=rwx`), the new mask is the logical complement of the file permission bits specified. So `umask a=rx` yields mask 0222 (write bits set in mask).

3. **Relative operations:** `+` and `-` in symbolic modes are relative to the *current* mask value, not an absolute starting point.

4. **Octal form:** Octal values (e.g., `022`) are set directly as the mask.

5. **Final mask assignment:** After all mode calculations, the file mode creation mask shall be set to the resulting numeric value.

6. **Round-trip acceptance:** The default output of a previous `umask` invocation shall be accepted as a mask operand.

**Spec reference:** Section OPERANDS (tag_20_132_05) in `utilities/umask.html`, lines 69-80.

GPT-5.4 review: The extracted quote matches the shared `mask` operand text in `docs/posix/utilities/umask.html#tag_20_132_05`. This row usefully narrows attention to the octal-form clauses inside that larger paragraph. The enrichment is accurate and keeps the normative distinction between symbolic and octal forms clear.

## Implementation Notes

GPT-5.4 review of `tests/spec/SHALL-20-132-05-004.sh`: the script directly verifies the octal-form requirement by setting an octal mask and confirming that the resulting numeric mask matches.
