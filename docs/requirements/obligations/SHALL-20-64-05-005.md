---
id: SHALL-20-64-05-005
category: shall
section: "OPERANDS"
anchor: tag_20_64_05
source_file: utilities/kill.html
source_lines: [148, 148]
keywords: ["shall"]
tier: 3
spec_tested: true
status: enriched
refs: []
related_sections: []
tests: []
reviewed: GPT-5.4
---

## Extracted Text

> A decimal integer specifying a signal number or the exit status of a process terminated by a signal.

## Context

**Behavior:** The `exit_status` operand is a decimal integer that may represent:
1. A raw signal number (e.g., `9` for SIGKILL) — `kill -l 9` should output `KILL`.
2. The exit status of a process terminated by a signal (typically 128 + signal_number in most shells) — `kill -l 137` should output `KILL` (137 = 128 + 9).

The implementation must handle both interpretations as described by the `-l` option text in `docs/posix/utilities/kill.html#tag_20_64_04`.

**Spec reference:** Section OPERANDS (tag_20_64_05) in `utilities/kill.html`, line 148.

GPT-5.4 review: The earlier extracted text again merged the general operand-list introduction with the specific `exit_status` definition. The corrected quote now matches the exact line from `docs/posix/utilities/kill.html#tag_20_64_05`. The enrichment should also be careful not to treat `128 + signal_number` as a POSIX-mandated encoding; the utility text instead ties interpretation to a decimal signal number or to a `?` value corresponding to a process terminated by a signal.

## Implementation Notes

GPT-5.4 review of `tests/spec/SHALL-20-64-05-005.sh`: the revised script still covers the raw signal-number interpretation and now derives a real signal-terminated child status for the `exit_status` form, which aligns more closely with the corresponding `-l` processing rules in `docs/posix/utilities/kill.html#tag_20_64_04`.
